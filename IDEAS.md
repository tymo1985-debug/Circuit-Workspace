# IDEAS

Идеи проекта без сроков и обязательств. Даже небольшая мысль должна быть
записана здесь. Решили реализовать — идея переезжает в `TODO.md`.
Идеи не удаляются без явной просьбы.

---

## Интерфейс

- **Переименовать мобильный слой Конгрессов.** `js/mobile.js`, `.mobile-bar`,
  `.mobile-menu`, `#mCongressBtn` описывают оболочку, которая с 4.31.0
  работает до 1100px и по кнопке — на любой ширине. Имена вводят в
  заблуждение. Переименование затрагивает `@media print`, четыре места в
  `styles.css` и разметку; делать отдельным заходом, не вместе с правкой
  поведения.

- **Плотный режим письма в «Назначениях»** — переключатель, ужимающий поля и
  межстрочные интервалы, когда список братьев длинный и письмо не помещается на
  одну страницу.
- **Предпросмотр печати прямо в модуле** вместо системного диалога, чтобы было
  видно разбиение на страницы до отправки на принтер.
- **Единый визуальный язык карточек и списков действий во всех модулях.**
  Приёмы, опробованные на карточке ближайшего визита в Клиндарии: главное
  число или статус вперёд отдельным акцентным чипом, заголовок вторым уровнем,
  метаданные приглушённым цветом, статусы тонированными плашками, действия в
  один столбец с иконкой слева. Всё это выражается уже существующими
  `--cw-*`/MD3-токенами и `Material Symbols`, отдельная библиотека не нужна.
  Требует ревизии экранов всех четырёх модулей и разбивки на фазы —
  разом не делать. Первый шаг сделан в Конгрессах 4.28.0 (список заданий на
  телефоне); приёмы оттуда — раскладка `<td>` через `order`, чипы вместо
  подписанных полей, действия по выбору строки — переносимы на остальные.
- **Мобильный вид Конгрессов перегружен и малоинформативен.** Сейчас
  `@media(max-width:680px)` разворачивает таблицу программы в «строка = блок»:
  каждое задание превращается в десять подписанных полей, где подпись («ЧАС»,
  «ХВ.», «РЕП.») длиннее самого значения, а тема — главное содержимое — имеет
  тот же вес, что и длительность. Плюс до программы нужно проскроллить четыре
  экрана служебного: сайдбар со списком конгрессов (вложенный скролл
  `max-height:340px` с красной кнопкой «Сбросить данные»), карточку из трёх
  полей и шесть кнопок действий подряд. Направление правки — не менять логику
  модуля, а перестроить мобильный слой: список конгрессов и поля конгресса —
  в шапку и bottom sheet, шесть кнопок — в меню «ещё» и FAB, строка задания —
  в карточку с иерархией (время + тема вперёд, статусы тонированными метками,
  действия по нажатию). Это частный случай пункта про единый визуальный язык
  карточек выше — делать в одной системе с ним, начиная с Конгрессов как
  самого тяжёлого экрана.

---

## Функции

- **История писем в «Назначениях»** — сохранять отправленные письма с датой и
  составом, чтобы было видно, кого и когда назначали.
- **Экспорт письма в PDF без системного диалога** (jsPDF, как в остальных
  модулях) — даст стабильное имя файла на всех платформах.
- **Список братьев и собраний из общей базы** — подстановка имён из
  `shared/db.js` вместо ручного ввода в каждом модуле. Сейчас каждый модуль
  ведёт свою историю ввода.
- **Поле пола у учащегося Школы.** Письмо-приглашение начинается «Дорогой брат
  / дорогая сестра», потому что различить некого: в карточке учащегося и в
  анкете пола нет. Автоматический выбор обращения потребует поля в
  `Students.DEFAULT_COLUMNS`, вопроса в `registrationSchema.js` и перевода
  новых строк — то есть тянет за собой отдачу текста носителям. Отдельная
  задача, сознательно не вошедшая в фазу 6.
- **Массовая выдача писем Школы** — «письма всем учащимся» одним действием.
  Композер сейчас работает с одним адресатом; пакетная выдача поднимает
  вопросы, которых у одиночной нет (один PDF на всех или файл на каждого, как
  дедуплицировать снимки в архиве).


---

## Производительность

- **Ленивая загрузка шрифтов PDF.** Base64-шрифты для кириллицы весят больше
  самих модулей и грузятся даже когда PDF не собирают.

---

## Архитектура

- **Проверка «переменная CSS используется, но не объявлена».** В Документах
  `--line` стоял в двенадцати правилах и не был объявлен нигде: рамки молча
  рисовались чёрным через `currentColor`, потому что неразрешимая `var()`
  делает объявление недействительным на этапе вычисления, а не откатывает его
  к значению по умолчанию. В тёмной теме этого не видно вовсе. Реализация
  дешёвая и статическая: собрать по всем CSS и инлайновым `<style>` модуля
  использованные `var(--x)` и вычесть объявленные `--x:` (с учётом
  `shared/style.css`, который модуль подключает). Рядом просится вторая
  половина того же прогона — сверка `<body>` каждого модуля с эталоном
  соседей по фону, шрифту и цвету. Условие для переноса в TODO: второй случай.
  Правило добавления проверок — «класс ломался повторно», а этот сломался
  один раз (24.08.2026).

- **Проверка «перевод дословно совпадает с русским».** После заливки 17.08.2026
  таких совпадений было 54, и все оказались законными: марки (PDF, EPUB,
  WhatsApp, Email), форматные строки (`{entity} · {lang}`) и омонимия украинского
  («Телефон», «Транспорт», «причина»). Автоматическая проверка тут даст в
  основном ложные срабатывания, поэтому в гейт не берём — но при следующей
  большой заливке отчёт по этому признаку стоит смотреть глазами: недоперевод
  выглядит ровно так же.

- **Проверка длины подписей в печатных трактах.** Немецкий длиннее русского
  примерно на четверть, и на бланке это уже дважды упиралось в физический край:
  ряд «Другой: ____» (линия для ответа исчезает целиком) и вопрос про
  автомобиль. Сейчас ловится только живым прогоном и только если посмотреть.
  Проверка могла бы мерить ширину каждой подписи тем же шрифтом и падать, если
  под ответ остаётся меньше 20 пунктов. Условие для переноса в TODO: третий
  случай, когда перевод не влез.


- **Экран управления общим справочником собраний, плитки внутри «Документов».**
  Обсуждалось 17.08.2026, отложено — работа не начата, решения не приняты.
  *Проблема:* общий справочник (`CWDB.communities`) не правится нигде. Клиндарий
  обращается к нему всегда по id конкретного события (проверены все 19 точек
  вызова — везде `CWDirectory.get(id)`), а его список строится через
  `allEvents()`, то есть `App.state.app.events.map(...)`. Карточка без своего
  события в Клиндарии не видна ни в одном модуле и не удаляется ниоткуда.
  Единственное место, где виден ВЕСЬ справочник, — выпадающий список поля
  «Собрание» у участника в Конгрессах (`directoryCongregations()` подмешивает
  `CWDirectory.all()`), но это только просмотр, и редактор списка Конгрессов
  показывает меньше строк, чем подсказок в поле. Расхождение задумано, но
  выглядит как ошибка.
  *Откуда берутся невидимые карточки:* штатное удаление в Клиндарии чистит и
  справочник (`detach()`), сирот при обычной работе не копится. Но раздел
  `communities` в резервной копии помечен `partial` и при восстановлении
  СЛИВАЕТСЯ, а не заменяется, — старая копия вернёт прежние карточки, тогда как
  `events[]` заменится текущим состоянием целиком.
  *Идея Алекса:* переименовать модуль и завести внутри плитки. Название —
  «Документы и данные» (Алекс) или «Документы и справочники» (Claude: слово
  «данные» означает всё и потому не ограничивает рост — та же беда, что у слова
  «Архив»; модуль-«Архив» отдельной сущностью отвергнут по этой же причине,
  справочник собраний — данные ЖИВЫЕ, они питают автодополнение Конгрессов).
  *Цена переименования:* отображаемое имя живёт в одном ключе
  `module.documents.title` (и плитка хаба, и шапка модуля). Идентификатор
  `documents` и папку менять НЕЛЬЗЯ — вшиты в `cw-lang:documents`,
  `cw-doclang:documents`, реестр резервного копирования и пути service worker'а.
  Перевод пишет носитель, а до тех пор русская строка стоит на первом экране
  хаба — заметнее прочих долгов, отдавать носителю пакетом с 4 ключами
  `cp.delete_*` и 26 ключами `cong.match.*`.
  *Правило отбора плиток:* плитка появляется только у хранилища, у которого уже
  есть модуль-писатель. На старте три — `templates`, `documents`, `communities`.
  «Люди» не заводить: `people` пусто с первого дня, писателя нет, а наполнить
  его значит слить `speakerProfiles` Конгрессов с учащимися Школы — разные
  сущности, совпадают редко, склейка необратима и стоит правки писем обоих
  модулей. Отдельное решение, не побочный эффект экрана.
  *Границу 16.08 не двигать:* справочник владеет идентификацией (название,
  номер, адрес, контакт, координаты), модуль — тем, как собрание выглядит в его
  работе (цвет метки, расписание, тип визита, язык формуляра). Поэтому «перенести
  названия и события из Клиндария» распадается надвое: идентификация в плитку
  добавляется и так, а цвет с расписанием остаются в Клиндарии — иначе одну
  карточку придётся править в двух модулях, что хуже нынешнего.
  *Главный риск:* «Документы» станут ВТОРЫМ пишущим модулем. Шаг 4б это не
  воскрешает (он был про снятие копии с `events[]`, копия остаётся навсегда), но
  окно выбора области удаления (9.80.0) впервые начнёт срабатывать по-настоящему,
  и четыре ключа `cp.delete_*` станут видимыми на разрушительном действии.

- **Внести `check-directory-match.mjs` в `check-all.mjs`** — когда
  сопоставление начнёт что-то МЕНЯТЬ (второй кусок шага 6). Сейчас не внесено
  по правилу отбора самого гейта: туда попадает то, что уже ломалось повторно,
  а сопоставление только что написано и пока ничего не трогает — ошибка в нём
  стоит пустой результат функции. Как только оно начнёт связывать записи
  Конгрессов, цена ошибки станет ссылкой на чужое собрание в письмах, и
  проверка обязана попасть в гейт. Скрипт уже есть и прогоняется вручную.
- **Общий помощник для раскрывающихся меню в Конгрессах.** `js/topbar-menu.js`
  и выпадающий список конгрессов в `js/mobile.js` повторяют одну механику:
  открыть/закрыть по триггеру, закрыть по действию внутри, закрыть по клику
  мимо, закрыть по Esc. Различаются только селекторами элементов и тем, что
  меню шапки умеет ещё и стрелки клавиатуры. Третье такое меню сделает дубль
  дорогим — тогда и вынести в `js/menu.js` с параметрами
  `{trigger, panel, itemSelector}`. Сейчас не сделано осознанно: рефакторинг
  рабочего кода шапки не входил в объём задачи про мобильный вид.
- **Рендерить `register.html` Школы пионеров из `registrationSchema.js`** —
  сейчас состав вопросов описан дважды, и править нужно оба места.
- **Выборочная выгрузка записей общего хранилища в резервную копию.**
  `shared/backup.js` умеет брать отдельные store общей базы, но не отдельные
  записи внутри store. С появлением архива документов (13.08.2026) это значит,
  что копия Клиндария везёт и снимки Конгрессов. Данные при восстановлении не
  теряются — секция `partial` сливается, — но файл копии растёт лишним. Решение:
  необязательное поле-фильтр в реестре (`sharedStores: { db: [{ store:
  'documents', where: { module: 'circuit-planner' } }] }`). Делать не раньше,
  чем архив начнут вести два модуля, и обязательно с проверкой в
  `check-backup.mjs`.
- **Единый слой печати.** У Конгрессов, Школы и Назначений своя реализация
  печати и своё именование файлов; общий помощник убрал бы дублирование.
- **Наращивать `check-all.mjs` по следам повторяющихся багов.** Каждый класс
  ошибок, который уже рецидивировал, дешевле поймать скриптом, чем глазами.
  Кандидаты по убыванию доказанности:
  дрейф `@media print` (старый селектор топбара после переименования класса —
  били все четыре модуля); коллизии i18n-ключей (последний выигрывает молча,
  нужна AST-проверка дублей); вымывание файлов после drag&drop-загрузки на
  GitHub (наличие `shared/version.js`, всех `*/index.html`, всех `sw.js`);
  рассинхрон whitelist `_sanitizeForFont` и cmap субсета шрифта;
  подписи PDF, вылезающие за правое поле на длинных языках.
- **Единый вход хаба вместо PIN-защиты Клиндария** — или наоборот, PIN на
  уровне хаба для всех модулей. Решение не принято.

---

## Будущее

- **Фотореалистичная иконка Liquid Glass.** Текущие иконки — SVG-рендер; SVG не
  умеет `backdrop-blur` и преломление, для настоящего фотореализма нужен
  внешний AI-генератор растровых изображений.
- **Синхронизация между устройствами.** Сейчас всё хранится локально; общий
  слой данных — предпосылка, но не решение.
