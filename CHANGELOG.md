# Changelog

Здесь — последние 10 записей. Всё старше вынесено в `docs/changelog/`
(30.08.2026, фаза 2 аудита контекста): файл читался целиком и стоил
~76 000 токенов. Записи не изменены, только разделены по дате.

Правило: держать в этом файле не больше 10 записей, старые переносить
в архив текущего месяца.

## 2026-09-22 — Журнал J4a: посещения и экран посещения

Посещение — строка `journalEntries` с `type:'visit'` (пятого хранилища нет,
схема не менялась), доступ только через новый `CWJournal.visits`. Родитель —
собрание, группа или предгруппа; `type/nodeId/circuitId` неизменяемы; даты
`dateFrom/dateTo` в формате `YYYY-MM-DD`, окончание не раньше начала,
пересечение посещений одного узла допускается. Статус: `open ↔ completed`,
любой → `archived` с `archivedAt` (возврат к прежнему статусу) — только
через `complete/reopen/archive/unarchive`, не через `update()`. Удаление
отказывает, пока у посещения есть записи (`fields.visitId`, контракт для
J4b) или связи — безопасная альтернатива архив.
Граница держится в самом слое данных: общий фасад `CWJournal.entries`
посещения не мутирует — `add({type:'visit'})`, `update()` и `remove()`
посещения отклоняются с `journal-visit-use-facade` (без молчаливого
перенаправления), так что обойти инварианты через официальный фасад нельзя.
Чтение через `entries.*` не ограничено.

Маршрут: `#districts/<c>/congregation/<n>/visits` (вкладка «Посещения») и
`…/visit/<v>` (экран посещения). Разбор вынесен в `journal/js/route.js` —
чистая функция под тестом; роутер один. Неизвестное или чужое посещение и
неполный хвост маршрута ведут к вкладке «Посещения» собрания, битые id
района/собрания — к списку районов.

Вкладка «Посещения» собрания стала рабочей: реальный список (новые сверху),
пустое состояние, FAB «Визит» с диалогом дат. Экран посещения по эталонам
`04-visit-m/-d.jpg`: крошки район → собрание → сезон, заголовок «Визит —
весна 2028» и диапазон дат (производные от `dateFrom`, не хранятся),
контекст в подстроке топбара, действия в меню «Ещё» (даты, завершить/
возобновить, архив, удалить). Намеренно без фиктивных данных: чип Клиндария
не показывается (связь — J9), вместо него — статус посещения; баннер
переносов скрыт (J5); редактор показан каркасом с неактивной панелью и
нейтральным пустым состоянием (J4b); строка связи с проектом отсутствует (J7).

Попутно: FAB модуля не прятался атрибутом `hidden` (общее
`.md-fab { display: inline-flex }` перебивало `[hidden]`) и ссылки крошек
красились системным синим — обе правки только в `journal/css/styles.css`.

Новый гейт `scripts/check-journal-visits.mjs`: создание, даты, правка и
неизменяемые поля, порядок по узлу, жизненный цикл, безопасное удаление,
разбор маршрута, граница UI ↔ данные (нет прямого `CWDB` в `app.js`),
закрытый обход инвариантов через `CWJournal.entries`.

**Версии:** Журнал `0.4.0 → 0.5.0`; хаб `0.41.42 → 0.41.43`; патч-бамп
кэш-фёрст оболочек из-за правки `shared/version.js`: Конгрессы
`4.46.64 → 4.46.65`, Школа `1.14.40 → 1.14.41`, Назначения `5.5.79 → 5.5.80`,
Документы `1.10.30 → 1.10.31`. Клиндарий не поднимался.

Следующая фаза (J4b — редактор записей посещения) **не начата**.

## 2026-09-21 — Журнал J3b: экран собрания + связь с CWDirectory

Экран собрания стал реальным: маршрут `#districts/<circuitId>/congregation/<nodeId>`,
карточка идентичности с четырьмя состояниями — LINKED (собрание связано с
общим справочником CWDirectory), UNLINKED (локальная запись Журнала без
связи), BROKEN (сохранённый `communityId` не разрешился в справочнике —
ссылка не стирается молча, доступно пересвязывание) и UNAVAILABLE
(справочник не прочитан — связь не трогается и не считается разорванной;
готовность берётся из `CWDirectory.ready`). Дочерние группы/предгруппы того
же собрания переиспользуют дерево J3a без второй реализации CRUD.

Переименование узла идёт через один диспетчер для всех входов (строка
района, топбар собрания, топбар района): связанное собрание показывается
под именем из CWDirectory, поэтому «Переименовать» открывает правку общей
идентичности; несвязанное/разорванное/недоступное собрание, район, группа
и предгруппа переименовываются локально (`label` узла).

Граница зафиксирована архитектурно (`shared/directory.js`, решение
16.08.2026) и здесь только соблюдена: CWDirectory владеет идентичностью
(название/номер/адрес/контакт), Журнал — расписанием встреч в собственном
поле `fields.schedule` узла. Правка связанной идентичности вызывает
`CWDirectory.upsert()`, правка расписания — только `CWJournal.nodes.update()`;
ни одна из операций не задевает границу другой (проверено тестом).

Добавлен `CWDirectory.create()` (`shared/directory.js`) — минимальное
расширение для записей без заранее известного id (в отличие от Клиндария,
который зеркалит собственный `event.id`). Генерации id внутри не появилось:
обёртка вызывает существующий `CWDB.communities.add()` без id, который сам
пользуется `uid('com')` из `shared/db.js`, и просто забирает готовый id для
кэша/индекса/маячка `cw-directory-rev` — тем же путём, что и `upsert()`.

Связывание с существующей записью требует явного подтверждения человеком
(клик по конкретному кандидату) — автоматического применения слабых/
неоднозначных совпадений нет. Создание новой записи и последующее
связывание рассматриваются как одна операция с точки зрения пользователя:
если `CWDirectory.create()` успел записать карточку, а сохранение
`communityId` на узле Журнала не удалось, узел `detach()`-ается из
созданной записи, чтобы не остался осиротевший справочный элемент, который
не знает ни один модуль.

**Версии:** хаб `0.41.41 → 0.41.42`, Журнал `0.3.0 → 0.4.0`. Патч-бамп
Конгрессов, Школы пионеров, Назначений и Документов — правка `shared/directory.js`
(общий слой); Клиндарий не бампится (stale-while-revalidate). `shared/directory.js`
и новый тест — `journal/AGENTS.md` границу не меняли.

`node scripts/check-journal-directory.mjs` (новый): связывание/разрыв/
создание и компенсация create→link, граница идентичность↔расписание,
источники связи (attach/detach, relink, общая запись у двух узлов),
контактные поля, `CWDirectory.ready` как источник готовности и единый
диспетчер переименования для строки района и топбара собрания — 0 FAIL.
`node scripts/check-versions.mjs`, `check-journal-db.mjs`: 0 FAIL.
`node scripts/check-all.mjs`: см. отчёт по фазе (доклад в чате).

## 2026-09-21 — Журнал J3a: реальное дерево района

Статическая фикстура «Район» (J1) заменена настоящим деревом на
`CWJournal`/`journalNodes`: районы → собрания → группы/предгруппы. Экран
Обзора остаётся фикстурой — вне рамок этой фазы.

Реализовано: список районов с пустым состоянием (без автосоздания примера);
создание района/собрания/группы/предгруппы через общий диалог
(`.md-dialog`, тот же паттерн, что в «Документах»); переименование,
архив/восстановление, удаление через строчное меню (`.md-menu`, тот же
примитив, что в Конгрессах/Документах); ручной порядок соседей (▲/▼,
обмен `sort`). Иерархия (`kind` ↔ `parentId`) проверяется в
`CWJournal.nodes.add/update`, а не только в интерфейсе: невалидную
комбинацию (группа под районом, собрание под собранием и т.п.) нельзя
завести в обход экрана. Удаление отказывает, если у узла есть дети,
собственные записи или связи — каскадного удаления в этой фазе нет.
`parentId` корня — строка `'root'` (`CWJournal.ROOT_PARENT`), не `null`:
null/undefined не являются валидным ключом IndexedDB (тот же класс
инварианта, что `carryKey` в J2).

Попутно найден и исправлен реальный баг J1: `.j-tag--link` ссылался на
необъявленный нигде токен `--cw-teal` (внутри неразрешимый `var()` делает
всё объявление `color` недействительным) — `journal` просто отсутствовал в
списке страниц `scripts/check-css-vars.mjs`, поэтому проверка ни разу не
видела CSS модуля. Заменено на уже объявленную для обеих тем пару
`--status-success`/`-bg`; `journal` добавлен в проверку.

Новые проверки в `scripts/check-journal-db.mjs`: создание района/собрания/
группы/предгруппы с наследованием `circuitId`, отказ пяти видов невалидной
иерархии (включая проверку в `update`), переименование сохраняет
`id`/`createdAt`, архив/восстановление, детерминированный порядок (`sort`,
затем `label`, затем `id`), безопасное удаление (дети/записи/связи).

**Версии:** Журнал `0.2.0 → 0.3.0`; хаб `0.41.40 → 0.41.41`; патч-бамп
кэш-фёрст оболочек из-за правки `shared/version.js`: Конгрессы
`4.46.62 → 4.46.63`, Школа `1.14.38 → 1.14.39`, Назначения
`5.5.77 → 5.5.78`, Документы `1.10.28 → 1.10.29`. Клиндарий не поднимался.

Следующая фаза (J3b — интеграция с `CWDirectory`) **не начата**.

## 2026-09-21 — Журнал J2: слой хранения в общей базе

Схема `circuit-workspace-db` поднята **5 → 6**, аддитивно: общий обработчик
апгрейда создаёт только недостающие хранилища, существующие и их индексы не
трогаются, строки не переносятся. Новые хранилища Журнала (все `keyPath: id`):
`journalNodes` (индексы `parentId, circuitId, kind, status, updatedAt`),
`journalEntries` (`nodeId, circuitId, type, status, updatedAt, dueDate,
carryKey`), `journalLinks` (`from, to, rel`), `journalMeta` (без индексов).
Вторая физическая база не заводилась; `CWState`/localStorage Журналом не
используются.

`journal/js/data.js` — фасад `CWJournal` поверх CRUD-наборов `CWDB`
(узлы/записи/связи/meta, индексные выборки, `openCarry(nodeId)` по
`carryKey = '<nodeId>:open'`, URN-помощники). Интерфейс Журнала не изменился:
экраны по-прежнему статическая фикстура, в базу ничего не пишется.

`shared/backup.js`: копия Журнала — четыре хранилища целиком + `communities`;
восстановление копии модуля сливает по ключу и соседей не трогает. Известная
асимметрия: полная копия хаба, снятая до v6, хранилищ Журнала не содержит и
при восстановлении их не очищает.

Новый гейт `scripts/check-journal-db.mjs`: апгрейд v5 → v6 на базе со
строками-стражами во всех прежних хранилищах, индексы, CRUD, `carryKey`,
граница хранения, реестр копий, слияние при восстановлении, отказ копии со
схемой новее потолка. Правила модуля — `journal/AGENTS.md`.

**Версии:** Журнал `0.1.0 → 0.2.0`; хаб `0.41.39 → 0.41.40`; патч-бамп
кэш-фёрст оболочек из-за правки `shared/*`: Конгрессы `4.46.61 → 4.46.62`,
Школа `1.14.37 → 1.14.38`, Назначения `5.5.76 → 5.5.77`, Документы
`1.10.27 → 1.10.28`. Клиндарий не поднимался (stale-while-revalidate).

Следующая фаза (J3 — дерево узлов на живых данных) **не начата**.

## 2026-09-20 — Журнал: новый модуль, фаза J1 (оболочка и визуальная основа)

Шестой модуль хаба — «Журнал», локальные рабочие записи района (собрания,
визиты, проекты, задачи). Эта фаза — **только оболочка**: ни IndexedDB, ни
CRUD, ни реальных данных нет. `shared/db.js` не тронут, `DB_VERSION` не
поднят; `shared/state.js`/`shared/db.js` в модуль не подключены — у него
пока нет ни одного домена данных, который стоило бы туда класть.

Реализовано: общая оболочка (`.md-shell`/`.md-sidenav`/`.md-bottomnav`,
топбар, акцент амбер→бирюза), пять назначений навигации (Обзор / Районы /
Задачи / Поиск / Архив), два экрана на детерминированных статических
фикстурах (Обзор и Район EU-K-03) — визуально по эталонным макетам, три
остальных назначения — заглушка `.md-emptystate` до последующих фаз.
Модульный словарь на пять языков (только оболочка интерфейса; фикстурные
данные, как и текст письма в других модулях, не переводятся). Собственный
`journal-icon-source.svg` (раскрытая книга + закладка) по той же графической
грамматике, что и у остальных четырёх модулей с акцентом.

`shared/backup.js`: заведена пустая запись реестра `journal` — по тому же
правилу, что требует `check-backup.mjs` («модуль обязан явно объявить
зависимость от общего слоя, пусть и «ничего»»); наполнится в фазе J2 вместе
с подъёмом схемы.

**Версии:** хаб `0.41.38 → 0.41.39` (правка `shared/version.js`,
`shared/style.css`, `shared/backup.js`, `shared/i18n/common.js`,
`index.html`). Патч-бамп кэш-фёрст оболочек, кроме Клиндария
(stale-while-revalidate, не требуется): Конгрессы `4.46.60 → 4.46.61`,
Школа `1.14.36 → 1.14.37`, Назначения `5.5.75 → 5.5.76`, Документы
`1.10.26 → 1.10.27`. Журнал — новый модуль, `0.1.0`.

`node scripts/check-all.mjs`: 0 FAIL, 0 SKIP среди проверок, которых
касается эта фаза (`check-shared-bump`/`check-shell-bump` выполнены реально,
не пропущены — клон с полной историей). Один сбой в `check-doclang.mjs`
(Школа пионеров, jsdom/Node) воспроизводится и на чистом `main` без единой
правки Журнала — окружение, не эта фаза; см. `docs/journal/gate.md`.

Следующая фаза (J2 — схема: `DB_VERSION` 5→6, `journalNodes/Entries/
Links/Meta`) **не начата**.

## 2026-09-20 — Hotfix: невалидный легаси Клиндария больше не становится App.state.app

Живой прогон в реальном Chrome нашёл дыру, которую предыдущий гейт
пропустил: структурно чужой легаси (`{"foo":"bar"}`) корректно НЕ мигрирует
на первом запуске — но `load()` уже к тому моменту скармливал ЛЮБОЙ
распарсенный легаси в `migrate()`/`normalizeApp()`, ДО того, как решение
«мигрировать или нет» вообще проверяло его форму. `normalizeApp()` всеяден
и превращал `{foo:'bar'}` в «валидные данные приложения» вместе с `foo` —
обычный runtime, сохраняя это состояние (в реальном Chrome — где именно,
не важно: любой из полусотни вызовов `save()`), заносил `foo` в канон.
На следующем запуске уже исправленная в прошлом hotfix'е ветка «канон уже
существовал» видела валидный канон и удаляла легаси — единственную
сохранившуюся копию.

**Root cause, `circuit-planner/app.js`, `load()`:** валидность легаси
(`isValidPersistedState()`) теперь проверяется ОДИН РАЗ, до присвоения
`App.state.app`, и используется дважды — и для решения «доверять ли
`migrate()`», и для решения «переносить ли в канон» ниже. Невалидный легаси
теперь никогда не становится состоянием приложения: вместо него —
`createDefaultData()`, чистый дефолт без единого поля из легаси.

Отдельно (второй root cause из прошлого прогона аудита, который на самом
деле НЕ доехал до предыдущей поставки: origin/main нёс только
однобокую проверку) — ветка «канон уже существовал» тоже переписана: легаси
удаляется, только если ОБЕ стороны — и канон, и заново прочитанный
легаси-ключ — по отдельности проходят `isValidPersistedState()`.

Регресс расширен полным сценарием в два запуска (свежий контекст, не
повторный вызов `load()` в той же вкладке): проверяется явно, что `foo`
отсутствует и в `App.state.app`, и в каноне — и после первого запуска, и
после второго; тест воспроизводит реальный баг на неисправленном коде
(проверено намеренным откатом файла к `origin/main` перед фиксом — 7
ассертов падали именно так, как в живом Chrome).

**Версии:** Клиндарий `9.94.15 → 9.94.16`. Конгрессы не тронуты — `4.46.60`.
`shared/state.js` не тронут.

`node scripts/check-all.mjs`: 0 FAIL, 0 SKIP (3 прогона подряд).


## 2026-09-20 — Hotfix: канон-канона недостаточно для удаления легаси (Конгрессы)

Живой прогон нашёл реальную дыру в фазе F: структурно чужой легаси
(`{"foo":"bar"}`) корректно НЕ мигрирует на первом запуске — но обычный
runtime (у Конгрессов — `newC()`, создающий демо-конгресс на пустом
`congresses[]` и сам же его сохраняющий) заводит валидный канон уже В ТОМ
ЖЕ запуске, безо всякой миграции. На следующем запуске прежняя ветка
«уже мигрировавшая установка» проверяла ТОЛЬКО валидность канона и удаляла
легаси — стирая единственную сохранившуюся копию чужого содержимого только
потому, что канон (демо-конгресс) сам по себе оказался валиден.

**Правило:** валидность канона в одиночку НИКОГДА не даёт права удалить
легаси. Для уже мигрировавших установок легаси убирается только когда ОБЕ
стороны валидны — и канон, и само содержимое легаси-ключа, отдельно.

`congress-project/js/state.js`: исправлена ветка «канон уже существовал»
— добавлена повторная проверка легаси через `isValidState()` перед
удалением, тем же путём, что уже был у Клиндария (при разборе
обнаружилось, что `circuit-planner/app.js` уже нёс эту защиту — фактической
правки кода Клиндарию не потребовалось, только тестового покрытия).

Расширен `scripts/check-f-legacy-cleanup.mjs`: полное покрытие в два
запуска (свежий контекст на диске, не повторный вызов в той же вкладке) —
структурно чужой легаси переживает и первый, и второй запуск; отдельно —
валидный канон + невалидный/испорченный легаси (легаси цел) и валидный
канон + валидный легаси (легаси удалён), для обоих модулей.

**Версии:** Конгрессы `4.46.59 → 4.46.60`. Клиндарий не тронут — исходный
код уже был корректен, версия остаётся `9.94.15`. `shared/state.js` не
тронут.

`node scripts/check-all.mjs`: 0 FAIL, 0 SKIP.


## 2026-09-20 — Phase F: безопасное удаление легаси-ключей Клиндария и Конгрессов

Главные легаси-ключи Клиндария (`service-year-planner-v9-4-2`) и Конгрессов
(`congress-pwa-v34-speakers`) больше не остаются постоянным входом миграции
навечно — теперь они удаляются, но ТОЛЬКО когда безопасность подтверждена
дважды: подтверждённой записью канона И проверкой его формы. Существование
канонической строки (`remote.migrated()`) само по себе НЕ является условием
удаления — оно доказывает лишь, что строка есть, а не что она пригодна:
`normalizeApp()` Клиндария намеренно всеяден и превращает любой JSON-мусор
в валидное на вид пустое состояние без единой ошибки, поэтому подтверждённая
запись (`writeOutcome()==='written'`) тоже сама по себе недостаточна.

**Конгрессы** (`congress-project/js/state.js`, `load()`): легаси-строка
парсится и проверяется существующей `isValidState()` ДО попытки записи в
канон — непригодная форма не пишется и не удаляется вовсе. После записи
легаси-ключ удаляется только при `writeOutcome()==='written'`. Для уже
мигрировавших установок (канон существовал до этой сессии) легаси убирается
отдельным путём — только когда САМ канонический payload заново проходит
`isValidState()`.

**Клиндарий** (`circuit-planner/app.js`): добавлен новый минимальный
предикат `App.store.isValidPersistedState()` — три структурных инварианта,
которые `normalizeApp()` иначе тихо подставил бы дефолтом
(`Array.isArray(events)`, `Array.isArray(entries)`, `serviceYears` — объект).
`{}` и `{"foo":"bar"}` не проходят ни одной проверки. Перенос переведён с
безусловного `remote.write()` на подтверждённый `remote.writeOutcome()`;
удаление ключа — тот же двупутный алгоритм, что у Конгрессов.

В обоих случаях: `REFUSED`/`FAILED`/недоступная или не успевшая открыться за
таймаут база — легаси не удаляется ни при каких условиях, никакого отката к
записи в прежний ключ как в запасной путь не появилось. `shared/state.js` не
тронут. `-history`/`-backups`/`-accent` и Phase E backup/restore — вне
рамок, не затронуты.

Новый гейт: `scripts/check-f-legacy-cleanup.mjs` — реальные
`circuit-planner/app.js` (jsdom) и `congress-project/js/state.js` (реальный
ES-модуль через dynamic import, тот же код, что грузит браузер) поверх
fake-indexeddb: чистый старт, валидный легаси → миграция → удаление,
валидный канон + легаси → удаление без потери содержимого, испорченный
JSON, структурно чужой легаси, недоступная база, невалидный канон — легаси
сохраняется во всех этих случаях.

**Версии:** Клиндарий `9.94.14 → 9.94.15`, Конгрессы `4.46.58 → 4.46.59` —
собственные файлы модулей, `shared/*` не тронут, поэтому cascade на
остальные четыре оболочки не требуется (правило патч-бампа общего слоя
здесь неприменимо).

`node scripts/check-all.mjs`: 0 FAIL, 0 SKIP.
Живой Chromium-прогон не выполнялся (см. LIVE limitation при выпуске).


## 2026-09-20 — Phase E: легаси-мосты главных блоков Клиндария и Конгрессов

Restore-мост (`extractLegacyStateRow`/`writeExtraStateRow`, введён фазой C2
для отправителя и обобщён фазой D для Назначений) расширен ещё двумя
записями `LEGACY_STATE_BRIDGES`, без единой строки новой логики: старый
главный ключ **Клиндария** (`service-year-planner-v9-4-2`) и **Конгрессов**
(`congress-pwa-v34-speakers`) при восстановлении старой копии теперь
переносятся прямо в канонические строки `state/circuit-planner` и
`state/congress-project`, минуя повторный вход через localStorage.

Оба ключа уже были read-only в живом приложении (сама программа не пишет
в них ни разу — подтверждено чтением `circuit-planner/app.js`/
`congress-project/js/state.js`, runtime-хранение не менялось), поэтому
перенос — вопрос консистентности и на один перезапуск меньше, а не
исправление бага: без моста старая копия и раньше корректно доезжала до
канона, только за два шага (restore пишет легаси-ключ обратно →
следующая обычная загрузка сама мигрирует его) — теперь один.

**Вне рамок фазы, сознательно** — `service-year-planner-v9-4-2-history` и
`congress-pwa-v34-speakers-backups` НЕ тронуты: их читает и удаляет
`shared/snapshots.js` собственным `migrateLegacy()` (массив снимков →
хранилище `snapshots`, другая форма данных, другое назначение); блокировка
в `EXCLUDE` без отдельного array-моста сломала бы уже рабочий перенос
истории. `service-year-planner-accent` тоже не тронут: ни одного
чтения/записи этого ключа в приложении нет вовсе.

`EXCLUDE` пополнен обоими главными ключами — восстановление, старое или
новое, никогда не пишет их обратно в localStorage. Из `MODULES.*.local`
убраны только главные ключи; `-history`/`-backups`/`-accent` остались как
были.

Регресс-тесты (`check-backup.mjs`, +20 сценариев на оба модуля): чистое
устройство, канон-в-файле, канон-на-диске при частичном восстановлении,
испорченный легаси (не-JSON и массив вместо объекта), сохранность
вложенной структуры (массивы вложенных объектов, не просто строка).

Отдельно доставлен `scripts/check-update-orchestration.mjs` (написан в ходе
разбора live-блокера School после Phase D — код признан корректным, разбор
не потребовал правок; тест закрывает реальный пробел: у оркестрации
`checkAll()`/`applyAll()` не было ни одной автоматической проверки) —
зарегистрирован в `check-all.mjs`.

**Версии:** хаб `0.41.37 → 0.41.38`. Патч-бамп общего слоя
(`shared/backup.js`) для cache-first оболочек: Конгрессы `4.46.57 → 4.46.58`,
Школа `1.14.35 → 1.14.36`, Назначения `5.5.74 → 5.5.75`, Документы
`1.10.25 → 1.10.26`. Клиндарий не тронут (stale-while-revalidate,
исключён из cascade rule) — `9.94.14`.

`node scripts/check-all.mjs`: 0 FAIL, 0 SKIP (полная история git).
Живой Chromium-прогон не выполнялся (см. LIVE limitation при выпуске).


## 2026-09-20 — Phase D: канон Назначений переехал в общую базу

Канон Назначений (`appointments/js/app.js`) переехал из
`localStorage['cw-appointments-v1']` в запись `appointments` хранилища
`state` общей базы — тот же механизм, что уже несёт отправителя (C2),
Клиндарий и Конгрессы. Внутренняя модель данных не менялась ни на бит,
включая PNG-подпись внутри `signature.image`: она переносится байт в байт,
backup.js её не разбирает и не нормализует. Прежний ключ остаётся на
устройстве дореформенным снимком и с этой фазы только читается — как вход
одноразовой миграции при пустом каноне; ни одной новой записи в него нет.

`load()`/`save()` перестроены по образцу `congress-project/js/state.js`
(первый модуль, прошедший этот путь для собственных доменных данных, не для
отправителя): миграция A–D, подтверждённый рубеж (`domainBaselinePayload`)
отдельно от `remote.get()` (тот пишется оптимистично, до подтверждения),
`WRITTEN`/`REFUSED`/`FAILED`, липкая деградация с блокировкой всего
редактируемого борта (`<aside class="editor">`), конфликт соседней вкладки.
Добавлен синхронный флеш на `pagehide`/`beforeunload`/`visibilitychange`
(закрывает пробел, существовавший ещё в C1: 400-мс окно дебаунса могло
потерять последнюю правку при закрытии вкладки бесшумно).

**Найдены и исправлены два дефекта в ходе разработки** (regression-тесты
на оба — `check-d-appointments.mjs`):
- cross-tab приём чужого состояния обновлял списки/подпись/предпросмотр
  письма, но не переставлял значения в четырёх обычных полях (дата,
  собрание, координатор, адрес) — они простаивали на устаревшем значении до
  следующей перезагрузки. Добавлен `syncBasicFields()`.
- на пустом старте (ни канона, ни легаси) рубеж не заводился вовсе, и
  `domainDirty()` трактовал `null`-рубеж как «правки нет» безусловно —
  первая же чужая запись в это окно молча стирала ещё не подтверждённый
  ввод пользователя. Рубеж на пустом старте теперь ставится на саму пустую
  state, как и для случая с уже существующим каноном.

**Backup**: `cw-appointments-v1` убран из `local` модуля и добавлен в
`EXCLUDE` — новые копии несут только `state/appointments`
(+ `shared:sender`), восстановление ни разу не пишет легаси обратно.
Restore-мост (`extractLegacyStateRow`/`writeExtraStateRow`) обобщён из
sender-специфичного варианта фазы C2 в таблицу мостов
(`LEGACY_STATE_BRIDGES`), покрывающую и отправителя, и Назначения одним
и тем же кодом; канон (в файле или уже на диске) побеждает легаси в обоих
случаях.

**Версии:** хаб `0.41.36 → 0.41.37`. Назначения `5.5.73 → 5.5.74`
(собственная миграция). Патч-бамп общего слоя (`shared/backup.js`) для
остальных cache-first оболочек: Конгрессы `4.46.56 → 4.46.57`, Школа
`1.14.34 → 1.14.35`, Документы `1.10.24 → 1.10.25`. Клиндарий не тронут —
`9.94.14`.

`node scripts/check-all.mjs`: 0 FAIL, 0 SKIP (полная история git).
Живой Chromium-прогон не выполнялся (см. LIVE limitation при выпуске).
