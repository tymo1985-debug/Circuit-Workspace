# Changelog

## 0.41.68 — 25.09.2026

- Update/PWA: устранена причина противоречивого «не удалось обновить» сразу
  за «обновление установлено». Это была не гонка внутри самой post-update
  верификации (0.41.66 её уже сериализовал и подтвердил), а гонка МЕЖДУ ней и
  независимой фоновой автопроверкой хаба (`cw-update-available` от
  собственного `watch()`-наблюдения за SW): автопроверка могла запустить
  параллельный `runCheck()` и показать свой баннер поверх/раньше настоящего
  результата apply — как сразу после reload, так и во время самого apply.
- `shared/update.js`: `CWUpdate.isBusy()` — публичный признак «идёт финальная
  post-update верификация»; `showBar()` больше не рисует ничего после
  запланированного собственного reload (`location.reload()` не обрывает
  текущий тик синхронно).
- `index.html`: автотриггер `cw-update-available` пропускается, пока идёт
  ручной check/apply (`checking`/новый `applying`) или post-update
  верификация (`CWUpdate.isBusy()`) — не откладывается в очередь, следующий
  реальный повод придёт сам.
- Версии cache-first оболочек подняты для доставки правки общего слоя;
  Клиндарий остаётся `9.95.1`.

## 0.41.67 — 25.09.2026

- Хаб: режим «Настроить порядок» — плитки переставляются перетаскиванием за ручку или стрелками, отдельно в рабочем и в системном разделе; перенос между разделами технически невозможен.
- Порядок хранится как последовательность id по каждому разделу (`localStorage['cw-hub-order']`, в резервную копию не входит); новые модули дописываются в конец своего раздела, чужие/неизвестные/повторные id отбрасываются. Кнопка «Восстановить стандартный порядок».
- Новая проверка `scripts/check-hub-order.mjs`. Версии cache-first оболочек подняты для доставки правки общего слоя; Клиндарий остаётся `9.95.1`.

## 0.41.66 — 25.09.2026

- Update/PWA final status теперь подтверждается по фактически активным service workers всех scope, а не по timeout старой страницы.
- `applyAll()` сравнивает активированный worker с версией waiting-worker, который реально применялся; задержанный `active` swap больше не даёт ложный `versionMismatch`.
- Для модулей дополнительно проверяется Hub-generation, поэтому неизменная module-version (например, Клиндарий) не маскирует старый общий слой.
- Post-update баннер заново проверяет active workers перед «Обновление установлено»; временный check/apply failure больше не превращается в противоречивый success после reload.
- Версии cache-first оболочек подняты для доставки правки общего update-слоя; Клиндарий остаётся `9.95.1`.

## 0.41.65 — 25.09.2026

- Post-update баннер теперь скрывает записи release metadata с `technical: true`; после установки пользователь видит только реальные изменения.
- Добавлена gate-регрессия именно для post-update ветки, чтобы `Version-only` cascade не вернулся после reload.
- Версии cache-first оболочек подняты для доставки release metadata; Клиндарий остаётся `9.95.1`.

## 0.41.64 — 25.09.2026

- Проверка обновлений продолжает остальные Circuit Workspace scope, если регистрация одного модуля временно отклонена.
- Ошибки регистрации и обновления сохраняют модуль, scope, фазу, имя и сообщение ошибки; Hub показывает название проблемного модуля без raw URL.
- Версии cache-first оболочек подняты для доставки правки общего слоя; Клиндарий остаётся `9.95.1`.

## 0.41.63 — 25.09.2026

- Update/PWA Audit V2: хаб проверяет и применяет обновления только для точных семи scope Circuit Workspace; service workers других проектов на том же origin полностью игнорируются.
- Баннер обновления показывает только реальные пользовательские изменения; служебный version/cache cascade сохраняется в release metadata с `technical: true`.
- Partial failure учитывает только Circuit Workspace и больше не выводит raw URL чужих приложений.

Здесь — последние 10 записей. Всё старше вынесено в `docs/changelog/`
(30.08.2026, фаза 2 аудита контекста): файл читался целиком и стоил
~76 000 токенов. Записи не изменены, только разделены по дате.

Правило: держать в этом файле не больше 10 записей, старые переносить
в архив текущего месяца.

## 2026-09-25 — Журнал 0.15.2: J-Final remediation

**Версии:** Журнал `0.15.1 → 0.15.2`. Правка `shared/backup.js` и реестра
версий — патч-бамп cache-first оболочек: хаб `0.41.61 → 0.41.62`,
Конгрессы `4.46.83 → 4.46.84`, Школа `1.14.59 → 1.14.60`, Назначения
`5.5.98 → 5.5.99`, Документы `1.11.8 → 1.11.9`. Клиндарий не тронут
(stale-while-revalidate).

- Lock очищает расшифрованный текст во всех динамических контейнерах и
  значениях форм, включая скрытые ранее посещённые маршруты; текстовые поля
  задач и проектов запрещают восстановление истории браузером.
- Замена сейфа восстановлением копии в другой вкладке немедленно сбрасывает
  старый ключ сессии и запускает ту же очистку plaintext.
- События `CWDirectory.init/onChange` обновляют текущий маршрут целиком, не
  подменяя меню посещения действиями собрания.
- Живой тест Клиндария использует настоящую перезагрузку; update-pipeline
  больше не зависит от пути конкретной машины и устаревшего baseline.

## 2026-09-25 — Журнал 0.15.1: приёмка живого Обзора на телефонах (O3)

**Версии:** Журнал `0.15.0 → 0.15.1`. Правка `shared/style.css` — патч-бамп
cache-first оболочек: хаб `0.41.60 → 0.41.61`, Конгрессы `4.46.82 → 4.46.83`,
Школа `1.14.58 → 1.14.59`, Назначения `5.5.97 → 5.5.98`, Документы
`1.11.7 → 1.11.8`. Клиндарий не тронут (stale-while-revalidate).

Расширенная приёмка Обзора (320/360/430, пять языков, пусто/наполнено,
восстановление Клиндария и Журнала, J8 во всех секциях, гонки, офлайн, темы)
нашла три дефекта:

- Нижняя навигация (`.md-bottomnav`) не влезала в 320/360: пять пунктов
  теперь делят ширину поровну, индикатор — не шире пункта.
- Общая шапка `.md-topbar-v2` на узком телефоне наезжала сама на себя
  (кнопки хаба уходили под выбор языка, последняя кнопка обрезалась). В
  блоке `max-width: 600px`: маркер скрыт, ведущая часть не сжимается уже
  своих кнопок и занимает место распорки, заголовок — остаток (уже 64px не
  показывается), сжимается только выбор языка (не шире 7.5rem), зазоры
  меньше. Проверено во всех шести модулях на 320/360/430.
- Проекты на Обзоре показывались все: теперь до трёх строк канонического
  порядка, счётчик полный, «Все» нет — остальные в своих районах.

Проверки: `scripts/live-journal-overview-hardening.mjs`,
`scripts/live-shell-topbar.mjs`, дополнение `check-journal-overview.mjs`.

## 2026-09-24 — Журнал 0.15.0: живой Обзор (O2)

**Версии:** Журнал `0.14.0 → 0.15.0`. Правка `shared/version.js` — патч-бамп
cache-first оболочек: хаб `0.41.59 → 0.41.60`, Конгрессы `4.46.81 → 4.46.82`,
Школа `1.14.57 → 1.14.58`, Назначения `5.5.96 → 5.5.97`, Документы
`1.11.6 → 1.11.7`. Клиндарий не тронут (stale-while-revalidate).

Обзор остаётся стартовым экраном; статическая фикстура J1 удалена целиком
(вымышленный район, счётчики, вопросы, задачи, посещения, «Недавно
изменённые», стрелки и кнопки без действия).

- Экран `journal/js/app/overview.js` (`renderOverview`) строит всё из
  `CWJournal.overview.read()` и `CWPlanner.listCommunities()`; прямого чтения
  базы и блоба Клиндария нет.
- Сводка: районы Журнала, собрания/группы/предгруппы Клиндария. Клиндарий
  не прочитан — «…», недоступен/испорчен — «—», не 0; сбой чтения Журнала —
  «—» и подсказка.
- Секции «К следующим посещениям», «Открытые задачи», «Проекты района»,
  «Последние посещения»: до трёх строк, полный счётчик, только существующие
  маршруты (`#tasks/<id>`, посещение, проект); «Все» — только у задач.
- FAB на Обзоре скрыт; ключи `j.fab.new_entry`, `j.link.history`,
  `j.section.recent_edits` удалены, новые — на пяти языках.
- Свежесть без опроса: Клиндарий, записи и (новое) узлы Журнала — тем же
  каналом `cw-journal` с меткой `nodes`, справочник, смена блокировки.
- Гонки: поколение отрисовки; устаревший результат не вставляется, а
  раскрытый текст не возвращается после блокировки.
- Проверки: `check-journal-overview.mjs` расширена (jsdom), живой прогон
  `scripts/live-journal-overview.mjs` (1280×900 и 430×900).

## 2026-09-24 — Журнал 0.14.0: данные живого Обзора (O1)

**Версии:** Журнал `0.13.4 → 0.14.0`. Правка `shared/planner.js` — патч-бамп
cache-first оболочек: хаб `0.41.58 → 0.41.59`, Конгрессы `4.46.80 → 4.46.81`,
Школа `1.14.56 → 1.14.57`, Назначения `5.5.95 → 5.5.96`, Документы
`1.11.5 → 1.11.6`. Клиндарий не тронут (stale-while-revalidate).

Фундамент данных для будущего живого Обзора; экран Обзора не менялся.

- `CWPlanner.listCommunities()` — полный уникальный список объектов
  Клиндария `{ communityId, name, visitType }` из `events[]` канона: только
  `congregation|group|pregroup`, объект есть и без назначенного посещения,
  несколько посещений — одна строка (id события = id справочника).
  Замороженные копии, без внутренних полей. Смена объектов уведомляет
  подписчиков тем же `subscribe()`; пустой список значим только при
  `status()` `ok`/`empty`. `listEntries/getEntry/candidates/urlForEntry` —
  без изменений (прежний публичный контракт покрыт regression-проверкой).
- `CWJournal.overview.read()` — одно чтение, пять выборок: районы вне
  архива, открытые задачи (порядок экрана «Задачи»), открытый перенос,
  active-проекты неархивных районов, посещения (новые сверху, с id
  района/собрания для существующего маршрута). Архивный контекст — то же
  вычисление, что у поиска/архива. J8: раскрываются только итоговые строки
  через `revealList`, при блокировке текста нет. Ничего не хранится,
  опроса нет, `DB_VERSION` = 6.
- Проверка `scripts/check-journal-overview.mjs`.

## 2026-09-24 — Production repair update pipeline

**Версии:** Hub `0.41.57 → 0.41.58`; Конгрессы `4.46.79 → 4.46.80`;
Клиндарий `9.95.0 → 9.95.1`; Школа `1.14.55 → 1.14.56`;
Назначения `5.5.94 → 5.5.95`; Документы `1.11.4 → 1.11.5`;
Журнал `0.13.3 → 0.13.4`.

- Hub регистрирует и проверяет все модули из `CW_MODULES`, а не только
  те, которые уже были открыты в этом browser profile.
- Каждый worker отвечает фактической active-версией; success выдаётся
  только после совпадения с реестром. Partial result называет module/scope.
- Cache namespace модуля включает и module version, и Hub version: waiting worker
  больше не перезаписывает active cache до активации.
- Changelog берётся из waiting Hub-worker. Гонка generic/detailed banner убрана;
  `CWI18n.apply()` больше не удаляет вложенный список изменений.
- Chromium live upgrade `0.41.58 → 0.41.59` прошёл с тем же profile/cache state:
  семь active-версий совпали, hard refresh не использован, возврат в Hub — HTML UI.

## 2026-09-24 — Congress: отдельные PDF писем

**Версии:** Конгрессы `4.46.78 → 4.46.79`. Version-only cache-first bump:
хаб `0.41.56 → 0.41.57`, Журнал `0.13.2 → 0.13.3`, Школа
`1.14.54 → 1.14.55`, Назначения `5.5.93 → 5.5.94`, Документы
`1.11.3 → 1.11.4`. Клиндарий не тронут (stale-while-revalidate).

- Каждое letterable-задание активного конгресса экспортируется отдельным PDF.
- Chromium/PWA один раз запрашивает папку; остальные браузеры получают один ZIP.
- Имена файлов очищаются, коллизии получают `(2)`, а CWDocs snapshot пишется
  только после успешной выдачи конкретного письма.
- Экспорт использует существующий `letterHTML`, включая актуальный шаблон,
  fallback, переменные, разметку и настройки шрифта.

## 2026-09-24 — Congress ↔ Documents: выбор, форматирование и удаление шаблонов

**Версии:** Конгрессы `4.46.77 → 4.46.78`, Документы `1.11.2 → 1.11.3`.
Version-only cache-first bump: хаб `0.41.55 → 0.41.56`, Журнал
`0.13.1 → 0.13.2`, Школа `1.14.53 → 1.14.54`, Назначения
`5.5.92 → 5.5.93`. Клиндарий не тронут (stale-while-revalidate).

- Задание Конгрессов хранит optional `letterTemplateId`; разрешение письма:
  explicit → шаблон типа → общий Congress → builtin. Удалённая ссылка не
  стирается, но безопасно использует fallback и помечается в UI.
- Выбор шаблона входит в общие поля linked task и сохраняется обычным state
  backup/restore без копирования тела шаблона.
- Pure-user шаблон в Документах можно удалить с подтверждением; системный
  builtin удалить нельзя, для builtin+override остаётся Restore original.
- Text-шаблоны получили bold/italic/underline через `**`, `*`, `__` с
  сохранением caret и вложенной разметки; HTML-шаблоны продолжают использовать
  прежний RTE.
- Добавлены regression gate и Chromium live-сценарий полного workflow.

## 2026-09-24 — Журнал 0.13.1: защита записей не затирает параллельную правку (J8-H1)

Три пути J8 пишут строку `journalEntries` целиком из копии, прочитанной до
асинхронного шифрования: защита и первая настройка (`protectOp`), снятие
защиты (`unprotect`) и правка защищённого текста (`writeProtectedText`).
Предусловие было по отдельным полям (`sec`/`title`/`body`/`updatedAt`); если
соседняя вкладка меняла статус, срок, перенос, `fields` или любое другое поле
в ту же миллисекунду, старая копия молча её затирала. Теперь все три пути
пишут через один помощник Журнала `guardedPut()`: строка в базе обязана
совпасть со снимком ЦЕЛИКОМ — те же поля и ни одного нового (общий фасад
записей допускает и произвольные поля, поэтому не по списку). Не совпало —
пакет отказывает с прежней ошибкой `journal-protected-conflict`, чужая
строка не тронута.

Общий слой: `CWDB.batch` получил необязательный флаг `exact: true` у
предусловия (`match` — полный снимок записи, сравнение без учёта порядка
ключей). Без флага семантика прежняя. `DB_VERSION` = 6, формат шифрования,
хранилища, копия и контракты J9a/J9b/J9c не менялись.

Новая проверка гейта — `check-journal-protection-race.mjs`: часы заморожены,
гонки в одну миллисекунду воспроизводятся детерминированно; на прежнем коде
она падает. Патч-бамп кэш-фёрст оболочек (правка общего слоя): Конгрессы
4.46.77, Школа 1.14.53, Назначения 5.5.92, Документы 1.11.2; хаб 0.41.55.
Клиндарий не менялся.

## 2026-09-24 — Журнал 0.13.0: задачи для общего To Do (J9c)

Публичная граница, через которую будущий общий экран задач или другой модуль
видит задачи Журнала, — без переноса данных: задачи остаются строками
`journalEntries` (type `todo`), правила — в `CWJournal.tasks`.

Общий слой `shared/todo.js` (`CWTodo`) — только чтение: поставщики по
модулям (`register`), `init/refresh/status/list/get/urlFor/subscribe`.
Контракт задачи: `key` (module:id), `module`, `id`, `ref`, `status`
(open|done), `dueDate`, `text`, `protected`, `mutable`, `origin`, `url`;
замороженные копии, ничего не хранится. Единственный поставщик — Журнал
(`journal/js/todo-provider.js`) через `CWJournal.integration`: сырые строки
без раскрытия J8 — у защищённой задачи `text` = null и при разблокированном
Журнале, `sec` наружу не выходит; одна строка todo — одна задача, перенос и
связи проектов не копируются; `mutable` — по жизненному циклу посещения.

Ссылка на задачу — `#tasks/<id>` (`CWJournalRoute.build.task`): экран
«Задачи», нужная вкладка, строка подсвечена; защищённая закрытая —
обычная разблокировка; нет задачи или кривой хвост — просто «Задачи».
Свежесть без опроса: уведомление в вкладке после каждой записи задач,
`BroadcastChannel('cw-journal')` для соседних (сообщение без данных), bfcache
и `refresh()`; в хранилищах браузера следа нет. После первого `init()` ни
один сигнал не теряется: чтение идёт «насосом» с флагом dirty — сигнал или
новый поставщик во время чтения дают ещё один проход, пачка сигналов
сливается в один; до `init()` мост ленив. Новых хранилищ нет,
`DB_VERSION` = 6, копия Журнала прежняя.

Новая проверка гейта — `check-journal-todo-integration.mjs`. Патч-бамп
кэш-фёрст оболочек (новый общий файл): Конгрессы 4.46.76, Школа 1.14.52,
Назначения 5.5.91, Документы 1.11.1; хаб 0.41.54. Клиндарий не менялся.

## 2026-09-24 — Журнал 0.12.0 и Документы 1.11.0: письма проекта района (J9b)

В карточке проекта Журнала — «Создать письмо» и раздел «Документы проекта».
Второго механизма документов нет: текст — `CWTemplates`, язык —
`CWDocLang`, архив — `CWDocs`, карточки истории — `CWDocsView`, печать —
`CWPrint`. Новый системный шаблон `sys.journal.project.letter` (контекст
`journal.project.letter`, формат text) минимален: тема `{{project.title}}`,
текст `{{project.body}}`; новое пространство переменных `project`. Правка в
композере — только для этого письма; сам шаблон правится в «Документах».
Снимок в архив (`ref {journal, project, id}`) — только печать и «Сохранить
в архив»; просмотр, смена языка и копирование не пишут. `journalLinks` для
документов не заводятся; удаление проекта историю писем не удаляет.

Граница J8 — правило слоя данных (`CWJournal.documents`): защищённый проект
письма не получает ни заблокированным, ни разблокированным; проект со
снимками в архиве защитить нельзя, пока они не удалены; архив недоступен —
защита проекта отказывает — через новый строгий `CWDocs.listStrict()`
(терпимый `list()` при сбое базы отдаёт `[]` и для этого решения не годится).
«Защита ∥ снимок» — не гонка, а сериализация одной транзакцией IndexedDB:
снимок пишется новым `CWDocs.saveGuarded()` одним `CWDB.batch` с предусловием
на строку проекта (та же, без `sec`, не в архиве), защита и первая настройка —
одним пакетом с новым предусловием `expectNone` «у проекта нет снимков».
Кто закоммитил первым, тот и прав; уборки задним числом нет. Архивный проект — только
чтение: история писем видна, новое письмо не собирается. Отказ архива (`CWDocs.save()` → null) не выдаётся за
«сохранено»; при печати — «отправлено на печать, в архив не сохранено».
Композер при каждом открытии перечитывает шаблоны (`CWTemplates.reload()`):
правка в «Документах» видна без перезагрузки Журнала.

«Документы»: Журнал в фильтрах библиотеки и архива; глубокие ссылки
`#template/<id>` и `#archive/<module>/<entity>/<id>` (кривая ссылка — обычный
запуск). Копия Журнала везёт `templates` и `documents` и сливает их; набор
замены J8 прежний — четыре хранилища Журнала.

Новая проверка гейта — `check-journal-documents.mjs`. `DB_VERSION` = 6.
Патч-бамп кэш-фёрст оболочек (правка общего слоя): Конгрессы 4.46.75,
Школа 1.14.51, Назначения 5.5.90; хаб 0.41.53. Клиндарий не менялся.

## 2026-09-23 — Журнал 0.11.0 и Клиндарий 9.95.0: связь посещения с Клиндарием (J9a)

Посещение Журнала связывается с настоящей записью Клиндария. Связь — одна
строка `journalLinks` `journal:entry/<посещение> → cw:circuit-planner/entry/<id>`
(rel `external`): ни дат, ни заголовка, ни названия собрания Журнал не
хранит — всё берётся живым при отрисовке. Кандидаты ранжируются по
собранию (id события = id справочника, только у визитов), пересечению и
близости дат (±14 дней) и типу визита; связь пишется только явным выбором.
У посещения не больше одной такой связи, и это держит ключ: связь —
«слот» с постоянным id на посещение, перепривязка — `put` в тот же ключ,
поэтому две вкладки не оставят двух строк. Прочие внешние связи и связи
проектов не трогаются. Мост обновляет название и по `CWDirectory.onChange()`. Правила «только чтение» закрытого/архивного посещения и запрет
удаления посещения со связями — прежние. Удалённая запись Клиндария связь
не стирает: чип показывает «Запись Клиндария не найдена», её можно
перепривязать или снять; запись с тем же id снова разрешается.

Общий слой: `shared/planner.js` (`CWPlanner`) — публичная граница
Клиндария только для чтения. Читает канон `state/circuit-planner`,
отдаёт замороженные копии `{id, eventId, start, end, title, name, visitType,
communityId}`, ничего не пишет и не кэширует на диске; обновляется по
маячку `cw-state-rev:circuit-planner` (соседняя вкладка) и при возврате из
bfcache, без опроса. Журнал блоб Клиндария не разбирает.

Клиндарий: глубокая ссылка `#calendar?entry=<id>` открывает календарь на
месяце записи с её карточкой; id — по белому списку символов, неизвестная
запись — обычный запуск, PIN не обходится. Ярлыки PWA не изменились.

Новая проверка гейта — `check-journal-planner.mjs`. `DB_VERSION` = 6.
Патч-бамп кэш-фёрст оболочек — правка общего слоя (docs/rules/release.md
§4): Конгрессы 4.46.74, Школа 1.14.50, Назначения 5.5.89, Документы 1.10.40;
хаб 0.41.52.
