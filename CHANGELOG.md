# Changelog

## 2026-08-10

- Клиндарий: блок ближайшего визита в шапке календаря вместо однострочного
  чипа. Раньше показывались только название и дата начала; теперь — сколько
  дней осталось, тип посещения, полный диапазон дат и статусы письма и S-302.
  Статусы только отображаются, переключать их по-прежнему нужно в «Контроле
  отправки» внутри карточки визита.
- Клиндарий: исправлено — в «Ещё действиях» длинные подписи кнопок обрезались
  по нижнему краю («Сформировать S-302» терял вторую строку). Кнопки
  выстроены в один столбец, высота больше не фиксирована.
- Клиндарий: подписи «Ещё действия», «Формуляр визита» и «Сформировать S-302»
  были вписаны по-русски напрямую и оставались русскими при выборе uk/en/pl.
  Теперь берутся из словаря; для uk/en/pl тексты ждут носителей языка.

## 2026-08-09 (3)

- Печатный бланк анкеты и онлайн-форма теперь задают вопросы одними и теми же
  словами. Часть формулировок на бланке изменилась: «Почтовый адрес
  проживания» стал «Адресом проживания», «Номер мобильного телефона» —
  «Телефоном (WhatsApp)». Переводить каждый вопрос теперь нужно один раз,
  а не два.
- Исправлено: на бланке вопрос про автомобиль не помещался в ширину страницы
  и обрезался при печати. Длинные вопросы переносятся на вторую строку.
- Исправлено: линия для ответа рядом с «Другой:» в списке языков заходила
  под саму подпись.

## 2026-08-09 (2)

- Профилактика вёрстки: оболочка модуля больше не может быть растянута шире
  экрана широким содержимым (таблицей, длинной строкой). Видимых изменений
  нет — сейчас ни один модуль до этого предела не доходил.
- Приведена в порядок внутренняя документация: в списке открытых задач
  `AGENTS.md` дизайн-система числилась невнедрённой, хотя закрыта 08.08.2026.

## 2026-08-09

- У анкеты, бланка и всех выгрузок Школы пионеров появился собственный язык —
  отдельный от языка приложения. Выбирается в настройках анкеты; ссылка для
  пионера получает этот язык автоматически. Тексты на украинском, английском,
  польском и немецком пока не написаны, поэтому в этих языках документы
  остаются русскими — переключатель об этом честно предупреждает.
- Исправлено: на странице анкеты после отправки формы не появлялись
  подтверждение и кнопки «отправить по email / в WhatsApp». Данные при этом
  сохранялись, поэтому со стороны выглядело, будто кнопка не работает.
- Исправлено: в PDF пропадали польские и немецкие буквы. Строка «Grüße Straße»
  печаталась как «Gr» — обрывалась целиком на первой же букве с умлаутом.
- Печатный бланк перестал наезжать подписями на линии для ответов, когда
  подписи длиннее русских.

## 2026-08-08 (9)

- Шрифт иконок пересобран и стал вчетверо легче; иконки по имени снова
  работают. На вид ничего не изменилось — модули рисуют иконки векторно.
- Стили всех модулей теперь подключаются в одном порядке. Побочно ожило
  правило, из-за которого шапка Конгрессов не могла подрасти под содержимое.
- Файл стилей Конгрессов приведён в читаемый вид: был одной сплошной строкой,
  стал структурированным. На поведение не влияет.

## 2026-08-08 (8)

- Убраны остатки миграции: неиспользуемые стили в трёх модулях и лишние
  проверки в общем коде навигации. На вид ничего не изменилось — приложение
  стало легче и проще в поддержке.
- Исправлено: правило печати Школы ссылалось на несуществующий блок.

## 2026-08-08 (7)

- Исправлено: выезжающее меню Клиндария уходило под шапку, и верхние пункты
  были недоступны. Теперь меню открывается под шапкой целиком, а кнопка
  возврата в хаб остаётся доступной при открытом меню.
- Высота шапки теперь измеряется, а не задана числом, — меню открывается
  ровно под ней при любом языке и размере шрифта.

## 2026-08-08 (6)

- Исправлено: шапка Клиндария занимала узкую полосу слева вместо всей ширины.
- Исправлено: шапка перекрывала выезжающее меню на узком экране — теперь
  на телефоне она прокручивается вместе со страницей и меню не мешает.
- Исправлено: цифры «Быстрого статуса» в сайдбаре были нечитаемы.

## 2026-08-08 (5)

- Клиндарий приведён к общему виду: появилась шапка с кнопкой возврата в хаб
  на том же месте, что во всех остальных модулях.
- Цветной сайдбар с выбранным вами акцентом сохранён без изменений.
- Полоса периода служебного года больше не остаётся светло-зелёной
  в тёмной теме.
- Этим завершён перевод всех четырёх модулей на единый дизайн.

## 2026-08-08 (4)

- Исправлено: в шапках модулей вместо иконок печатались слова («event»,
  «more_vert»). Шрифт иконок собран без таблицы лигатур, поэтому иконки
  переведены на векторные — как и во всём остальном приложении.
- Та же правка применена к макетам дизайн-системы в docs/design-system.

## 2026-08-08 (3)

- Шапка Конгрессов стала компактной: восемь кнопок собраны в одно меню,
  снаружи остались выбор языка и «Письмо». На ноутбуке шапка больше не
  растягивается в две строки.
- Исправлено: клик по шапке сбрасывал выделенное задание.

## 2026-08-08 (2)

- Конгрессы приведены к общему виду: шапка как в остальных модулях, кнопка
  возврата в хаб на том же месте.
- Сообщения об ошибках и результаты проверки программы больше не остаются
  светлыми в тёмной теме.
- Исправлено: выбор языка интерфейса всегда рисовался белым полем независимо
  от темы.

## 2026-08-08

- Школа пионеров приведена к общему виду: у модуля появилась шапка с кнопкой
  возврата в хаб на том же месте, что в остальных модулях.
- Сайдбар Школы больше не остаётся тёмным при светлой теме — он следует
  выбранной теме, как и всё остальное.
- Убраны цвета прежней палитры, которые не подчинялись теме, и синтетические
  жирные начертания, портившие кириллицу.
- Исправлено: при печати Школы новая шапка попадала бы на бумагу.

## 2026-08-07 (2)

- Слой компонентов дизайн-системы перенесён в `shared/style.css`: оболочка
  модуля, панели инструментов и фильтров, состояния загрузки, пустые
  состояния, баннеры, переключатели, шаги визарда. Разделы 1–3 не менялись.
- Единый фокус с клавиатуры и видимое недоступное состояние кнопок —
  во всех модулях сразу.
- Модуль «Назначения» полностью переведён на общие компоненты. Это эталон,
  по которому дальше приводятся остальные модули.
- Исправлено: при печати «Назначений» правило скрывало классы, которых уже
  не существовало, — на бумагу попадала бы вся форма редактора.

## 2026-08-07

- Единая тема оформления на всё приложение: `shared/theme.js` (`auto|light|dark`,
  по умолчанию системная). Хаб и все модули больше не расходятся по теме.
- Клиндарий переведён с собственного переключателя темы на общий механизм;
  прежний выбор пользователя перенесён без потери.
- Добавлены светлые значения фирменных переменных `--cw-*` — хаб теперь
  корректен в светлой теме.

Только реально внесённые изменения: дата и краткое описание.
Планы — в `ROADMAP.md`, задачи — в `TODO.md`.

## 0.8.2 — 06.08.2026 · статический аудит репозитория

- **Клиндарий v9.60.1 — четыре коллизии ключей локализации.** Словарь
  `circuit-planner/i18n/dict.js` собран из двух источников (атрибуты
  `data-i18n` разметки и старый объект `I18N` из `app.js`), и четыре ключа
  совпали по имени при разном смысле. В объектном литерале побеждает последнее
  значение, поэтому:
  - `cp.letter_subject` — подпись поля «Тема письма» в настройках подменялась
    текстом темы письма («Посещение районного надзирателя»). Видимый баг.
    Мёртвый дубль удалён: тема письма собирается из `LETTER_SUBJECT_PARTS`,
    а не из словаря.
  - `cp.sync_hint` — полная подсказка про кнопку «Открыть Google Drive»
    подменялась укороченной версией. Короткий дубль удалён.
  - `cp.letter_short` — вкладка настроек «✉ Письмо» и бейдж/заголовок таблицы
    «Письмо» делили один ключ. Разведены: вкладка → новый `cp.letter_tab`.
  - `cp.pin_off` — дубль с эмодзи удалён: `🔒` подставляет сам `app.js`,
    иначе в кнопке оказалось бы два замка.
  Итог по всем четырём языкам: 499 ключей, составы совпадают, дублей нет.
- **`shared/nav.js` локализован.** Подпись кнопки возврата больше не
  захардкожена по-русски — берётся из `common.back_to_hub` и перерисовывается
  по `CWI18n.onChange`, как и кнопка копии модуля. Без подключённого `CWI18n`
  обе деградируют на русскую строку.
- **Документация приведена в соответствие с кодом:**
  - `ARCHITECTURE.md` §4 утверждал, что письмо «Назначений» всегда украинское,
    тогда как §7 того же файла и код (`DOC_LANGS`) дают uk/ru/de;
  - `ARCHITECTURE.md` §7 и §8 считали Клиндарий не подключённым к
    `shared/doclang.js` — он подключён с v9.60.0;
  - `ARCHITECTURE.md` §3 требовал скрипты локализации только в `<head>` —
    реальное требование это порядок подключения, а не место;
  - `AGENTS.md` не знал о модуле «Назначения» (список модулей, дерево
    репозитория, чек-лист выпуска, снимок версий);
  - `README.md` и `AGENTS.md` называли «Формуляр посещения» четвёртым модулем,
    хотя четвёртый теперь — «Назначения».
- **Без замечаний:** синтаксис всех JS, граф ES-импортов, `src`/`href` в шести
  HTML, списки ассетов пяти service worker'ов, структура разметки, дубли `id`,
  пять манифестов и их иконки, CSS-переменные, реестр `shared/backup.js`
  против реальных ключей хранилищ, согласованность версий.

## Восстановление после сбоя загрузки — 06.08.2026

- При загрузке файлов в репозиторий (коммит `b1a649a` «renewed») были удалены
  8 файлов: точки входа всех четырёх модулей (`*/index.html`), три service
  worker'а модулей (`appointments/sw.js`, `circuit-planner/sw.js`,
  `pioneer-school/sw.js`) и реестр версий `shared/version.js`.
  Деплой перестал работать: хаб не мог прочитать версии, плитки вели на 404.
- Файлы восстановлены из последнего состояния, где они существовали
  (коммит `a494646`), то есть с уже поднятыми версиями — отката логики нет.
- Содержимое остальных файлов сверено с состоянием до сбоя: расхождений нет,
  все `shared/*` и модульные файлы байт-в-байт идентичны.

## 0.8.1 — 06.08.2026

- Версия Конгрессов синхронизирована между модулем и реестром хаба (`4.20.0`).
- Устаревшая версия удалена из HTML-заголовка страницы Конгрессов.
- Добавлена автоматическая CI-проверка согласованности версий хаба и модулей.

## 2026-08-06

- **`shared/backup.js`** — резервное копирование хаба и модулей (хаб v0.8.0).
  - На главной раздел «Резервное копирование»: одна кнопка сохраняет данные
    всех модулей и общий слой в один JSON с манифестом; таблица показывает по
    каждому модулю версию и дату последней копии.
  - В каждом модуле рядом с кнопкой возврата появилась кнопка копии — она
    сохраняет только этот модуль. Встроена один раз в `shared/nav.js`, поэтому
    в самих модулях кода не прибавилось.
  - Реестр декларативный: модуль объявляет, ГДЕ его данные (ключи
    localStorage, базы IndexedDB), а обход и сериализацию делает общий
    механизм. Новый модуль = одна запись.
  - Восстановление только целиком: проверка формата до записи, подтверждение с
    датой и версией копии, автоматическая выгрузка текущего состояния перед
    заменой, перезагрузка страницы после.
  - PIN-код Клиндария (`syp-pin-hash`) в копию не попадает и при
    восстановлении не трогается.

## 2026-08-05 (третья поставка)

- **Клиндарий подключён к общим слоям** (v9.60.0) — до этого он оставался
  единственным модулем со своей копией данных отправителя и с языком документа,
  привязанным к языку интерфейса.
  - `App.shared.adopt()` переносит прежние `senderName/Address/Phone/Email` в
    `cw-sender` и удаляет их из данных модуля; поля настроек читают и пишут
    общий слой, `CWSender.onChange` перерисовывает их при правке из соседнего
    модуля.
  - `CWDocLang.init(...)` + `adopt(settings.language)` — прежнее поведение
    засеивается, дальше язык документа независим. Фолбэк формуляра визита:
    `event.formLanguage || CWDocLang.get()`.
  - Подсказка в формуляре («совпадает с языком интерфейса») исправлена во всех
    четырёх языках — она перестала быть правдой.
- **`appointments` внесён в `CW_MODULES`** — записи не было, из-за чего плитка
  показывалась без номера версии.
- Записи об этом были в документации раньше, чем в коде: `CHANGELOG` уже
  утверждал, что Клиндарий переведён на `shared/sender.js`, а `app.js` его не
  использовал. Стоит сверять такие пункты с кодом перед закрытием.

## 2026-08-05 (вторая поставка)

- **`shared/sender.js`** — единый источник данных отправителя для всей
  экосистемы (ключ `cw-sender`, поля `name/code/address/phone1/phone2/email`).
  - Конгрессы, Клиндарий и Назначения переведены на него; свои копии данных
    удалены, прежние значения переносятся при первой загрузке.
  - Убрано чтение хранилища Конгрессов из Назначений — ни один модуль больше не
    зависит от внутренних данных другого.
  - Подсказки названий собраний в Назначениях теперь берутся из собственной
    истории ввода, а не из справочника Конгрессов.
- **`shared/doclang.js`** — язык документа как отдельная от интерфейса
  настройка (ключи `cw-doclang`, `cw-doclang:<module>`).
  - Письмо «Назначений» доступно на украинском, русском и немецком; выбор — в
    панели рядом с кнопкой печати. Дата и имя PDF-файла следуют языку документа.
  - Строки документов регистрируются в тех же словарях `CWI18n` под префиксом
    `doc.*`, разметка помечается `data-doc-i18n` — второго механизма перевода не
    появилось.
  - Выбор языка писем в Конгрессах (`settings.language`) переведён на общий
    механизм; прежнее значение переносится автоматически.
- Версии: хаб v0.9.0, Конгрессы v4.20.0, Клиндарий v9.60.0, Назначения v5.1.0.

## 2026-08-05 (первая поставка)

- Добавлен модуль **«Назначения»** (`appointments/`, v5.0.0): письмо о
  назначении и вычёркивании старейшин и служителей собрания.
  - Состояние сохраняется в localStorage (`cw-appointments-v1`) — исходный
    автономный бланк терял всё при перезагрузке.
  - Интерфейс переведён на ru/uk/en/pl/de; текст письма остаётся украинским как
    свойство документа.
  - Данные отправителя берутся из настроек писем модуля «Конгрессы»; в панели
    есть переключатель на локальные значения.
  - Пустые разделы письма печатаются всегда; отсутствие содержимого показывает
    короткая центрированная линия-плейсхолдер.
  - Собственный service worker с префиксом кэша `appointments-cache-v`,
    манифест и комплект иконок в фирменном стеклянном стиле.
- Хаб обновлён до **v0.8.0**: четвёртая плитка, сетка перестроена с 3 на 4
  колонки со ступенями 1240 / 1000 / 700px, добавлена четвёртая ячейка
  шины-коннектора.
- `shared/version.js`: модуль внесён в `CW_MODULES`, чек-лист выпуска дополнен.
- `shared/i18n/common.js`: добавлены `module.appointments.title` и `.desc` на
  пяти языках.
- Документация перенесена в корень монорепо: `README.md` переписан как точка
  входа с индексом документов, добавлены `ARCHITECTURE.md`, `TODO.md`,
  `IDEAS.md`, `ROADMAP.md`, `CHANGELOG.md`, `CLAUDE.md`.
