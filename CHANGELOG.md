# Changelog

Здесь — последние 10 записей. Всё старше вынесено в `docs/changelog/`
(30.08.2026, фаза 2 аудита контекста): файл читался целиком и стоил
~76 000 токенов. Записи не изменены, только разделены по дате.

Правило: держать в этом файле не больше 10 записей, старые переносить
в архив текущего месяца.

## 2026-09-01 (пятый заход) — Документы: UX-переделка селектора переменных

**Версии:** Документы `1.5.27 → 1.6.0` (minor — новый функционал, не патч).
Version-only патч-бамп (правка `shared/version.js` требует его у cache-first
оболочек — правило `docs/rules/release.md` §4/§7, `check-shared-bump.mjs`
пропущен на мелком клоне, но правило применено вручную по коду прекэша):
хаб `0.41.4 → 0.41.5`, Конгрессы `4.46.26 → 4.46.27`, Школа пионеров
`1.14.4 → 1.14.5`, Назначения `5.5.43 → 5.5.44`. Клиндарий не в списке —
у него stale-while-revalidate, бамп не требуется (исключение §3).

Постоянная правая колонка «Переменные» (`<aside class="vars">`) в редакторе
шаблонов заменена на единый `<dialog>`: кнопка «+ Вставить переменную»
открывает компактный selector (desktop ~420px по центру, mobile — bottom
sheet через переиспользованный breakpoint 900px, одна разметка и одна
JS-логика без anchored-popover positioning engine). Редактор теперь занимает
всю доступную ширину.

- **Presentation-слой вынесен в новый `documents/js/variables.js`** — mapping
  `namespace.field → i18n-ключ` человеческого названия для всех 46
  видимых переменных, и список полей, скрытых из picker'а (не из реестра и
  не из движка): `visit.type`, `visit.typeLabel`, `doc.lang`. Все три
  проверены живым разбором фактического использования — `visit.type`
  технический код события, `visit.typeLabel` заполняется пустой строкой
  ВСЕГДА (`circuit-planner/app.js`, `letterData()`), `doc.lang` служебный код
  языка для внутренней логики модулей. Ни один из трёх не встречается в
  `shared/templates/builtin.js`. Реестр `shared/templates/namespaces.js`
  и движок подстановки не тронуты — старые шаблоны и aliases продолжают
  работать как раньше.
- **Поиск** по человеческому названию, `ns.field`, текущему token и старым
  aliases (алиасы участвуют в поиске, но не показываются в карточке
  результата — старое `doc.was_named` в UI больше не выводится, хотя ключ
  в словаре сохранён).
- **Caret/selection fix.** `insertToken()` не переписан. Обнаружен и
  устранён реальный риск: открытие диалога переводит фокус в поле поиска,
  и `window.getSelection()` в RTE-режиме теряет позицию курсора редактора
  (`rte.contains(sel.anchorNode)` перестаёт быть истиной — подтверждено
  экспериментом на jsdom, включая то, что даже повторный `rte.focus()`
  восстанавливает Range на позицию 0, а не на исходное место). Добавлена
  обвязка `saveEditorCaret()`/`restoreEditorCaret()`: сохраняет курсор перед
  открытием диалога, восстанавливает перед каждой вставкой; работает и для
  textarea (`selectionStart`/`selectionEnd`, риска не было изначально), и
  для RTE (клонированный `Range`). Диалог остаётся открытым после вставки —
  несколько переменных подряд вставляются корректно одна за другой.
- **i18n:** 59 новых ключей в `documents/i18n/dict.js` (46 меток переменных +
  8 названий категорий + 5 строк UI диалога), все 5 языков закрыты сразу
  (переводы утверждены Алексом), долг перевода по фазе = 0.
- Не сделано в этой фазе (сознательно, по утверждённому плану): autocomplete
  по синтаксису переменных, split-view текст+preview — оставлены отдельными возможными фазами.

Live-прогон Playwright/Chromium не выполнялся: `cdn.playwright.dev` не в
allowlist сети этой сессии, скачивание браузера невозможно. Отмечено как
известное ограничение среды, не блокер фазы (по решению Алекса). Проверки
заменены статическими (jsdom-симуляция логики caret/поиска/module filtering)
— это не эквивалент живого браузерного теста.



**Версии:** хаб `0.41.3 → 0.41.4`; Клиндарий `9.94.3 → 9.94.4`. Version-only
патч-бамп (правка `shared/update.js`/`shared/version.js` требует его у
cache-first оболочек, подтверждено `check-shared-bump.mjs`): Конгрессы
`4.46.25 → 4.46.26`, Школа пионеров `1.14.3 → 1.14.4`, Назначения
`5.5.42 → 5.5.43`, Документы `1.5.26 → 1.5.27`. Клиндарий сам не в этом
списке — bump тут функциональный (свой фикс), не version-only каскад.

Два дефекта с реального Android-теста.

- **Полоса «Доступна нова версія» частично уходила ниже видимой области**
  на телефонах с нижней жестовой зоной/навигационной панелью —
  `#cwUpdateBar{bottom:16px}` не учитывал `env(safe-area-inset-bottom)`.
  Добавлено `calc(16px + env(safe-area-inset-bottom, 0px))`
  (`shared/update.js`, общий для всех модулей). Для реального эффекта на
  хабе также добавлен `viewport-fit=cover` в его `<meta viewport>`
  (`index.html`) — без него `env(safe-area-inset-*)` всегда возвращает 0;
  у Клиндария этот атрибут уже был.
- **Дублирующее авто-напоминание о backup в Клиндарии** (`window.confirm`
  «Останній backup був N дн. тому») отключено — теперь есть общий backup
  всех модулей в хабе. Убран только вызов `checkAutoBackupReminder()` на
  старте (`circuit-planner/app.js`); сама функция и ручной
  backup/export/import (`exportJson`, кнопки `#backupBtn`/
  `#exportConfirmBtn`) не тронуты.



**Версии:** Конгрессы `4.46.24 → 4.46.25`; хаб `0.41.2 → 0.41.3`. Version-only
патч-бамп (правка `shared/version.js` требует его у cache-first оболочек,
подтверждено `check-shared-bump.mjs`): Школа пионеров `1.14.2 → 1.14.3`,
Назначения `5.5.41 → 5.5.42`, Документы `1.5.25 → 1.5.26`. Клиндарий не
тронут — stale-while-revalidate, свежий общий слой подхватит сам.

Два дефекта с реального Android-теста.

- **Section rows программы («РАНКОВА ПРОГРАМА» и т.п.) не открывались тапом
  на mobile** — прошлая фаза сделала tap-to-edit прямым для обычных заданий,
  но явно исключила разделы (`if(compact&&!section)`), оставив их на старой
  select→actions-cell схеме. Теперь tap по разделу тоже вызывает
  `openEdit(t.id)` напрямую, как и у обычных заданий; `.actions-cell`
  (Edit/Mail/Delete) для всех строк, включая разделы, показана всегда
  компактным рядом — без новой бизнес-логики, те же существующие
  `.ed`/`.le`/`.rm`.
- **`#backupGuardBtn` («Повернутися до стану перед відновленням») в хабе
  вылезал за карточку на mobile** — базовый `.cw-btn` держит
  `white-space:nowrap` без `max-width`, что подходит для коротких подписей
  остальных кнопок хаба, но самая длинная подпись раздвигала именно эту
  кнопку шире карточки. Исправление точечное: только `.cw-backup__actions
  .cw-btn` на ≤560px получает `width:100%` и разрешение переноса текста;
  базовый `.cw-btn` и остальные кнопки хаба не изменены.



**Версии:** Конгрессы `4.46.23 → 4.46.24`. Version-only патч-бамп (правка
`shared/version.js` требует его у cache-first оболочек, подтверждено
`check-shared-bump.mjs`): хаб `0.41.1 → 0.41.2`, Школа пионеров
`1.14.1 → 1.14.2`, Назначения `5.5.40 → 5.5.41`, Документы `1.5.24 → 1.5.25`.
Клиндарий не тронут — stale-while-revalidate, свежий общий слой подхватит сам.

Два реальных дефекта, найденных на живом Android-тесте после прошлой фазы.
Только `congress-project/styles.css`.

- **`#editDialog` всё ещё был шире viewport на реальном телефоне**, хотя
  прошлая фаза закрыла известные min-content источники внутри формы
  (`.form-grid`/`.checks`/`label`/`input`/`select`). Сам `<dialog>` (не
  только `.modal` внутри него) не имел собственного mobile-ограничения
  ширины — добавлен внешний потолок прямо на `<dialog>`
  (`width/max-width: calc(100vw - 16px)`), независимо от того, какая именно
  внутренняя деталь могла оставаться источником overflow. Конкретный
  оставшийся intrinsic-offender без настоящего browser inspector не
  подтверждён — зафиксировано в отчёте честно, не как «найдена точная
  причина».
- **Edit/Mail/Delete пропали с обычных карточек программы на mobile** после
  того, как прошлая фаза сделала тап по карточке прямым `openEdit()` —
  `.actions-cell` показывалась только по `tr.selected`, а обычный тап
  больше не выставлял `.selected`. Исправлено показом `.actions-cell` для
  обычных строк (не разделов) всегда, компактным рядом из уже существующих
  кнопок `.ed`/`.le`/`.rm` — без новой бизнес-логики. Разделы программы не
  затронуты, их прежняя схема выбора строки сохранена.



**Версии:** Конгрессы `4.46.22 → 4.46.23`. Version-only patч-бамп (правка
`shared/version.js` требует его у cache-first оболочек, подтверждено
`check-shared-bump.mjs`): хаб `0.41.0 → 0.41.1`, Школа пионеров
`1.14.0 → 1.14.1`, Назначения `5.5.39 → 5.5.40`, Документы `1.5.23 → 1.5.24`.
Клиндарий не тронут — stale-while-revalidate, свежий общий слой подхватит сам.

Небольшая mobile-фаза congress-project поверх текущего HEAD, без архитектурной
переделки. Только этот модуль получил функциональные изменения.

- **Tap-to-edit на compact (≤680px):** тап по свободной области обычной
  карточки задания сразу открывает `openEdit(t.id)`, вместо только выделения
  строки. Desktop и разделы программы (`section-title`) — прежнее поведение.
  Guard интерактивных потомков расширен до `button,select,input,textarea,a,label`.
- **Исправлен горизонтальный overflow `#editDialog` и `#printColumnsDialog`
  на мобильном** — причина в min-content: `label`/`.epart`/`.time-row` внутри
  grid-раскладки (`.form-grid`/`.checks`) по умолчанию не сжимаются ниже
  intrinsic-ширины своих `input`/`select` (длинные `<option>`, нативный
  минимум `input[type=date]`). Исправлено явным `min-width:0` по всей цепочке
  grid→label→input/select, не `overflow-x:hidden`. `.checks` также переведена
  в одну колонку на ≤680px — раньше не входила в список наравне с `.form-grid`.
- **Найдена и исправлена причина «tools menu почти без подписей» на узком
  экране:** правило `.top-actions span[data-i18n]{display:none}` (≤900px)
  случайно гасило подписи и внутри `.md-menu__panel`, так как меню лежит
  физически в `.top-actions`. Разметка (иконка+подпись) была верной с начала;
  правка — `:not(.md-menu *)` в том же локальном селекторе, `shared/style.css`
  не тронут.
- **`participant.phone`** — новое опциональное поле, backward-compatible
  (`{name, congregation, phone}`, старые записи без `phone` читаются как
  `phone:""`). Добавлено в редактор участника (`input[type=tel]`), в
  `cloneTask(..., "emptyPeople")`, в печатный план как отдельная опциональная
  колонка («Мобільний номер», формат «Имя — номер»). Полный clone,
  `propagateTask`, backup/export/import переносят поле без правок — уже
  сериализуют участника как объект целиком.


**Версии:** Клиндарий `9.93.0 → 9.94.3`. Остальные модули не затронуты.

Уточнение предыдущей правки той же карточки: визуально высокая и пустая на
mobile, плюс «Нужно отправить» появлялось даже когда по рабочему правилу
письмо ещё рано отправлять.

- **Root cause высокой пустой карточки — исправлен через layout, не
  замаскирован фиксированной высотой.** `.nv-row` держал
  `align-items:stretch`, из-за чего компактная карточка растягивалась под
  высоту соседней «Следующее посещение» (там контента больше — тип
  посещения, диапазон дат, два флага). Заменено на `align-items:flex-start`
  на контейнере + явный `align-self:stretch` только на `#nextVisitPill` —
  основная карточка не потеряла высоту, компактная считает её от своего
  содержимого.
- **Порог 60 дней до начала посещения** (по дате начала, не окончания —
  та же формула `daysUntil`, что и в `App.data.getUpcomingReminders()`, не
  отдельная копия): `> 60` дней — зелёный индикатор, «Ещё рано · До
  отправки N дня»; `≤ 60` — красный индикатор, «Пора отправить письмо».
  Цвет — только у маленькой точки и статусного текста, не у всей карточки;
  использованы существующие токены `--status-success`/`--status-critical`
  из общего слоя (тот же паттерн, что `.success-banner` и `.ls-tag.ok` в
  соседних модулях), новых цветов не вводилось. Никакого нового
  persisted-флага — чисто вычисляемое UI-правило поверх `entry.flags.letter`.
  Состояния `partial`/`full` остались зелёными и без порога — это спокойные
  success-состояния, не про срочность конкретного письма.
- **Mobile:** компактный status-block по содержимому, без больших пустых
  областей; длинное название переносится, статус-строка и стрелка не рвут
  layout, горизонтального скролла нет. Desktop: обе карточки по-прежнему в
  ряд, вторая — компактнее основной.
- **Второй заход по mobile-высоте (9.94.1), после реальной проверки на
  телефоне.** Первое исправление root cause (`align-items:stretch →
  flex-start` на `.nv-row`) оказалось верным, но неполным — огромная пустая
  область под компактной карточкой сохранялась. Настоящая причина: `.nv-row`
  переключается на `flex-direction:column` на mobile, а вместе с этим
  меняется смысл `flex-grow` у детей — то же самое число, что на desktop
  растягивало карточки по ширине, на mobile растягивало их по высоте,
  заставляя обе карточки делить свободное вертикальное пространство
  контейнера поровну вне зависимости от объёма контента. Исправлено явным
  `flex:0 0 auto` на `#nextVisitPill` и `#letterDuePill` только внутри
  `@media (max-width:767px)` — desktop-правила не тронуты.
- **Третий заход, ширина letterDuePill на mobile (9.94.2), после
  подтверждения root cause высоты живым скриншотом с телефона.** Высота уже
  была верной, но карточка визуально оказывалась у́же соседней «Следующее
  посещение» — обе стороны совпадали не полностью. Причина: в
  `flex-direction:column` шириной (кросс-осью) управляет `align-items`, а не
  `flex-grow`; базовое `align-items:flex-start` на `.nv-row` не
  переопределялось на mobile, поэтому каждый ребёнок брал ширину по
  собственному содержимому — у более насыщенной `#nextVisitPill` она и так
  была близка к полной, у компактной `#letterDuePill` — меньше. Добавлен
  один `align-self:stretch` только на `#letterDuePill` внутри того же
  media query; `flex:0 0 auto` (высота) не тронут — `align-self` в
  column-контейнере влияет только на кросс-ось (ширину), не на главную
  (высоту).
- **Четвёртый заход (9.94.3), системная Grid-переработка после проверки на
  нескольких реальных desktop/tablet ширинах.** Три предыдущих захода чинили
  высоту и ширину точечными flex-хаками — по отдельности верными, но вместе
  оставлявшими раскладку "плавающей": на tablet/wide-desktop пара карточек
  имела фиксированный `max-width:760px` и превращалась в два маленьких
  острова с пустым пространством справа вместо использования реальной
  ширины календарной колонки. `.nv-row` переведён с flex на CSS Grid:
  `grid-template-columns: minmax(0,1.6fr) minmax(280px,1fr)` на `>767px`
  (пропорция ~60–65%/35–40%, без верхнего потолка ширины), `1fr` на
  `≤767px` (mobile — без изменений). `align-items:start` на Grid — это
  прямой аналог прежнего `align-items:flex-start`, без него Grid по
  умолчанию растягивает ячейки строки и высота снова "поплыла" бы, уже
  через Grid, а не через flex-grow. Все точечные flex/max-width/align-self
  правила на `.nv-row` и её детях убраны как избыточные.
- **Уточнение той же правки: `.nv-row` не равен левой колонке календаря.**
  `.nv-row` физически живёт в `<header class="topbar">`, вне
  `.calendar-layout` (сиблинг выше по DOM, не потомок) — на wide desktop
  (`≥1181px`, где у `.calendar-layout` реально появляется правая
  detail-панель `.calendar-side`) `width:100%` у `.nv-row` означало 100%
  ширины `.main`, включая пространство над правой панелью, а не ширину
  левой колонки. Добавлено `@media (min-width:1181px){.nv-row{max-width:
  calc(100% - var(--calendar-side-width) - var(--calendar-layout-gap))}}`
  — та же ширина, что у левой колонки `.calendar-layout`, через те же
  переменные (`--calendar-side-width`, и новая `--calendar-layout-gap:22px`,
  вынесенная, чтобы не дублировать значение gap в двух местах), не
  отдельные магические числа. Порог `1181px` — не изолированное новое
  число, а парная граница к уже существующему `max-width:1180px` у
  `.calendar-layout` (та же природа, что у пар `767/768`, `1100/1101`,
  `1200/1201` — задокументировано отдельной записью в
  `docs/design-tokens/03-breakpoints.md`, добавлено в `LEGACY` в
  `scripts/check-breakpoints.mjs` с обоснованием в комментарии). Ниже
  порога (tablet, 768–1180px) правило не действует — правой колонки там
  физически нет, верхний dashboard законно занимает всю ширину `.main`.
- Новые i18n-ключи `cp.needs_sending_now`, `cp.letter_soon`,
  `cp.days_until_send` — добавлены на всех 5 языков (тексты утверждены
  явно, не сгенерированы; переменные `{value}`/`{label}` проверены во всех
  переводах).
- Затронутые файлы: `circuit-planner/{app.js,index.html,style.css,
  ui/ui-aux.js,i18n/dict.js}` (9.94.0) + `circuit-planner/style.css`
  (9.94.1 — высота, 9.94.2 — ширина letterDuePill, 9.94.3 — системная
  Grid-модель и учёт правой панели), `docs/design-tokens/03-breakpoints.md`
  и `scripts/check-breakpoints.mjs` (9.94.3, фиксация пары `1180/1181`),
  `shared/version.js` (версия). PDF, S-302, IndexedDB/localStorage, backup,
  service worker не тронуты.
- Гейт: **22 применимых проверки, все зелёные** на каждом из четырёх
  заходов, включая отдельно проверенное покрытие словарей (§2 «только
  по-русски» — пусто, долга перевода нет), переменные в переводах и (с
  9.94.3) реестр контрольных точек — новая пара `1180/1181` задокументирована
  и внесена в `LEGACY` осознанно, стык проверен как сомкнутый без щели, той
  же логикой, что подтверждает три существующие пары. «Патч-бамп при
  правке общего слоя» пропущена — мелкий клон без git-истории, `shared/*`
  функционально не менялся. Живой Chromium-прогон в среде выполнения не
  выполнялся ни разу (браузер недоступен), но итоговый результат (высота,
  ширина, и теперь Grid-раскладка на трёх диапазонах ширины) подтверждён
  реальными скриншотами с телефона и desktop от Алекса.

## 2026-08-31 — Календарь: PDF формуляра и карточка «Письмо не отправлено»

**Версии:** Клиндарий `9.92.0 → 9.93.0`. Остальные модули не затронуты.

Точечная UX-задача без изменения модели данных, локальный контекст
(`circuit-planner` целиком, соседние модули не открывались).

- **PDF формуляра посещения:** строка «Тип посещения: Собрание / Группа /
  Предгруппа» убрана из рендера; сразу после заголовка страницы — название
  собрания/группы с номером в скобках, например `Sangerhausen-Ukrainisch
  (35865)` (номер — из `event.congNumber`, при отсутствии — без скобок).
  `state.visitType`/`event.visitType` в данных не тронуты — используются
  фильтрами, календарём, backup/export как прежде; i18n-ключи
  `visitTypeMeeting/Group/Pregroup` и `pdfVisitTypeLabel` оставлены
  нетронутыми в словаре модуля, просто не рендерятся в этом формуляре.
  Освободившееся место не компенсировано — авто-fit по уровням плотности
  (`STYLE_TIERS`) сам подтягивает контент вверх.
- **Новая карточка «Письмо не отправлено»** рядом с «Следующим посещением» в
  топбаре календаря. Без нового хранимого состояния — вычисляется из
  `entries[].flags.letter`, с дедупом против визита, уже показанного в
  основной карточке. Три состояния:
  - есть другой будущий визит с неотправленным письмом → показывается он;
  - основной визит сам без письма и дублировать нечего, но письмо
    фактически не отправлено → «Остальные ближайшие письма отправлены»
    (`cp.other_letters_sent`);
  - неотправленных писем нет вовсе среди будущих визитов → «Все ближайшие
    письма отправлены» (`cp.all_letters_sent`).
  Desktop — обе карточки в ряд; mobile (≤767px) — компактный блок под
  основной, чисто CSS-раскладкой без отдельной мобильной бизнес-логики.
  S-302 эта карточка не касается — только статус письма.
- Новые i18n-ключи `cp.all_letters_sent` и `cp.other_letters_sent` добавлены
  на всех 5 языков модуля (тексты утверждены явно, не сгенерированы).
- Затронутые файлы: `circuit-planner/{app.js,visit-pdf.js,index.html,
  style.css,ui/ui-aux.js,i18n/dict.js}`, `shared/version.js` (версия).
- Гейт: **22 применимых проверки, все зелёные**; «Патч-бамп при правке
  общего слоя» пропущена — мелкий клон без git-истории, `shared/*`
  функционально не менялся (только версия в реестре). Живой
  Chromium-прогон не выполнен: браузер недоступен в среде выполнения задачи;
  раскладка desktop/mobile проверена по CSS box-model и статическому
  снапшоту разметки со style.css модуля.

## 2026-08-31 — N-3 закрыт: ПДФ-стек Школы на догрузке по требованию

**Версии:** хаб `0.40.18 → 0.41.0` · Конгрессы `4.46.21 → 4.46.22` ·
Клиндарий `9.91.22 → 9.92.0` · Школа `1.13.28 → 1.14.0` · Назначения
`5.5.38 → 5.5.39` · Документы `1.5.22 → 1.5.23`. Патч-бамп всех модулей:
правка `shared/pdfstack.js` и `shared/i18n/common.js`.

Гейт **23 проверки, все зелёные.** Живой прогон пяти трактов выдачи бумаг
Школы (`scripts/live-tracts.mjs`) с перехватом конструктора `jsPDF`: холодный
старт без единого имени ПДФ/Excel-стека, каждый набор догружается один раз,
письмо/бланк/список/экспорт .xlsx/анкета доводятся до настоящего содержимого
на бумаге. **Отчёт `docs/audit/02-recheck.md` закрыт целиком.**

- **ПДФ-стек Школы пионеров переведён на догрузку по требованию.** Семь
  файлов, 2913 КБ, разбирались при каждом открытии модуля — теперь по
  кнопке, четырьмя наборами (`pdf`, `import`, `excel`, `form`). Одиннадцать
  точек выдачи защищены `ensure()`. Механизм (`shared/pdfstack.js`) вынесен
  из `App.pdf` Клиндария в общий слой; Клиндарий переведён на него же.
- `js/export/excelExport.js` разрезан: `.xlsx`-выгрузка (SheetJS) уехала в
  `js/export/xlsxExport.js`, три CSV-выгрузки остались на старте.
- Мёртвый тракт `register.html` удалён: две кнопки, 57 строк мёртвого кода,
  девять осиротевших ключей словаря на пяти языках.
- `check-lazy-pdf.mjs` переписан целиком: `DEBT` пуст, храповик обходит все
  страницы модуля, признаки — разбор `acorn` вместо отступа в шесть
  пробелов. Прогнан на семи сломанных входах. `check-sri.mjs` обновлён под
  новый адрес воркера pdf.js.
- Подробности и все обоснования — `docs/journal/pdf-print.md`.

## 2026-08-31 — AI-context optimization: фазы 1–4 закрыты

**Версии:** хаб `0.40.14 → 0.40.18` · Конгрессы `4.46.17 → 4.46.21` ·
Клиндарий `9.91.16 → 9.91.22` · Школа `1.13.24 → 1.13.28` · Назначения
`5.5.34 → 5.5.38` · Документы `1.5.18 → 1.5.22`. Пять патч-бампов подряд —
каждая правка `shared/version.js` сама оказалась событием уровня общего слоя
(см. `docs/rules/release.md` §7, technical debt).

- `AGENTS.md`: 413,6 КБ → 15,7K chars / 24,3 КБ UTF-8. 52 раздела перенесены
  дословно в `docs/journal/*.md` (9 файлов), ни строки не потеряно.
  `CHANGELOG.md` и `TODO.md` разрезаны по тому же принципу, архивы —
  `docs/changelog/`, `docs/todo-archive/`.
- Добавлены `.aiignore`, `scripts/check-context-budget.mjs` (гейт: 23-я
  проверка, warning 16K chars / fail 20K chars), 6 локальных `AGENTS.md`
  по модулям, `docs/rules/{delivery,release}.md`.
- `circuit-planner/app.js`: 5467 → 4794 строки (6 срезов через
  `window.CPParts`: `calendar-styles.js`, `doc-templates.js`, `modals.js`,
  `ui-aux.js`, `ui-toggles.js`; плюс вынос inline CSS в `style.css`,
  `index.html` 104 КБ → 64,6 КБ). Гейт 23/23 и живой прогон чисты на каждом
  срезе.
- **PIN/security и визит-данные/документы/композер (`entry.visitForm`,
  `CWDocs.save()`, композер) сознательно оставлены нетронутыми** — не
  техдолг, решение по риску/пользе. `App.store`/`App.shared` не выносились
  по той же причине.
- Полный разбор и итоговые цифры — `docs/context/01-ai-context-audit.md`.
## 2026-08-29 (шестой заход) — повторный аудит закрыт, кроме N-3

**Версии:** хаб `0.40.12 → 0.40.14` · Конгрессы `4.46.15 → 4.46.17` ·
Клиндарий `9.91.14 → 9.91.16` · Школа `1.13.22 → 1.13.24` · Назначения
`5.5.32 → 5.5.34` · Документы `1.5.16 → 1.5.18`. Два патч-бампа: правки
`shared/backup.js`, `shared/state.js`, `shared/theme.js` и хаба.

Гейт **21 → 22 проверки**, все зелёные. Живой прогон шести страниц в Chromium:
отрисовка, ноль ошибок консоли, ноль упавших запросов.

### Исправлено

- **N-1 (CRITICAL): потолок версии базы для МОДУЛЕЙ.** Копия из более новой
  версии Школы поднимала `pioneer-school-db` до версии, которой модуль её уже
  не откроет, — необратимо. Потолком служила схема самой копии, то есть ровно
  та величина, до которой база и поднималась: защита не срабатывала никогда.
  Теперь подъём версии базы модуля разрешён только когда хранилищ нет вообще
  («призрачная» база). Проверка добавлена ДО правки и падала на старом коде.
- **N-2: откат к предохранительному снимку.** Кнопка отдавала снимок файлом
  через `download()`, который может отказать молча, — страховка была
  недоступна ровно в тех условиях, ради которых заводилась. Теперь откат
  идёт `guard.get() → restore()`, той же функцией, что и после разбора файла.
  Новых строк перевода не понадобилось: интерфейс уже обещал именно откат.
- **N-7: перевод в разметке экранируется.** 67 мест в пяти файлах. Заодно
  найден настоящий баг: `${…}` внутри обычной строки в Конгрессах —
  пользователь видел `${t("cong.opt.no_series")}` буквально.
- **N-6: повторная подписка.** `CWState.onForeign()` ставил второй слушатель
  `storage` на каждый вызов, `CWTheme.mountSelect()` — второй обработчик
  `change` на тот же `<select>`.
- **N-9: призрачные базы.** Снятие копии больше не создаёт пустых баз
  модулей, которые на устройстве ни разу не открывали.
- **N-4: замок подписей.** Шапка о том, что сверять ему нечего (CDN убран), и
  сохранение этой шапки при перезаписи `build-sri.mjs --lock`.

### Проверки

- **Новая, 22-я: «Перевод в разметке»** (`check-i18n-html.mjs`) — разбор AST.
  Ловит перевод в `innerHTML` без экранирования и подстановку в обычной строке.
- **`check-lazy-pdf.mjs` переписан.** Был зашит на один модуль, и зелёная
  строка сводки молчала про 2913 КБ Школы. Теперь обходит все модули реестра;
  долг Школы записан явно и печатается при каждом прогоне.
- **`check-orphan-classes.mjs` стал двусторонним.** Правило общего слоя без
  потребителя: 39 мёртвых зафиксированы храповиком, 76 живут только в каталоге
  дизайн-системы.
- **`check-backup.mjs`:** сценарии базы модуля (5.6, 5.7) и призрачных баз.
- Инструменты живого прогона в репозитории: `live-run.mjs`, `live-ceiling.mjs`,
  `live-splash.mjs`. Не в гейте — ему нужен браузер.

### Рассмотрено и отклонено

- **N-5 (пустой перевод).** Повтор уже отклонённого 28.08.2026 предложения.
  Предпосылка проверена заново: ноль пустых значений во всех словарях.
- **N-8 (`background_color`).** Снят замером: все шесть манифестов совпадают с
  настоящим фоном страницы. Два разных значения верны.

### Остаётся

- **N-3: перенос ПДФ-стека Школы в общий слой.** Единственная незакрытая
  находка отчёта. Пять трактов выдачи бумаг, отдельный выпуск. Проверка готова:
  убрать семь строк из `DEBT`.

## 2026-08-29 (пятый заход) — исправление собственной ошибки в реестре точек

**Версии не менялись:** правок в коде приложения нет — только проверка и
документация.

**Пары `767/768`, `1100/1101`, `1200/1201` НЕ являются дефектом.** Накануне
они были записаны как «полоса в один пиксель, где раскладка не определена», и
на этом основании назначены к правке. Замер в Chromium опроверг: на каждой
ширине, целой и дробной, срабатывает ровно одна ветвь — на 767.5px браузер
отдаёт `min-width: 768px`. Это стандартный способ записи взаимоисключающих
диапазонов, и три такие лестницы — единственные правильно написанные места во
всём проекте. Заход правок в пяти модулях отменён, записи в AGENTS.md,
CHANGELOG и реестре исправлены.

**`check-breakpoints.mjs` переписан с перечня подозрительных пар на расчёт.**
Теперь ищет две настоящие поломки лестницы: перекрытие (`max-width: N` и
`min-width: N` — побеждает порядок правил в файле) и разрыв (`max-width: N` и
`min-width: N+2` — ширины между ними не покрыты ничем). Прогнано на обоих
сломанных входах. В проекте нет ни одного из них.

**`lang` в манифесте закрыт как ограничение платформы** по решению Алекса:
без сервера, читающего `Accept-Language`, поле не может быть многоязычным, а
хостинг — GitHub Pages. К пункту не возвращаться.

## 2026-08-29 (четвёртый заход) — мост локализации и реестр брейкпоинтов

Хаб 0.40.11 → **0.40.12**, Конгрессы 4.46.14 → **4.46.15**, Клиндарий 9.91.13 →
**9.91.14**, Школа 1.13.21 → **1.13.22**, Назначения 5.5.31 → **5.5.32**,
Документы 1.5.15 → **1.5.16**.

Десятый заход по `docs/audit/01-full-audit.md`: **M-7** и **M-9** закрыты.
**Отчёт закрыт целиком**, кроме пунктов, отмеченных как рассмотренные и
отклонённые (L-1) и как ограничение платформы (L-4, `lang` в манифесте).

**Исправлен настоящий дефект: у Документов не работал переключатель языка.**
Модуль звал `CWI18n.init({ selectEl })` — опции с таким именем нет и не было,
аргумент молча игнорировался, и `<select id="uiLanguage">` никогда не
заполнялся. Воспроизведено на релизной сборке: ноль опций. Заодно модуль
впервые получил собственный язык — `init()` звался без `module`.

**`CWI18n.bindModule()` — мост локализации в одном месте.** Четыре модуля
вместо четырёх копий одного кода. `App.i18nBridge` Клиндария намеренно
остаётся своим: он зеркалит язык в настройки модуля и живёт без `data-i18n`.

**`scripts/check-i18n-bridge.mjs` — двадцатая проверка.** Признак самодельного
моста — опция «наследовать от хаба», а не построение `<select>` из
`CWI18n.LANGS`: первая версия ловила второе и краснела на законных селекторах
языка документов и хаба.

**`docs/design-tokens/03-breakpoints.md` — реестр контрольных точек:** 600,
900, 1200, 1600. В коде оказался **21** разное значение, а не семь.
`scripts/check-breakpoints.mjs` — двадцать первая проверка, устроена
храповиком: наследие живёт, краснеет только новое значение. Отдельно называет
пары, различающиеся на пиксель — `767/768`, `1100/1101`, `1200/1201`.

**Живой прогон переключателя во всех четырёх модулях:** селектор заполнен,
показывает наследование, переключает язык, надписи меняются, язык хаба при
этом не трогается, возврат к «как в хабе» работает.

## 2026-08-29 (третий заход) — ПДФ-стек Клиндария грузится по требованию

Хаб 0.40.10 → **0.40.11**, Конгрессы 4.46.13 → **4.46.14**, Клиндарий 9.91.12 →
**9.91.13**, Школа 1.13.20 → **1.13.21**, Назначения 5.5.30 → **5.5.31**,
Документы 1.5.14 → **1.5.15**.

Девятый заход по `docs/audit/01-full-audit.md`: **M-4** закрыт. Из блока P2
остались M-7 и M-9.

**969 КБ JS на холодный старт вместо ~4,5 МБ** — измерено живым прогоном.
Десять файлов ПДФ-стека (~3,4 МБ) убраны из стартовой разметки и догружаются
через `App.pdf.ensure()`. Кэш снимал загрузку по сети, но не снимал разбор и
компиляцию — эту цену телефон платил при каждом открытии модуля.

Файлы остались в прекэше service worker'а: офлайн не пострадал.

**Сборщики PDF не тронуты** — ожидание стека живёт в восьми вызывающих.
Делать асинхронными сами сборщики значило бы переписать четыре тракта разом.

**`scripts/check-lazy-pdf.mjs` — девятнадцатая проверка.** Догружаемый из JS
файл невидим для проверки прекэша, которая разбирает разметку; выпав из
прекэша, он умрёт при первой выдаче PDF без сети — и незаметно для того, кто
правил. Проверка держит три условия сразу: в прекэше, не в разметке, вызов
после подготовки стека.

Новый ключ `cp.pdf_preparing` на пяти языках: полоска показывается только
когда действительно есть что грузить.

**Проверено настоящей бумагой:** после догрузки собирается PDF со встроенным
Aptos, оба других набора (S-302 и расписание визита) поднимаются отдельно,
повторная подготовка ничего не грузит заново.

## 2026-08-29 (второй заход) — библиотеки PDF переехали в репозиторий

Хаб 0.40.9 → **0.40.10**, Конгрессы 4.46.12 → **4.46.13**, Клиндарий 9.91.11 →
**9.91.12**, Школа 1.13.19 → **1.13.20**, Назначения 5.5.29 → **5.5.30**,
Документы 1.5.13 → **1.5.14**.

Восьмой заход по `docs/audit/01-full-audit.md`: **M-3** закрыт.

**`shared/vendor/` — семь файлов, 3,8 МБ. CDN из проекта ушёл целиком.**
Раньше одни и те же версии приходили двумя путями: Клиндарий тянул pdf-lib и
fontkit с jsdelivr, у Школы лежали их локальные копии. Две поставки одной
версии, два кэша у пользователя с обоими модулями, два места правки.

**Побочный выигрыш крупнее самого дедупа.** Прекэш Школы тянул CDN «мягко»,
поэтому на первом запуске без сети выгрузка PDF и разбор импортированных PDF
не работали вовсе. Теперь работают — это проверено живым сценарием с
отключением сети после установки SW.

Байты не подменены: `shared/vendor` собран из npm-пакетов тех же версий, а их
совпадение с отдачей cdnjs и jsdelivr было независимо подтверждено накануне
при снятии хешей SRI.

**`check-sri.mjs` остался в гейте.** Ноль внешних скриптов — лучшее состояние,
а не поломка разбора; проверка сторожит возврат: новый внешний тег обязан
прийти с хешем и попасть в замок. `sri-lock.json` и `build-sri.mjs` сохранены.

**Проверено настоящими документами.** PDF генерируется и текст извлекается
обратно через pdf.js — в Клиндарии, в Школе и в формуляре. Плюс полный набор
живых прогонов: страницы, установка SW, восстановление, экранирование,
таблица регистраций.

**Гейт поймал два следствия переезда:** три файла не попали в прекэш Школы, а
`check-sri.mjs` требовала хотя бы один внешний скрипт.

## 2026-08-29 — `id` в манифестах, общий компонент таблиц ожил

Хаб 0.40.8 → **0.40.9**, Конгрессы 4.46.11 → **4.46.12**, Клиндарий 9.91.10 →
**9.91.11**, Школа 1.13.18 → **1.13.19**, Назначения 5.5.28 → **5.5.29**,
Документы 1.5.12 → **1.5.13**.

Седьмой заход по `docs/audit/01-full-audit.md`: **L-3**, **M-5**, **M-6**.

**`id` проставлен во всех шести манифестах.** Адрес развёртывания получен:
`https://tymo1985-debug.github.io/Circuit-Workspace/`. `id` указывает на тот
же ресурс, что и `start_url`, включая запрос — у Клиндария это
`/Circuit-Workspace/circuit-planner/?source=pwa`; обрезав `?source=pwa`, мы бы
сменили личность установленного приложения и осиротили его.
`check-manifests.mjs` усилен: теперь сверяет `id` с `start_url` и с базой
пути, а не только наличие ведущего `/`. Три сломанных входа прогнаны.

**Раздел 5.6 `shared/style.css` перестал быть спящим.** Полная реализация
таблицы-в-карточки существовала без единого использования в модулях. Теперь ею
пользуется Школа: таблица регистраций получила обёртку и режим карточек (шесть
колонок с контактами и транспортом раньше просто распирали телефон — это же
закрывает M-6), таблица учащихся — обёртку вместо инлайнового `overflow-x`.

**Конгрессы на компонент НЕ переведены — вопреки первоначальному плану.**
Разбор M-5 предполагал вариант А: перевести оба модуля и удалить локальную
копию Конгрессов. Код показал обратное: их мобильная вёрстка не копия, а
сознательная замена — подписи `data-label` там уже были и провалились, одно
задание занимало ~450px вместо ~90. Перевод вернул бы ровно ту вёрстку, от
которой они ушли. Обоснование записано в раздел 5.6, чтобы решение не
пересматривали заново.

**Живой прогон таблицы с настоящими данными:** 430px и 1440px, обе темы —
режим карточек включается только на узком, подпись реально выводится, страница
не уезжает вбок.

