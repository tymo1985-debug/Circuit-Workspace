# AGENTS.md — Circuit Workspace

Читается перед каждой задачей, поэтому короткий: **правила, а не история**.
Журнал разборов — в `docs/journal/`, по таблице в конце файла.

Разрезан 30.08.2026 (фаза 2, `docs/context/01-ai-context-audit.md`) с 413 КБ.
Ни один раздел не удалён: 52 перенесены дословно, 5 сокращены здесь и лежат
целиком в `docs/journal/misc.md`.

---

## Что это

Circuit Workspace — PWA-хаб, объединяющий инструменты для организации районного
служения под одним интерфейсом и общим слоем данных. Стартовый экран
(`index.html`) показывает плитки модулей; клик открывает модуль внутри того же
приложения.

- **Документы** (`documents/`) — шаблоны писем; своих данных нет, правит `CWDB.templates`
- **Конгрессы** (`congress-project/`) — программы, письма участникам; 14 ES-модулей
- **Клиндарий** (`circuit-planner/`) — календарь, S-302, письма; монолитный `app.js` (IIFE)
- **Школа пионеров** (`pioneer-school/`) — студенты, расписание, формуляры, экспорт
- **Назначения** (`appointments/`) — письмо о назначении; `window.print()`, PDF не собирается

«Формуляр посещения» **сознательно исключён** — поглощён Клиндарием.
Полный текст — `docs/journal/misc.md`.

## Стек

Vanilla HTML/CSS/JS, без фреймворков и сборки. IndexedDB — целевой общий слой
(`shared/db.js`), localStorage — легаси внутри модулей. GitHub Pages, офлайн
через Service Workers. PDF — jsPDF + jspdf-autotable, интерактивные формы —
pdf-lib. Языки RU/UK/EN/PL/DE.

## ⚠️ Единственный источник правды

Монорепо: **`https://github.com/tymo1985-debug/Circuit-Workspace.git`**

Отдельные репозитории модулей (`Assembly-Programm-`, `Circuit-Planner`,
`Pioneer-School-Workspace`) могут быть устаревшими. Годятся **только** как
источник новой логики, никогда как основа итоговых файлов: иначе риск откатить
хабовые интеграции (`nav.js`, MD3-стили, `version.js`) старой версией.

Перед подготовкой файлов — свежий `git clone --depth 1`; при длинной сессии
реклон перед самой сборкой архива.

Сверка модуля с хабом — **не** шаг любой задачи: только когда правка бралась
из отдельного репо модуля. Живёт в `docs/rules/delivery.md`. При обычной
локальной правке хаб не читается — это +156 КБ контекста даром.

## Структура репозитория

```
index.html  manifest.json  service-worker.js  icon-*.png  .aiignore
shared/     style.css db.js nav.js update.js i18n.js i18n/common.js
            version.js brand/ vendor/
<модули>/   congress-project circuit-planner pioneer-school appointments documents
scripts/    гейт check-*.mjs, живой прогон live-*.mjs
docs/       journal/ rules/ context/ audit/ design-system/ db-migration/ print/
```

`shared/version.js` — `CW_VERSION` + `CW_MODULES`. `shared/db.js` — IndexedDB
(communities, people, meetings, roles). `shared/nav.js` — возврат в хаб.
Правки одного модуля — только его файлы, не весь проект.

## Версии

`shared/version.js` — единственное место объявления: `CW_VERSION` (хаб) и
`CW_MODULES` (модули). В разметке номеров версий нет; версия модуля видна в
топбаре. Цепочка подъёма, исключения Клиндария, патч-бамп при правке
`shared/*` — **`docs/rules/release.md`**, читать только при подъёме версии.

## Статус миграции на общий слой

Не путать «спроектировано» и «внедрено». Проверять кодом: записи устаревают
тише кода.

- `shared/db.js` **используется** — `CWDB` есть в Клиндарии, Конгрессах и
  Документах (исправлено 30.08.2026, прежний текст утверждал обратное).
- MD3-слой и локализация (5 языков) **внедрены везде**; немецкий Клиндария
  включён 28.08.2026 (`circuit-planner/app.js:5249`).
- PIN Клиндария (`localStorage['syp-pin-hash']`) — отдельный слой модуля,
  решение о слиянии с входом хаба **не принято**.

Подробности — `docs/journal/`, полный прежний текст — `misc.md`.

---

## 🎯 AI context budget

**Use the smallest sufficient context.** Цель — не минимум токенов любой ценой,
а **минимальный достаточный контекст без снижения надёжности результата**.
Близнец на выходе: **minimal context / minimal patch / changed files only** —
минимизируется и читаемое на входе, и изменяемое на выходе.
**Local task → local context → local patch.**

Введено 30.08.2026: мелкая правка обходилась в ~186 000 токенов служебного
чтения до первой строки кода.

1. Сначала определить минимальный набор файлов, нужный задаче. Список
   расширяется по факту находки, а не «на всякий случай».
2. Не читать файл целиком, если хватает функции, секции или диапазона строк.
3. Не открывать без прямой необходимости: `CHANGELOG.md`, `docs/journal/*`,
   архивы Done, generated-файлы, шрифты/base64/binary/data-only, крупные файлы
   других модулей.
4. Не перечитывать уже изученные файлы без причины.
5. Для локальной задачи сначала ограничиваться текущим модулем.
6. `shared/*`, хаб и другие модули — только при реальной зависимости (семь
   признаков ниже).
7. Перед расширением области спросить себя: **«нужен ли этот файл для принятия
   решения или проверки изменения?»** Нет — не читать.
8. Задача решается локальным patch — общий аудит Workspace не проводить.
9. Не включать High/extended reasoning для очевидных механических действий.
10. После задачи показывать `Read:` и `Changed:`.
11. Прочитано более **8 исходных файлов** или затронуто более **2 модулей** —
    кратко объяснить, почему это было необходимо.
12. Контекст расширяется неоправданно — остановить и вернуться к минимальному
    достаточному набору.

### Семь признаков выхода за пределы модуля

Общая архитектура · общий слой (`shared/*`) · интеграция между модулями ·
версионирование · release · service worker/cache · общая схема данных.

Вне этих семи хаб, `shared/*` и соседние модули не читаются. **Локализация,
CSS и печать в список не входят** — правка словаря или стилей остаётся внутри
модуля: подгружается профильный журнал, но не чужой код.

### Отчёт перед каждой выдачей

```
Read:    какие файлы пришлось открыть
Changed: какие изменены
Added:   какие созданы
Delete:  какие удалить (или «нет»)
```

Молчаливое расширение области хуже самого расширения: оно не оставляет следа
для исправления. Пришлось читать много — объяснить в том же ответе и
предложить правку правил.

Размер этого файла стережёт `scripts/check-context-budget.mjs`: предупреждение
после 16 КБ **символов**, провал после 20 КБ символов. Решение принимается по
символам (прокси стоимости контекста); размер в байтах печатается рядом
справочно и ни на что не влияет — кириллица в UTF-8 весит 2 байта на символ.

## ❌ Что не читать

Эти файлы — данные, а не код. Читать в них нечего, а стоят они десятки тысяч
токенов: **65 % байт репозитория (7,5 МБ из 11,6)** приходится на них.
Открывать, только если задача прямо про сам файл.

- `shared/vendor/**` — минифицированные библиотеки, 3,8 МБ. Хеши — `sri-lock.json`.
- `**/fonts/**.js` — base64-шрифты (Клиндарий 4 × ~290 КБ, Школа 2 файла).
  Пересобираются `scripts/build-pdf-font-subset.mjs`.
- `circuit-planner/forms/s302-form.js` — 730 КБ, две строки, base64-PDF бланка
  S-302. Дороже, чем весь этот файл целиком.
- `**/icons/*.png`, `icon-*.png`, `apple-touch-icon.png` — растр, 9 наборов:
  изображение занимает контекст целиком, а прочитать из него нечего.
- `*.min.js`, `*.min.css` — сборочный вывод.

**Машинный признак:** строка длиннее 5000 символов, или `window.*_B64 = '`, или
путь в `vendor/`/`fonts/`. Тогда: наличие — `ls`, размер — `wc -c`, строка —
`grep -n` с узким шаблоном. **Правится генератор, а не его вывод.**

**Крупные, но осмысленные файлы — по фрагментам:** `circuit-planner/app.js`
(5466 строк) по диапазону; `circuit-planner/i18n/dict.js` (188 КБ) и
`pioneer-school/i18n/dict.js` (141 КБ) только `grep` по ключу;
`circuit-planner/index.html` (104 КБ, из них 37,6 КБ inline `<style>`) и
`shared/style.css` (74 КБ) — по секции.

Список продублирован в `.aiignore`. `.gitignore` при этом НЕ заводится: все
перечисленные файлы обязаны лежать в репозитории — без них нет офлайна.

## Рабочий процесс с файлами (правило Алекса, обязательное)

1. Claude называет конкретные файлы, нужные для задачи; Алекс присылает их
   или ссылку на коммит.
2. Возвращаются **только** изменённые/новые файлы: один — отдельно, несколько
   — ZIP только с ними, пути относительно корня монорепо. К каждому файлу
   указывать путь назначения: папки в GitHub руками не создаются, путь
   вписывается в имя файла при загрузке.
3. Изменения подтверждаются **до** выгрузки архива. Полный ре-зип — только по
   явному запросу. Автономное исполнение всех фаз — только по явной просьбе.
4. Удалений в архиве не бывает: строка `DELETE: path/to/file` в тексте ответа,
   Алекс удаляет вручную.
5. **Пост-проверка после каждого залива обязательна** — drag&drop папки может
   удалить файлы, которых в ней нет (инцидент 06.08.2026, 8 файлов).

Полный протокол сборки и рецепт пост-проверки — **`docs/rules/delivery.md`**.

## Проверки перед выпуском

Точка входа — `node scripts/check-all.mjs`, а не отдельные скрипты: набор
растёт, и запуск по памяти рано или поздно пропустит нужную проверку.

Коды: `0` — прошло; `1` — провал, выпускать нельзя; `2` — провалов нет, но
часть проверок не выполнена. Двойка намеренно не равна успеху: «не смогли
проверить» ≠ «проверили, всё хорошо».

Зависимости: `npm i jsdom jspdf acorn acorn-walk fake-indexeddb`.
Клон `--depth 1` даёт пропуск проверки патч-бампа — ожидаемо.

Новая проверка заводится по правилу «этот класс багов уже ломался повторно».

**Живой прогон обязателен перед выдачей файлов** (Chromium, `live-run.mjs`,
скриншот 430×900). Правка не касается кода — прогон не нужен, но сказать об
этом прямо, не выдавая статическую проверку за полную.

Обоснование каждой проверки и наработанные приёмы — **`docs/journal/gate.md`**.

## Открытые задачи

Живой список — **`TODO.md`** (разделы Inbox / Next / In Progress / Done
фиксированы). Здесь только то, что не влезает в формат задачи:

- **N-3** — `App.pdf` в общий слой (`shared/pdfstack.js`) + подключение Школы.
  Проверка готова, остаётся снять записи из `DEBT` в `check-lazy-pdf.mjs`.
- Мёртвые правила общего слоя: 39 в храповике `check-orphan-classes.mjs`,
  76 только в каталоге дизайн-системы. Разбирать по мере касания.
- PIN Клиндария в контексте единого входа хаба — решение не принято.
- Анкета пионера описана дважды: `registrationSchema.js` и `register.html`.
- Фазы 3–4 аудита контекста: split `circuit-planner/app.js`, валидация.

Полная история с закрытыми пунктами — `docs/journal/misc.md`.

## Стиль общения и рабочий режим Алекса

- Пишет кратко, императивами («Давай», «Weiter»); ожидает, что Claude сам
  спланирует, предупредит о рисках и исполнит.
- **Мокап до кода отменён 15.08.2026.** Взамен — живой прогон до выдачи:
  скриншоты, для печати реальный PDF через `page.pdf()` + `emulateMedia('print')`.
- **Ведение многофазной работы — на Claude.** Алекс не может следить за ходом
  непрерывно. Claude сам доводит фазы до конца, называет закрытую и следующую,
  говорит «фаза закончена» / «работа готова», не дожидаясь вопроса. Отдельно
  проговаривать разницу между «код готов» и «залито в репозиторий».
- Найденные несоответствия закреплять здесь как правило, а не проговаривать
  в чате.

Полный текст — `docs/journal/misc.md`.

---

## Указатель архива

Подгружать **один** файл по теме задачи, а не читать подряд. Пофамильный
список всех 52 разделов — `docs/journal/README.md`.

| Файл | Когда открывать |
|---|---|
| `docs/journal/db.md` | IndexedDB, `shared/db.js`, бэкап, восстановление, потолок версии базы |
| `docs/journal/i18n.md` | словари, `data-i18n`, заливки переводов, мост локализации |
| `docs/journal/design-system.md` | токены, палитра, акцент, главный экран, контрольные точки |
| `docs/journal/documents.md` | модуль Документы, общий справочник собраний, архив документов |
| `docs/journal/pdf-print.md` | `shared/print.js`, ленивая догрузка ПДФ, печатные бланки, замеры |
| `docs/journal/pwa-sw.md` | service worker, кэш, манифесты, `integrity`, обновление |
| `docs/journal/gate.md` | обоснование проверок, живой прогон, наработанные приёмы |
| `docs/journal/traps.md` | известные технические ловушки (jsPDF, кэш, RTE, иконки) |
| `docs/journal/security.md` | экранирование, подстановка в html-шаблоны |
| `docs/journal/misc.md` | закрытые мелочи, инцидент 06.08, полные тексты сокращённых разделов |

Правила по уровням: `docs/rules/release.md` (release / version bump only),
`docs/rules/delivery.md` (ZIP / commit only), `<модуль>/AGENTS.md`
(module-specific), `shared/AGENTS.md` (shared-layer only).

---
*Ядро держать в пределах 16 КБ. Новая история идёт в `docs/journal/`, а не
сюда: этот файл читается перед каждой задачей.*
