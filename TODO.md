# TODO

Задачи проекта. Разделы фиксированные: Inbox → Next → In Progress → Done.
Идеи без обязательств живут в `IDEAS.md`, крупные вехи — в `ROADMAP.md`.

## Inbox

Новые задачи, которые ещё не разобраны.

- Вычитка терминологии носителями языка в uk/en/pl/de во всех модулях.
- Отдельные `apple-touch-icon` 180×180 для модулей (сейчас у Конгрессов его нет
  вовсе, у Клиндария и Школы в этой роли подключён `icon-192.png`).
- Корневой `manifest.json` — единственный без `maskable`-иконки и без
  `description`/`scope`/`id`. Нужен отдельный файл `icon-maskable-512.png`
  (у всех модулей такой есть, у хаба нет).
- Порядок подключения CSS расходится между модулями: `appointments` грузит
  `../shared/style.css` перед своим, Конгрессы и Школа — после. Разный
  победитель каскада; переворачивать задним числом рискованно, нужен
  визуальный прогон.
- Хардкод цветов в `congress-project/styles.css` (`.error-box`, `.issue.warn`,
  `.issue.err`, `.checks label`) — не следуют тёмной теме.
- Несколько строк `uk` совпадают с `ru` (16 в Клиндарии, 22 в Конгрессах,
  16 в Школе) — часть может быть корректной омонимией, часть недопереведена;
  нужен просмотр носителем.

---

## Next

- **Фаза 7 — уборка.** Все четыре модуля мигрированы, можно убирать леса:
  осиротевшие локальные классы, выравнивание порядка подключения CSS.
  `.cw-home-fab` НЕ удалять — запасной слот для будущих модулей.
  Старые селекторы в `shared/nav.js` можно сократить до `.md-topbar-v2__lead`,
  но только после проверки всех четырёх модулей в браузере.

- **Пересобрать субсет иконок с лигатурами** (`pyftsubset --layout-features='liga'`).
  Сейчас `.md-icon` не работает, иконки везде инлайновым SVG. Требует доступа
  к полному Material Symbols — в рабочей среде сеть до fonts.gstatic.com закрыта.

- **Разминификация `congress-project/styles.css`** (32 строки минифицированного
  CSS). После неё — перевод `button,.btn` → `.md-btn` и `.program-table`
  → `.md-table`. Сейчас оба уже на токенах `--md-*`, поэтому задача про
  устранение дублирования, а не про внешний вид.

Ближайшие задачи.

- **Реальные переводы шаблонов писем Конгрессов для `ru` и `de`** — сейчас это
  байт-в-байт копии украинского. Механизм выбора языка уже общий
  (`shared/doclang.js`), не хватает самих текстов.
- **Подключить Школу пионеров к `shared/doclang.js`.** Её документы (анкета,
  S-253) существуют на одном языке. Осторожно:
  перевод в PDF завязан на сабсет шрифта (`_sanitizeForFont`) — ł, ż, ä, ß нужно
  проверить, иначе строка молча обрежется.
- **Вычитать немецкий и русский текст письма «Назначений» носителем.** Формулы
  вежливости и терминология переведены не носителем.
- **Немецкий интерфейс Клиндария** (~500 ключей). Пока язык хаба `de`
  отображается на ближайший доступный `en`.

---

## In Progress

То, что сейчас реализуется.

*(пусто)*

---

## Done

Выполненные задачи.

- **Дизайн-система, фаза 6 — Клиндарий** — 08.08.2026. Добавлена общая шапка
  над оболочкой, бренд перенесён из сайдбара. Старая `.topbar` намеренно
  сохранена как заголовок экрана: от неё зависит `measureTopbarHeight()`
  и липкая панель календаря. Градиент сайдбара с пользовательским акцентом
  не тронут. Убран последний хардкод `.sy-period-bar`. 25/25 в jsdom, печать
  проверена первой. Версии: хаб 0.13.0, Клиндарий 9.64.0.
  **Все четыре модуля приведены к единому виду.**

- **Дизайн-система, фаза 5 — Конгрессы** — 08.08.2026. Шапка на общий
  компонент, акцент модуля, сайдбар на общий токен высоты. Убран хардкод
  `.error-box`/`.issue.warn`/`.issue.err`/`.checks label`. Найден и исправлен
  скрытый баг: селект языка ссылался на неопределённые `--card`/`--accent`
  и всегда оставался белым. Печать проверена: бумага (`.letter-page`,
  `.planPrint`, `.planTable`, `dialog[open]`) не тронута. 24/24 в jsdom.
  Версии: хаб 0.12.0, Конгрессы 4.22.0.

- **Дизайн-система, фаза 4 — Школа пионеров** — 08.08.2026. Добавлена шапка
  модуля, бренд и выбор языка перенесены из сайдбара; сайдбар переведён на
  токены и получил адаптив (<900px становится блоком над содержимым);
  вычищен весь хардкод прежней палитры; начертания 600/700 → 500; кнопки и
  таблицы данных → общие компоненты. Побочно найден тот же баг печати, что
  в Назначениях: новая шапка не скрывалась, а правило `.sidebar-lang` ссылалось
  на удалённый блок. 17/17 сценариев в jsdom. Версии: хаб 0.11.0, Школа 1.8.0.

- **Дизайн-система, фазы 1–3 + пилот на Назначениях** — 07.08.2026.
  Слой компонентов перенесён в `shared/style.css` (разделы 4–6, +700 строк,
  разделы 1–3 не тронуты); `shared/nav.js` научен новому слоту с сохранением
  старых; модуль Назначения полностью переведён на общие компоненты.
  Побочно найден и исправлен реальный баг печати: после переименования
  классов правило `@media print` скрывало несуществующие `.topbar`/`.panel`.
  19/19 сценариев в jsdom. Версии: хаб 0.10.0, Назначения 5.3.0.

- **Дизайн-система, фаза 0 — общая настройка темы** — 07.08.2026. `shared/theme.js`
  (`cw-theme`: auto|light|dark, по умолчанию системная); жёсткий `data-theme` снят
  со всех 6 страниц; переключатель в шапке хаба; Клиндарий переведён на общий
  механизм с переносом прежнего выбора через `adopt()`; светлые значения `--cw-*`
  добавлены, чтобы хаб следовал теме. 20/20 сценариев в jsdom пройдены.
  Версии: хаб 0.9.0, Конгрессы 4.21.0, Клиндарий 9.61.0, Школа 1.7.0, Назначения 5.2.0.

- **Дизайн-проект единой системы** — 06.08.2026, база `ef495d8`. Аудит всех
  четырёх модулей и хаба, общая дизайн-система, универсальная оболочка модуля,
  концепт каждого модуля, 8 живых HTML-макетов, черновик слоя `cw-ui.css`,
  план внедрения. Всё в `docs/design-system/`, код приложения не изменён.
  Ограничение: только статическая верификация, макеты в браузере не проверялись.

- **Статический аудит репозитория** — 06.08.2026, хаб v0.8.2, Клиндарий v9.60.1.
  Найдены и устранены четыре коллизии ключей локализации в Клиндарии
  (`cp.letter_subject`, `cp.letter_short`, `cp.pin_off`, `cp.sync_hint`),
  подпись кнопки возврата в `shared/nav.js` переведена на общий слой,
  устранены расхождения `ARCHITECTURE.md`/`AGENTS.md`/`README.md` с кодом.
  Синтаксис, пути, манифесты, реестр резервного копирования и версии —
  без замечаний.
- **Резервное копирование хаба и модулей** — 06.08.2026, хаб v0.8.0. Создан
  `shared/backup.js` с декларативным реестром модулей; на главной — полная
  копия и таблица дат, в модулях — копия только своего модуля (встроена в
  `shared/nav.js`). Восстановление только целиком, с автокопией перед заменой.
- **Клиндарий переведён на общие слои** — 05.08.2026, v9.60.0. Данные
  отправителя переехали в `shared/sender.js` (разовый перенос через
  `CWSender.adopt()`, свои копии удалены), язык документа — в
  `shared/doclang.js` с засевом прежнего поведения. Формуляр визита больше не
  следует за языком интерфейса.
- **`appointments` добавлен в `CW_MODULES`** — 05.08.2026. Без записи в реестре
  плитка модуля показывалась без версии, а чек-лист выпуска для него не работал.
- **Отправитель вынесен в общий слой** — 05.08.2026. Создан `shared/sender.js`
  (ключ `cw-sender`); Конгрессы, Клиндарий и Назначения переведены на него,
  прежние копии переносятся при первой загрузке и удаляются. Чтение чужого
  хранилища из Назначений убрано — межмодульных зависимостей больше нет.
- **Язык документа отделён от языка интерфейса** — 05.08.2026. Создан
  `shared/doclang.js` (ключи `cw-doclang`, `cw-doclang:<module>`). Письмо
  «Назначений» доступно на uk/ru/de, выбор языка письма в Конгрессах переведён
  на тот же механизм.
- **Интеграция модуля «Назначения» (`appointments/`, v5.0.0)** — 05.08.2026.
  Автономный бланк Formuliar v5 приведён к стандарту хаба: сохранение состояния,
  5 языков интерфейса, MD3-оформление, кнопка возврата, свой service worker,
  плитка и запись в `CW_MODULES`. Сетка хаба перестроена на 4 колонки.
- **Документация собрана в корне монорепо** — 05.08.2026. `README.md` стал
  точкой входа с индексом; добавлены `ARCHITECTURE.md`, `TODO.md`, `IDEAS.md`,
  `ROADMAP.md`, `CHANGELOG.md`, `CLAUDE.md`.
- **Локализация, все пять фаз** — хаб и три модуля работают на одном механизме
  (`shared/i18n.js`), интерфейс переведён на ru/uk/en/pl/de.
- **Единая стратегия версионирования** через `shared/version.js`.
- **Комплект иконок** в едином стеклянном стиле для хаба и всех модулей.
- **MD3-визуальный слой** внедрён в хабе и во всех модулях, слит с фирменной
  палитрой `--cw-*`.
