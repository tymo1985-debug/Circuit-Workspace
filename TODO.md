# TODO

Задачи проекта. Разделы фиксированные: Inbox → Next → In Progress → Done.
Идеи без обязательств живут в `IDEAS.md`, крупные вехи — в `ROADMAP.md`.

## Inbox

Новые задачи, которые ещё не разобраны.

- ~~РЕШЕНИЕ: понятие служебного года — общий слой или копия в Школе?~~ —
  **решено 24.08.2026: общий слой.** `shared/serviceyear.js` создан,
  Клиндарий переведён делегированием, см. Done.

- ~~РЕШЕНИЕ: порядок работ по архивам — А → Б → В или сразу В?~~ —
  **решено 24.08.2026: А → Б, В откладывается.** Основание Алекса: ни одна
  школа ещё не архивировалась, и архив понадобится только когда он всерьёз
  займётся модулем. А закрыто (см. Done), следующее — Б.

- ~~В общий справочник попадают не только собрания.~~ — **закрыто 25.08.2026**
  фильтром по `visitType` в `identity()` плюс `sweepNonCongregations()`. См. Done.

- ~~Подпись «Восстановить» в окне истории Клиндария мимо i18n~~ — **снято
  25.08.2026**: строки нет ни в `renderHistoryModal`, ни рядом. Либо
  исправлено попутно, либо запись была неточной с самого начала.
- ~~Два ключа Конгрессов `cong.alert.backup_unreadable` и
  `cong.alert.backup_failed` только на `ru`~~ — **снято 25.08.2026**: оба
  переведены на все пять языков. Запись устарела.
- ~~Две подписи резервных копий Конгрессов написаны прямо в коде, мимо i18n~~
  — **закрыто полностью 25.08.2026** (Конгрессы 4.44.2). Плумбинг сделан
  шестым заходом, перевод носителя залит седьмым: 2 ключа × 4 языка =
  8 значений, 0 расхождений. Обе подписи проверены живым прогоном в реальном
  списке копий на uk/en/pl/de. Долга по этим ключам больше нет.

- ~~⚠️ ДЕЙСТВУЮЩИЙ ДЕФЕКТ: правленый пользователем текст письма Конгрессов не
  применяется~~ — **закрыто 26.08.2026** (Конгрессы 4.45.0, Клиндарий 9.88.0).
  Чинилось тремя шагами по возрастанию необратимости: достижимость отката в
  обоих модулях, затем сам перенос, затем третья подпись копии. Разбор —
  `docs/documents/02-templates-migration-audit.md`. Попутно закрыты два места
  того же класса в Клиндарии: `docPages()` подставлял системную памятку
  координатору вместо правленой пользователем, `docActualLang()` отвечал
  языком системного шаблона вместо языка реального источника.
- ~~Третья подпись копии зашита по-русски~~ — **закрыто 26.08.2026** тем же
  заходом: `cong.backup.before_move_templates`.
  **Перевод залит 26.08.2026** (Конгрессы 4.45.1): 4 значения, 0 расхождений.
  Долг по подписям резервных копий Конгрессов закрыт полностью — все три
  подписи на всех пяти языках, проверены живым прогоном в реальном списке.
  **Приём на будущее:** в таблицу носителю были вложены две уже переведённые
  подписи как образец строя, и перед заливкой отдельно сверено, что он их не
  правил (10 значений, 0 расхождений). Справочные строки в редактируемом файле
  сверять обязательно — просить их не трогать мало. Заодно к решению Алекса: подпись взята с прописной
  буквы по образцу соседних — прежний литерал был со строчной, но его не видел
  ни один пользователь, потому что ветка не исполнялась. Отменяется правкой
  одной ячейки словаря.
- **Перенос НЕ сделан повторяемым — и это по-прежнему так** (решение Алекса
  26.08.2026: вариант 3, «не повторять, но сказать»). Если перенос сорвётся,
  второй попытки в этой сессии не будет — она случится при следующем запуске.
  Превращать необратимую операцию в «пробуем, пока не выйдет» — отдельное
  решение со своим разбором, и оно не принималось.
  **Закрыта была беззвучность, а не однократность** (Конгрессы 4.46.0):
  `adoptTemplates()` отвечает `"failed"` вместо общего `false`, и `main.js`
  показывает сообщение в полосе `#errorBox`. Ветка подтверждена прогоном на
  сорванной записи в базу ДО заведения ключа.
- **Ключ `cong.alert.templates_move_failed` ждёт носителя** — заполнен только
  по-русски. Таблица собрана 28.08.2026 и отдана Алексу
  (`cong-templates-move-failed-28-08-2026.xlsx`): один ключ плюс две уже
  вычитанные строки того же модуля образцом строя. Значения образца взяты из
  словаря программно, а не набраны руками, — при сборке вручную все шесть
  ячеек оказались выдуманными.
  **Записью здесь этот долг больше не держится:** ключ, заведённый только
  по-русски, показывает `scripts/check-i18n-coverage.mjs`, §2. Заполнять
  «очевидным» переводом по-прежнему нельзя — потом не отличить от вычитанного
  носителем.

- **Приём: долг локализации проверять сканером, а не записями в этом файле.**
  25.08.2026 из пяти записей о долге три оказались устаревшими — `cp.delete_*`
  (4 ключа) и оба `cong.alert.backup_*` были переведены давно. Надёжный
  признак — русская кириллица в значениях `en`/`pl`/`de`; совпадение `uk`
  с `ru` признаком НЕ является, украинский законно совпадает в «Телефон»,
  «Тема», «Урок {n}».
- ~~Шестьдесят девять ключей ждут носителей~~ — **залиты 18.08.2026**
  (267 ячеек, 0 расхождений). ~~Ещё 62 строки из инлайновых таблиц `app.js`
  (`utils.colorName`, `VP_I18N_DICTS`)~~ — **залиты 18.08.2026 допником №2**
  (Клиндарий 9.84.0). Немецкий Клиндария закрыт целиком: интерфейс, названия
  цветов, печатный формуляр визита. С этого релиза такие таблицы стережёт
  `scripts/check-inline-lang-tables.mjs` — следующий язык не получится
  объявить готовым по одному каталогу `i18n/`.
- `cp.make_s302`: расклад не тот, что был записан (пересчитано 28.08.2026).
  Краткая форма «📋 S-302» только в uk/en/pl; немецкий — «📋 S-302 erstellen»,
  с глаголом, как и русский «📋 Сформировать S-302». То есть выбор не «русский
  против всех четырёх», а «ru + de против трёх». Решить, приводить ли к одной
  форме и к какой. Прежняя формулировка записи («носитель оставил краткую форму
  во всех четырёх языках») неверна.
- ~~`pioneer-school/sw.js` и `appointments/sw.js` читают кэш глобальным
  `caches.match()`~~ — **закрыто 28.08.2026**, см. Done. Запись была недосчитана
  вдвое: тем же глобальным чтением жили ещё `documents/sw.js` и
  `congress-project/service-worker.js` — четыре файла, а не два. Признак для
  будущих записей такого рода: искать по всему репозиторию, а не перечислять
  модули по памяти.
- Корневому `manifest.json` не хватает `scope` и `id` (maskable-иконка и
  `description` добавлены 10.08.2026). Осознанно отложено: явный `id` меняет
  идентичность установленного PWA и может привести к дублю на устройствах,
  где хаб уже установлен — нужна проверка на реальном устройстве.
- ~~Порядок подключения CSS расходится между модулями: `appointments` грузит
  `../shared/style.css` перед своим, Конгрессы и Школа — после.~~ — **запись
  устарела и указывала не на тот модуль** (проверено 28.08.2026). Все четыре
  модуля с отдельным CSS-файлом грузят общий слой ПЕРВЫМ, свой вторым;
  выровнено ещё 08.08.2026 («Три хвоста дизайн-системы»). Расхождение при этом
  существует, но оно в Клиндарии: у него ~335 строк инлайнового `<style>` в
  шапке (строки 13–348), а `../shared/style.css` подключается ПОСЛЕ них,
  строкой 349. То есть при равной специфичности у четырёх модулей побеждает
  свой стиль, а у Клиндария — общий. **Осталось решением Алекса:**
  переворачивать задним числом рискованно, нужен визуальный прогон, а поводов
  пока нет — ни один известный дефект сюда не упирается.
- ~~`.md-btn` в `shared/style.css` задаёт `height: 40px` жёстко~~ — **запись
  устарела**: `.md-btn` уже `min-height`, правка сделана раньше вместе с
  комментарием в файле. Третий подряд случай, когда запись здесь не
  соответствует коду — признак читать сканером, а не файлом.
  **Класс при этом был жив в других местах и закрыт 26.08.2026:** `.md-field`
  (поле адреса Назначений вылезало за карточку) и `.md-chip` (подпись вкладки
  в Документах обрезалась; корень — `md-chip-full` как признак выбора).
  Осталось наблюдением: `height` стоит ещё у `.md-sidenav__item` (48px),
  `.nav-item` Школы (40px), `.cw-btn` и `.cw-update-btn` хаба. Живой прогон на
  ru/de обрезки там не показал — трогать без повода не нужно, но при первой
  жалобе на длинном языке смотреть сюда.
- Совпадения `uk` с `ru` — **числа пересчитаны сканером 28.08.2026** и они
  другие: 24 в Клиндарии, 23 в Конгрессах, 19 в Школе, 17 в словаре документов
  Школы, 11 в общем, 4 в Назначениях, 1 в Документах — 99 против записанных
  здесь 54. Нужен просмотр носителем: часть законная омонимия, часть
  недопереведена, различить машинно нельзя.
  **Зато по надёжному признаку долга его нет вовсе:** русской кириллицы в
  значениях `en`/`pl`/`de` — ноль во всех семи словарях. Все совпадения там —
  марки и плейсхолдеры (`PDF`, `S-302`, `Google Maps`, `WhatsApp`, `EPUB`,
  `E-mail`, `№`, `{entity} · {lang}`). С этого числа обе величины считает
  `scripts/check-i18n-coverage.mjs`, а не запись здесь.

---

## Next

- ~~Перевести Клиндарий и Документы на `CWDocsView`.~~ — **закрыто 25.08.2026.**
  Копий рендерера в проекте не осталось. См. Done.

Ближайшие задачи.

- ~~**Цветовой слой: инверсия зависимости**~~ — **ТРЕК ЗАКРЫТ 27.08.2026**
  (хаб 0.38.0). Все фазы ниже помечены ✅ ещё тогда, а запись осталась висеть в
  «Next» — снято 28.08.2026. История фаз оставлена здесь намеренно: она и есть
  разбор, ради которого раздел писался.
  (Решения Алекса 26.08.2026, разбор —
  `docs/design-tokens/01-audit.md`). `--md-*` становятся источником правды,
  `--cw-*` — алиасами; собственные имена модулей приводятся к ролям MD3;
  остров пользовательского акцента Клиндария убирается — модуль живёт по тем
  же законам, что остальные, тон задаёт хаб. Значения выписываются числами,
  генератор палитры отклонён. Фазы:
  1. ✅ Тени светлой темы — сделано 26.08.2026.
  2. ✅ Реестр имён — сделано 26.08.2026,
     `docs/design-tokens/02-token-registry.md`. Часть оттенков оказалась
     мёртвой (`--cw-teal-dim`, `--cw-indigo`, `--cw-red`, `--blue`, `--red`),
     `--cw-teal`/`--cw-amber` складываются в существующие `--status-*`.
     Открыто одно решение: `--cw-lavender`/`--cw-violet`/`--cw-violet-dim`.
  3. ✅ Инверсия — сделано 26.08.2026. 3а (тёмная) ноль расхождений;
     3б (светлая) вариант А, тон задаёт хаб: поверхности стали фирменными,
     хаб 0, модули 200–614, тёмная 0.
  4а. ✅ Оттенки → роли — сделано 26.08.2026. Изменения только в хабе.
  4б-1. ✅ **Акцент Клиндария, всё кроме сайдбара — сделано 27.08.2026.**
     `--accent` → `--md-primary` (26 из 29 вхождений), заливка получила пару
     `--md-on-primary` вместо зашитого `color:#fff` (6 правил), `--accent-rgb`
     (20) и `--danger-rgb` (6) → `color-mix()`, оба объявления сняты,
     `--cal-*-bg` выведены из `--md-primary`. Попутно переведены 11 зашитых
     чисел, не следовавших за акцентом никогда: 8 × `rgba(20,83,45,…)`
     (зелёный от прежнего умолчания) и 3 × `rgba(138,92,246,…)`.
  4б-2. ✅ **Остров акцента снят целиком — 27.08.2026** (Клиндарий 9.91.0).
     Сайдбар по варианту C Алекса: плоский `--md-primary-container` +
     `--md-on-primary-container`, градиент снят и в `.sidebar`, и в подложке
     `body::before`. Вместе с фоном вычищен весь зашитый белый внутри
     сайдбара — семь правил; `.nav-btn` красился в `#eef7f1`, зеленоватый
     белый той же эпохи, что и восемь `rgba(20,83,45,…)` шага 4б-1.
     Переключатель убран целиком: `applyAccent()`, вызов, слушатель, реестр
     элементов, `renderSettings()`, разметка семи вариантов, `.accent-preview`,
     объявления `--accent`/`--accent2`, подмена `<meta name="theme-color">`,
     9 ключей × 5 языков. `settings.accentColor` в данных оставлена — проверено
     сценарием, что установка с `accentColor: 'red'` переживает загрузку,
     `save()` и перезагрузку, а цвет не применяется.
  6. ✅ **Вымывание `--cw-*` — ЗАКРЫТО 27.08.2026** (хаб 0.38.0).
     6-1: двенадцать прямых алиасов, 72 вхождения, 23 объявления, 0 расхождений.
     6-2: `--cw-bg-raised`/`--cw-surface`/`--cw-surface-2` сведены к роли тёмной
     темы (правило Алекса по `--surface`); `--cw-violet-dim` разведён по
     потребителям — рамка наведения в `--md-primary`, заливка живого блока в
     `--md-primary-container`. Починен зашитый белый на primary в четырёх
     правилах (в тёмной теме давал 1.7:1). Осталось намеренно: нецветовые
     `--cw-*`, плюс `--cw-shadow`/`--cw-shadow-sm` и `--cw-module*` — цветные,
     но роли MD3 для них нет (случай `--font-mono` Школы).
  Проверка на каждой фазе: замер вычисленных цветов всех видимых элементов на
  семи страницах в обеих темах до и после; фазы 3–6 обязаны давать ноль —
  кроме мест, где расхождение назначено решением ЗАРАНЕЕ и описано пофамильно.
  Обязателен контроль, что сценарий различает темы, иначе ноль пустой.

- **Смена фирменного цвета — ручная процедура, раз в год-три, скрипта НЕ
  заводим** (решение Алекса 26.08.2026). Генератор палитры отклонён: сборки в
  проекте нет, а инструмент пришлось бы поддерживать и он сам стал бы
  источником ошибок. Ручная работа выполнима потому, что от фирменного цвета
  зависит НЕ вся палитра, а только семейство `primary` — около двенадцати
  значений в двух темах. Поверхности, рамки и error к нему не привязаны:
  они намеренно взяты фиолетово-подкрашенными из фирменной палитры, а не
  тем, что даёт алгоритм, и пересчёту не подлежат.

  Порядок при смене (записан заранее, чтобы не вспоминать через три года):
  1. Поменять цвет в `shared/brand/icon-source.svg`, пересобрать иконки.
  2. Пересчитать семейство `primary` от нового сид-цвета (Material Color
     Utilities онлайн, вне репозитория) — светлая и тёмная тема.
  3. Обновить комментарий в шапке `shared/style.css` В ТОМ ЖЕ ЗАХОДЕ: там
     записан сид-цвет и его происхождение.
  4. Проверить `<meta name="theme-color">` на всех страницах.
  5. Прогон: снять вычисленные цвета всех элементов в обеих темах, убедиться,
     что изменилось только семейство `primary` и ничего кроме.

- **ОТЛОЖЕНО Алексом (25.08.2026): задача В — архив прошедших школ по
  служебным годам.** Не приоритет, вернуться позднее. Состояние на момент
  откладывания: ни одна школа ещё не архивировалась, то есть данных, которые
  можно потерять, пока нет — но риск задачи высокий и по данным необратимый.
  Предпосылки уже готовы: понятие служебного года вынесено в
  `shared/serviceyear.js` (24.08.2026), хранилища `documents` и `snapshots`
  в `shared/db.js` есть, `CWDocs` работает. **Условие начала:** аудит-документ
  по образцу `docs/db-migration/02-communities-audit.md` до единой строчки
  кода — что считается «прошедшей» школой, кто владеет этим признаком, что
  происходит с выданными документами архивированной школы и обратима ли
  операция.


- ~~**`CWPrint.document()`: `setTimeout(250)` → `onload`.**~~ — **сделано
  28.08.2026**, см. Done. Сделано ровно так, как было записано: сигнал — `load`,
  фиксированное ожидание осталось страховкой (3 с). Прибавилось то, чего в
  записи не было: `load` оказался необходимым, но НЕ достаточным условием, и
  после него нужны два кадра.

- **Описания новых переменных для справочника Клиндария.** Движок подставляет
  `congregation.address`, `congregation.contactPhone/contactEmail`, `visit.type`
  и все поля `sender.*`, но в таблице справочника их нет: у них нет описания в
  словаре, а описания пишет носитель языка. Ключи `ph_*` переведены на все пять
  языков (17.08.2026) — новым нужны такие же; в допник они не попали, потому что
  ключей для них ещё нет вовсе. Строка без описания в справочнике бесполезна,
  поэтому добавлять их одновременно с текстами.
- **Печатный бланк по-прежнему разложен вручную.** После схема-рендеринга
  онлайн-формы остался последний носитель структуры анкеты, не собираемый из
  схемы: `pdfExport.downloadRegistrationBlankForm`. Названия и порядок разделов
  он уже берёт из схемы (`_sectionHeading`), но сами поля расставлены
  подобранными координатами в две колонки — генерация «как получится» испортила
  бы готовую бумагу. Делать только вместе с раскладочными подсказками в схеме
  (ширина поля, перенос ряда), не раньше.
- **Линия «другой язык» на бланке пропадает при длинных переводах.** Ряд
  чекбоксов языков заканчивается подписью «Другой:», после неё рисуется линия
  для ответа. Немецкое «Andere Sprache:» дотягивается до 553 из 555 — места
  не остаётся, и линия не рисуется совсем: поле для ответа исчезает. Сейчас
  безопасно (переводов нет), всплывёт вместе с ними. Чинится переносом ряда
  или отдельной строкой под ним.
- **Расхождение «Печатный» / «Печатный экземпляр» между слоями.** Документы
  говорят «Печатный» (`doc.ps.reg.opt.format.print`), таблица регистраций в
  админке — «Печатный экземпляр» (`ps.reg.pechatnyy_ekzemplyar` в `dict.js`).
  Это разные слои — язык документа и язык интерфейса, — поэтому при сведении
  дублей не тронуто. Решить, приводить ли к одному слову.
- ~~Включить немецкий интерфейс Клиндария~~ — **сделано 18.08.2026**
  (9.83.0 — включение, 9.84.0 — допник №2). `'de'` в `SUPPORTED`, `NEAREST`
  пуста, опции языка документа на месте, формуляр визита печатается
  по-немецки. Осталось наблюдение, а не задача: немецкий длиннее русского
  примерно на четверть, и при первой же жалобе на обрезанную подпись в шапке
  или на кнопке править точечно — живой прогон 18.08.2026 переполнений не
  показал.
- **PIN Клиндария заменить единым входом хаба** (решение Алекса 15.08.2026).
  Отдельной фазой, НЕ внутри миграции данных: смешивать два необратимых
  изменения в одном заходе нельзя. Что решить до кода: где живёт хэш
  (`shared/`, не в резервной копии — так же, как `syp-pin-hash` сейчас), что
  происходит со старым ключом при переходе, и закрывает ли вход хаба сами
  модули (их открывают по прямой ссылке, минуя хаб).

---

## In Progress

То, что сейчас реализуется.

- ~~**Миграция Конгрессов и Клиндария на `shared/db.js`.**~~ — **трек закрыт,
  все пять фаз** (аудит — `docs/db-migration/01-audit.md`; вариант **Б
  поэтапно**, решение Алекса 15.08.2026). Фаза 1 — отложенная запись; фазы 2–3 —
  блоб состояния в базе; **фаза 4 — снимки и автокопии (16.08.2026,
  `shared/snapshots.js`, `CWDB` v5)**; **фаза 5 — общий справочник собраний,
  закрыта 16.08.2026**, связывание отменено решением Алекса, шаг 4б не наступит.
  Всё — в разделах AGENTS.md «Миграция на `shared/db.js`».
  **Запись пролежала здесь устаревшей двенадцать дней** и звала следующей фазу,
  сделанную в тот же день, когда была закрыта предыдущая. Четвёртый случай
  подряд, когда TODO расходится с кодом, — читать сканером, а не файлом.
- **Судьба старого ключа `service-year-planner-v9-4-2`.** Фаза 2 его сохранила
  как дореформенный снимок — в этом обратимость фазы. Решить, когда его
  удалять (не раньше, чем переезд отработает у живого пользователя), и что
  делать с его местом в реестре `shared/backup.js`: сейчас копия модуля везёт
  и его, то есть состояние на день переезда.

---

## Done

- **Долг перевода считается сканером** (28.08.2026, `scripts/check-i18n-coverage.mjs`,
  гейт 14/14). Кода приложения правка не касается: ни один файл модулей и общего
  слоя не тронут, версии не поднимались. Проверка обходит каталоги словарей, а не
  перечисляет их, и делит находки на поломку (частичное покрытие, лишний ключ —
  провал) и долг (ключ только по-русски, кириллица в en/pl/de — отчёт, код 0).
  Первый прогон: ключ с неполным покрытием во всём репозитории ровно один
  (`cong.alert.templates_move_failed`), кириллицы в латинских языках нет вовсе,
  совпадений `uk` с `ru` — 99 против записанных в TODO 54. Прогнана на четырёх
  сломанных входах, включая словарь, переставший отдавать секцию `ru`.

- **Уборка устаревших записей TODO** (28.08.2026). Три записи расходились с
  кодом: порядок подключения CSS был выровнен ещё 08.08.2026, а настоящее
  расхождение оказалось в другом модуле (Клиндарий грузит общий слой ПОСЛЕ
  своего инлайнового `<style>`); `cp.make_s302` в немецком с глаголом, то есть
  краткая форма не «во всех четырёх языках», а в трёх; числа совпадений
  `uk`/`ru` устарели вдвое. Трек цветового слоя, закрытый 27.08.2026, перенесён
  из «Next» в Done. Шестой, седьмой и восьмой случай подряд, когда запись здесь
  не соответствует коду.

- **Кэш модуля читается только свой** (28.08.2026, хаб 0.40.2, Конгрессы 4.46.5,
  Школа 1.13.12, Назначения 5.5.22, Документы 1.5.6). Четыре service worker'а
  читали кэш глобальным `caches.match()` и могли получить общий файл из кэша
  хаба или соседа. Правка — `matchOwn()` по образцу Клиндария. Главное здесь не
  сам промах, а то, что он обесценивал патч-бамп: бамп менял имя своего кэша,
  а чтение всё равно уходило в чужой. Заведена `check-sw-cache-scope.mjs`
  (гейт 13/13) из двух слоёв — статического и поведенческого на поддельном
  Cache API; прогнана на четырёх сломанных входах, причём случай «`matchOwn`
  открывает чужой кэш» ловится ТОЛЬКО поведенческим слоем. Подробности —
  в AGENTS.md, раздел «Кэш модуля читается только свой».

- **Печать документа стартует по готовности окна** (28.08.2026,
  Клиндарий 9.91.4 + четыре модуля выше). `setTimeout(250)` заменён на `load`
  плюс два кадра, с фиксированной страховкой на 3 с. Найдено при исполнении:
  `load` — необходимое, но не достаточное условие, ждать надо отрисовки, иначе
  повторяется тот самый пустой лист Safari, против которого стояла задержка.
  Заведена `check-print-ready.mjs` (гейт), 22 сценария, прогнана на пяти
  сломанных входах. Что НЕ доказано: что Safari действительно успевает —
  это проверяется бумагой после заливки.

- **Проверка необъявленных CSS-переменных в гейте** (26.08.2026,
  `scripts/check-css-vars.mjs`, гейт 9/9). Кода приложения правка не касается:
  ни один файл модулей и общего слоя не тронут, версии не поднимались.
  Считает по СТРАНИЦАМ, а не по репозиторию: переменная разрешается на живой
  странице, и объявление в соседнем модуле не помогает. JS разбирается как
  текст намеренно — стили в проекте вставляются строкой
  (`ensureCalendarViewStyles`, `plan-fit.js`, `printing.js`, `shared/update.js`),
  а `--cw-topbar-h` вообще объявляется только через `setProperty()`.
  Использование с запасным значением выведено отдельной секцией и кода
  возврата не меняет: там ничего не сломано, но реальным значением навсегда
  становится запасное — ровно случай `.ui-lang select` Конгрессов.
  Прогон на двух заведомо сломанных входах: снятое объявление `--line`
  в Документах даёт код 1, дописанный `var(--card,#fff)` в Конгрессах — код 0
  с записью в секции запасных.

- **Долг локализации закрыт, модуль переименован** (25.08.2026, хаб 0.37.0,
  Конгрессы 4.44.0, Документы 1.5.0). 26 строк связывания серии переведены
  носителем (104 значения, русский не тронут), заведены `doc.dir_hint` и новое
  название «Документы и данные» на пяти языках. Идентификатор `documents` и
  папка не менялись. Попутно выяснилось, что 4 ключа `cp.delete_*` были
  переведены давно — запись о них в долге устарела.

- **Копий рендерера карточки не осталось** (25.08.2026, хаб 0.36.9,
  Клиндарий 9.87.0, Документы 1.4.1). Обе местные копии сняты, переезд
  визуально нулевой (замерены фон, радиус и все подписи). В слой добавлено
  `opts.kindLabel` — Клиндарий называет свои документы точнее общих строк, и
  терять это ради единообразия было бы регрессом. Попутно починен хук
  копирования: звался `onToast` и передавал подпись кнопки вместо сообщения.

- **Экран просмотра общего справочника — пункт 5 закрыт** (25.08.2026,
  хаб 0.36.8, Документы 1.4.0). Третий раздел «Собрания / группы», только
  просмотр. 9 подписей перенесены в общий словарь (45 значений, 0 расхождений),
  новых строк не сочинялось. Правка и удаление отсюда — отдельное решение,
  см. IDEAS.md.

- **В общий справочник попадают только собрания** (25.08.2026, хаб 0.36.7,
  Клиндарий 9.86.0). Фильтр по `visitType` в `identity()` — признак взят из
  собственной подписи формы «Не визит (обычная запись)», а не из id, потому что
  переименованный в собрание шаблон обязан публиковаться. Уже попавшие карточки
  убирает `sweepNonCongregations()` внутри `seed()`, отвязывая только свой
  модуль. Пять сценариев прогнаны живьём, включая карточку соседа — она цела.
  Остаётся вторая половина пункта 5: экран ПРОСМОТРА общего справочника.

- **Экран архива в Школе — задача Б закрыта целиком** (25.08.2026, хаб 0.36.6,
  Школа 1.13.3). Фаза Б3. Маршрут `docsarchive`, общий `CWDocsView`, новых
  строк перевода не потребовалось. Оба модуля-писателя теперь имеют свой
  экран архива; остаётся отложенное — перевести на общий рендерер Клиндарий и
  Документы (см. Next).

- **Экран архива в Конгрессах + `shared/docsview.js`** (24.08.2026, хаб 0.36.5,
  Конгрессы 4.43.0). Задача Б, фазы 1–2. Общий рендерер карточки снимка вместо
  третьей и четвёртой копии; 21 строка архива перенесена в
  `shared/i18n/common.js` (105 значений, 0 расхождений). Попутно починены все
  18 диалогов модуля: `<dialog>` не наследует `color` от `<body>`, в тёмной
  теме текст был чёрным. Дальше — Б3: тот же экран в Школе.

- **Служебный год вынесен в `shared/serviceyear.js`** (24.08.2026, хаб 0.36.4,
  Клиндарий 9.85.0). Решение по вопросу выше: общий слой, не копия. Слой держит
  только календарную арифметику (`forDate`, `label`, `bounds`, `START_MONTH`);
  недели, визиты и школы остаются данными модуля. Клиндарий переведён
  делегированием — три функции сохранили имена, 34 места вызова и 7 мест,
  считающих месяцы от `App.config.serviceYearStartMonth`, не тронуты.
  Поведенческая эквивалентность доказана: 440 проверок дат и 101 год границ,
  0 расхождений; запасной путь без общего файла проверен живьём.
  Школа подключится к слою вместе с задачей В, когда та наступит.

- **Документы: тело модуля и рамки** (24.08.2026, хаб 0.36.3, Документы 1.3.2).
  Три дефекта, все найденные живым прогоном и ни один — чтением кода:
  у `<body>` не было правила вообще (прозрачный фон, сериф, чёрный текст),
  `--line` использовался в двенадцати правилах и нигде не объявлялся (все
  рамки модуля рисовались чёрным через `currentColor`), название шаблона
  слипалось с типом из-за `margin-top` на строчном `<span>`. Правки локальны
  для модуля, `shared/style.css` не тронут. Собственный акцент модулю НЕ
  заводился: решение «Документы держат палитру хаба» подтверждено.

- **Заливка переводов, фазы 1–4** (17.08.2026; хаб 0.36.0, Клиндарий 9.82.0,
  Школа 1.13.0, Документы 1.3.0, Конгрессы 4.42.1, Назначения 5.5.13).
  1583 ячейки из `circuit-workspace-perevod_completed_v3.xlsx` в семь словарей
  и шесть писем; осталось 263 из 1846. Немецкий словарь Клиндария лежит в
  файле, но НЕ подключён — см. Next. Побочно: снят номер раздела с переводов
  `doc.ps.reg.section.*`, удалён мёртвый `cp.event_templates`, убраны
  `KEYS`/`blank()` из `documents/i18n/dict.js`, две зашитые подписи Клиндария
  вынесены в словарь, добавлена проверка `check-i18n-placeholders.mjs`.
  Подробности — в `CHANGELOG.md` и `AGENTS.md`.


- **Миграция на `shared/db.js`, фаза 3 — Конгрессы в общей базе** (15.08.2026,
  хаб 0.33.0, Конгрессы 4.38.0, патч-бамп остальных четырёх модулей). Тем же
  путём, что фаза 2; добавлены снимок перед переносом, ожидание базы при
  запуске и немедленная запись при сбросе приложения. Живой прогон: переезд,
  правка после переезда, перезагрузка, зеркало на закрытии, диалоги, два модуля
  в одной базе, копия модуля без чужого состояния, работа без общего слоя.
- **Миграция на `shared/db.js`, фаза 2 — Клиндарий в общей базе** (15.08.2026,
  хаб 0.32.0, Клиндарий 9.75.0, патч-бамп остальных четырёх модулей).
  `shared/state.js` (`CWState`), хранилище `state` в `CWDB` v4, синхронное
  зеркало на закрытие вкладки, маячок для соседней вкладки. Старый ключ
  оставлен как дореформенный снимок. `shared/backup.js` научился адресной
  выгрузке записей. Живой прогон: переезд существующей установки, правка после
  переезда, перезагрузка, спасение правки зеркалом при недоехавшей записи,
  синхронизация двух вкладок, работа без общего слоя — все семь сценариев.
- **Миграция на `shared/db.js`, фаза 1 — отложенная запись** (15.08.2026,
  хаб 0.31.0, Конгрессы 4.37.0, Клиндарий 9.74.0). `shared/persist.js`:
  `save()` остаётся синхронной, но только ставит запись в очередь (окно 400 мс,
  потолок 2 с), а пишет планировщик. 79 вызовов `save()` не тронуты. Страховка
  на `pagehide`/`beforeunload`/`visibilitychange` — в общем слое; у Конгрессов
  её раньше не было вовсе. Живой прогон подтвердил: запись не уходит
  немедленно, доезжает через окно ожидания, при непрерывном наборе пробивается
  потолком на 2.1 с, на `pagehide` уходит сразу, при отсутствии общего файла
  модуль пишет немедленно как раньше.
- **Проверка прекэша общего слоя** (15.08.2026): `scripts/check-shared-precache.mjs`
  в `check-all.mjs`. Файл общего слоя, подключённый в разметке модуля, обязан
  лежать и в прекэше его service worker'а; отдельно проверяется пара
  `backup.js` → `version.js`.
- **Общий слой печати, фаза 2 + поля бумаги** (15.08.2026, хаб 0.30.0,
  Конгрессы 4.36.3, Клиндарий 9.73.0, Школа 1.12.3, Назначения 5.5.8).
  Печать календаря Клиндария переведена на `CWPrint.document()`:
  `buildPrintHtml()` возвращает части документа, а не готовый HTML. Поля
  бумаги теперь объявляет документ (`@page{margin:28mm 20mm}` по умолчанию,
  календарь передаёт свои `A4 landscape / 10mm`) — раньше их задавал диалог
  печати, из-за чего предпросмотр расходился с бумагой, а «поля: нет»
  прижимало текст к краю. Найдено при переносе: общий слой задаёт шрифт
  сокращённым `font:`, поэтому модуль обязан сбрасывать `line-height` —
  в календарь протекала письмовая интерлиньяж. Бумага календаря после
  переноса совпала попиксельно.

- **Общий слой печати, фаза 1** (15.08.2026, хаб 0.29.0, Конгрессы 4.36.2,
  Клиндарий 9.72.2, Школа 1.12.2, Назначения 5.5.7). `shared/print.js`:
  `CWPrint.document()` для изолированного окна и `CWPrint.filename()` для
  имени PDF при печати страницы. Композеры Клиндария и Школы переведены на
  слой — снят дубль, написанный дважды независимо. Починены два бага:
  `doPrint()` Клиндария при заблокированном окне печатал экран приложения
  вместо календаря; в Конгрессах не было защиты от двойного `beforeprint`,
  из-за чего имя вкладки могло навсегда остаться именем документа. Бумага до
  и после совпала попиксельно (сравнение хешей отрисовки в режиме печати,
  прогнано и на заведомо сломанном входе). Аудит — `docs/print/01-audit.md`.

- **Удалён мёртвый `congress-project/js/template-editor.js`** (15.08.2026,
  Алексом вручную в GitHub). Файл никем не импортировался, диалога
  `#templateDialog` в разметке не существовало — остаток фазы 3б. Там же жил
  спящий баг затенения `t()`.

- **`.sr-only` сведён в `shared/style.css`** (15.08.2026, хаб 0.28.0). Четыре
  копии правила в модулях удалены. Патч-бамп Конгрессов, Школы, Назначений и
  Документов — кэш-фёрст service worker'ы иначе не отдали бы новый общий CSS;
  Клиндарий не бампался, у него stale-while-revalidate и своего `.sr-only` не
  было. Проверено живым прогоном, в том числе с намеренно выключенным правилом:
  без него подписи «Interface language» и «Раздел модуля» видны на экране.

- **Композер письма в Конгрессах** (15.08.2026, хаб 0.27.1, Конгрессы 4.36.0).
  Выбор конгресса связки, разовая правка, снимок в архив — поверх существующего
  диалога предпросмотра. Мокап удалён вместе с выпуском.

- **Снимки документов в Конгрессах** (15.08.2026, хаб 0.27.0, Конгрессы 4.35.0).
  Подключён `shared/documents.js`, печать письма и печать всех писем пишут
  снимок, `documents` добавлен в реестр резервного копирования модуля.

- **Починен режим писем Конгрессов** (14.08.2026, хаб 0.26.3, Конгрессы 4.34.5).
  Диалог «Письма» не открывался с v4.19.0: переменная задания затеняла функцию
  перевода `t()`. Заведена проверка `check-i18n-shadow.mjs` (acorn, разбор
  областей видимости) и включена в `check-all.mjs`.

- **Схема-рендеринг `register.html`** (14.08.2026, хаб 0.26.2, Школа 1.12.0).
  Состав вопросов онлайн-анкеты больше не описан в разметке: поля собирает
  `js/modules/registrationForm.js` из `registrationSchema.js` — того же
  источника, из которого строится интерактивная PDF-анкета. Найденный при этом
  живой дубль (`doc.ps.page.notes` против `doc.ps.reg.field.notes` — один вопрос,
  два ключа с разным текстом) снят. Нумерация разделов уехала из словаря в
  схему (`heading`). Язык учебника стал «таблетками» вместо `<select>`.
  Условные поля теперь очищаются при скрытии. Проверки §5–§6 в
  `check-doclang.mjs` переписаны и прогнаны на заведомо сломанном входе.

- **Сведение дублей формулировок Школы, второй заход** (14.08.2026, хаб 0.26.1,
  Школа 1.11.1). Словарь документов 140 → 110 строк; наборы `doc.ps.field.*` и
  `doc.ps.send.*`; пунктуация уехала из словаря в генератор. Снимает
  предпосылку из «Порядок работ»: список для носителей можно отдавать.

- **Общий слой документов, фаза 6: письма Школы пионеров** (14.08.2026, хаб
  0.26.0, Школа 1.11.0, Документы 1.2.3). Шаблон
  `sys.school.student.invitation`, слой `pioneer-school/js/modules/letters.js`,
  композер с двумя входами, PDF на встроенном шрифте, снимки в архив, Школа
  подключена к резервному копированию общего слоя. **Все шесть фаз общего слоя
  документов закрыты.**

- **Сборщик PDF для одиночного документа** (14.08.2026, Клиндарий 9.72.0).
  Режим `options.plain` у `buildLetterPdfDoc`; PDF доступен всем документам
  композера. Снимает ограничение, которое иначе упёрлось бы в фазу 6.

- **Общий слой документов, фаза 5: композер и меню «Создать документ»**
  (14.08.2026, хаб 0.25.0, Клиндарий 9.71.0). Меню в карточке визита, предпросмотр
  с подставленными данными за два клика, разовая правка с пометкой в архиве,
  страницы письма отдельными пунктами.

- **Общий слой документов, фаза 4: история выданных документов** (13.08.2026,
  хаб 0.24.0, Клиндарий 9.70.0, Документы 1.2.0). Хранилище `documents`
  (`CWDB` v3), общий слой `shared/documents.js` (`CWDocs`), снимки при выгрузке
  PDF и отправке письма в Клиндарии, экран «Документы визита» под карточкой,
  раздел «Архив» в модуле «Документы». Дедупликация повторной выдачи, снимки
  переживают удаление сущности, архив входит в резервную копию модуля.

- **Общий слой документов, фаза 3б: правка ушла из модулей** (13.08.2026, хаб
  0.23.0, Конгрессы 4.34.0, Клиндарий 9.69.0, Документы 1.1.0). Редакторы
  шаблонов убраны из обоих модулей, вместо них вход в «Документы». Сборка писем
  осталась в модулях. Создание письма под тип задания перенесено в
  «Документы», чтобы возможность не пропала.

- **Общий слой документов, фаза 3а: модуль «Документы»** (13.08.2026, хаб
  0.22.0, Документы 1.0.0). Библиотека шаблонов и общий редактор: визуальный
  для HTML-документов Клиндария, текстовый для Конгрессов; языковые колонки,
  дополнительные страницы, предпросмотр с подстановкой, восстановление
  оригинала. Пятая плитка в хабе. Своих данных у модуля нет.

- **Общий слой документов, фаза 2б: Клиндарий в общем хранилище**
  (12.08.2026, хаб 0.21.0, Клиндарий 9.68.0). Письма, памятки, страницы, тела
  писем и обращения для трёх типов визита. Заодно исправлен бесшумный дефект
  фазы 2а: шаблон для типа задания сохранялся с неверным контекстом и не
  находился при печати. Настройки модуля больше не материализуют системные
  тексты.

- **Общий слой документов, фаза 2а: шаблоны Конгрессов в общем хранилище**
  (12.08.2026, хаб 0.20.0, Конгрессы 4.33.0). `CWDB` v2 с хранилищем
  `templates`, системные тексты в `shared/templates/builtin.js`, необратимый
  перенос из настроек модуля с копией перед началом и откатом на прежний
  источник до готовности базы. `ru`/`de` очищены и помечены `pending`.

- **Копия модуля перестала терять общий слой** (12.08.2026, хаб 0.19.1,
  все модули +1 патч). В реестре `shared/backup.js` появились `sharedLocal` и
  `sharedStores`; копия модуля везёт объявленные части общего слоя отдельной
  секцией с пометкой `partial`, которая при восстановлении сливается, а не
  заменяет общий слой целиком. `formatVersion` поднят до 2, чтобы прежний код
  честно отказался читать такой файл вместо тихого удаления ключей. Заодно
  закрыт блокер фазы 2: механизм умеет выгружать отдельные хранилища общей
  базы, фазе 2 останется дописать `templates` в реестр. Проверка
  `scripts/check-backup.mjs` заведена в `check-all.mjs`.

- **Общий слой документов, фаза 1: `shared/templates.js`** (12.08.2026, хаб
  0.19.0, Конгрессы 4.32.0, Клиндарий 9.67.0). Подстановка переменных стала
  общей: один движок вместо двух, один реестр переменных вместо массива `PH` и
  функции `renderPlaceholderReference()`, канонический синтаксис
  `{{namespace.field}}` с 29 бессрочными алиасами старых имён. Хранилище не
  тронуто — тексты остались в настройках модулей, это фаза 2. Приёмка: 52
  сравнения «до/после» на реальных шаблонах монорепо, расхождений нет.

- **Мобильный вид Конгрессов** (11.08.2026, 4.28.0 + 4.29.0). Фаза 1 — таблица
  программы стала списком карточек целиком на CSS (`order` по ячейкам,
  действия по `tr.selected`), `render.js` не тронут. Фаза 2 — список
  конгрессов, поля конгресса и панель действий переехали в нижние листы,
  добавление задания на плавающую кнопку, «без участника» вместо прочерка.

- **Обновление приложения перестало зависеть от Shift+Cmd+R** (11.08.2026).
  Версионирование кэша Клиндария, общий слой `shared/update.js`, полоса
  «Доступна новая версия» с выбором пользователя, кнопка проверки обновлений
  в шапке хаба, обновляющая заодно все четыре модуля.


- **Слияние трёх копий `visitLabel` в Клиндарии** — все берут
  `App.utils.visitTypeLabel()`; четвёртое место (модалка письма) оставлено
  отдельным осознанно. 10.08.2026, v9.65.3.
- **Мёртвое вычисление `#screenSubtitle` в Клиндарии** убрано; разметка и
  ключи `subtitle_*` оставлены для возможного возврата. 10.08.2026, v9.65.3.
- **`apple-touch-icon` 180×180 для четырёх модулей и maskable-иконка хаба** —
  собраны из фирменных исходников, подключены в разметке, манифесте и
  предкэше service worker'ов. 10.08.2026.

- **Общие задания серии в «Конгрессах».** Повторяющаяся программа внутри серии
  ведётся один раз: содержание и подтверждение общие, время, репетиция и письмо
  свои у каждого конгресса, статус общий до «Подтверждено» включительно.
  Режим создания «Копия с общими заданиями» — 10.08.2026, v4.25.0; связывание
  уже существующих копий кнопкой в шапке серии — 10.08.2026, v4.26.0.
Выполненные задачи.

- **Клиндарий подключает `shared/version.js`** — 10.08.2026. Заметка из
  журнала закрыта. Пустой `CW_VERSION` внутри модуля бил не по интерфейсу
  (свою версию модуль берёт из `App.config.version`), а по `shared/backup.js`:
  копии из Клиндария сохранялись без версий хаба и модуля. Правка в двух
  файлах — `index.html` (перед общим слоем локализации) и `sw.js`
  (`APP_SHELL_URLS`, ради офлайна).
- **`scripts/check-all.mjs` — единая точка прогона проверок** — 09.08.2026.
  Обёртка над `check-versions.mjs` и `check-doclang.mjs`. Отдельный статус
  ПРОПУЩЕНА для проверок, чьи npm-пакеты не установлены: «не проверили» не
  выдаётся за «прошло». Коды возврата: 0 — всё прошло, 1 — есть провал,
  2 — провалов нет, но что-то не выполнено.
- **Формулировки `doc.ps.blank.*` и `doc.ps.reg.*` сведены** — 09.08.2026.
  Двенадцать дублей удалены, бланк берёт подписи из общего набора
  `doc.ps.reg.*`, двоеточие дорисовывает генератор (`_withColon`). Победитель
  выбирался по каждой паре на замерах реального шрифта: короткая формулировка
  бланка выиграла в пяти случаях из шести, потому что на бумаге место под
  ответ конечно. Носителю теперь на 12 строк меньше на каждый язык.
- **`_label()` научился переносить длинные подписи** — 09.08.2026. Вопрос про
  автомобиль вылезал за правое поле на 16 пунктов уже по-русски (531 при 515
  доступных) и молча обрезался бумагой; в немецком и польском было бы хуже.
- **Линия «другой язык» больше не заходит под подпись** — 09.08.2026. Рисовалась
  от жёсткой координаты 475, тогда как подпись «Другой:» дотягивается до 506.
  Теперь стартует от реального правого края ряда чекбоксов.

- **`.md-shell` получил `grid-template-columns: minmax(0, 1fr)`** — 09.08.2026.
  Неявная колонка сетки имела минимум `min-content`, поэтому широкий потомок
  мог растянуть оболочку модуля шире экрана. Правка профилактическая — ни
  один модуль этот предел сейчас не пробивает. Синхронно поправлен справочный
  `docs/design-system/proposed/cw-ui.css`, чтобы правка не потерялась при
  следующем копировании из него.

- **Школа пионеров подключена к `shared/doclang.js`** — 09.08.2026, база `e3707c3`.
  Фаза 0: субсет шрифта пересобран (364 → 586 глифов) скриптом
  `scripts/build-pdf-font-subset.mjs`, whitelist `_sanitizeForFont` расширен;
  до этого «Grüße Straße Übung» доезжало до PDF как «Gr», а «Szkoła» — как
  «Szkoa». Фазы 1–2: ~190 строк документов вынесены в `i18n/doc.js`
  (`doc.ps.*`), добавлен мост `js/doclang.js` (`PSDocLang` + `D()`), переведены
  четыре генератора, `register.html` и схема анкеты; переключатель языка
  документа — в настройках анкеты, ссылка для пионера получает `?lang=`.
  Попутно исправлено: `register.html` роняла `registration.js` на
  `T is not defined`, из-за чего после отправки формы не появлялись ни баннер,
  ни кнопки отправки; экспортное значение радиокнопки в PDF было подписью, а
  не ключом; раскладка бланка ломалась на длинных подписях.
  56/56 проверок в `scripts/check-doclang.mjs` (включая сквозную сборку PDF).
  Версии: хаб 0.16.0, Школа 1.9.0.

- **Три хвоста дизайн-системы за один прогон** — 08.08.2026.
  (1) Субсет иконок пересобран с лигатурами: 18 КБ → 4.3 КБ, `.md-icon`
  работает; рецепт зафиксирован в `scripts/build-icon-subset.mjs`.
  (2) Порядок подключения CSS выровнен во всех модулях (shared → свой);
  побочно ожило мёртвое правило шапки Конгрессов.
  (3) `congress-project/styles.css` разминифицирован: 47 → 916 строк,
  сверено по 188 правилам — расхождений нет.
  32/32 регрессионных проверки. Версии: хаб 0.15.0, Конгрессы 4.24.0, Школа 1.8.3.

- **Дизайн-система, фаза 7 — уборка** — 08.08.2026. `shared/nav.js`: 5 слотов → 1.
  Удалён мёртвый CSS в трёх модулях (Школа 523 → 477 строк, Конгрессы 6 правил
  `.topbar`, Назначения `.locked`). Из печати Школы убран призрак `.org-panel`.
  31/31 регрессионных проверок по всем четырём модулям. Порядок подключения CSS
  намеренно не трогал — вынесен в отдельную задачу.
  Версии: хаб 0.14.0, Конгрессы 4.23.2, Школа 1.8.2, Назначения 5.3.2.

- **Дизайн-система, фаза 6 — Клиндарий** — 08.08.2026. Добавлена общая шапка
  над оболочкой, бренд перенесён из сайдбара. Старая `.topbar` намеренно
  сохранена как заголовок экрана: от неё зависит `measureTopbarHeight()`
  и липкая панель календаря. Градиент сайдбара с пользовательским акцентом
  не тронут. Убран последний хардкод `.sy-period-bar`. 25/25 в jsdom, печать
  проверена первой. Версии: хаб 0.13.0, Клиндарий 9.64.0.
  **Все четыре модуля приведены к единому виду.**

- **Дизайн-система, фаза 5 — Конгрессы** — 08.08.2026. Шапка на общий
  компонент, акцент модуля, сайдбар на общий токен высоты. Убран хардкод
  `.error-box`/`.issue.warn`/`.issue.err`/`.checks label`. Найден и исправлен
  скрытый баг: селект языка ссылался на неопределённые `--card`/`--accent`
  и всегда оставался белым. Печать проверена: бумага (`.letter-page`,
  `.planPrint`, `.planTable`, `dialog[open]`) не тронута. 24/24 в jsdom.
  Версии: хаб 0.12.0, Конгрессы 4.22.0.

- **Дизайн-система, фаза 4 — Школа пионеров** — 08.08.2026. Добавлена шапка
  модуля, бренд и выбор языка перенесены из сайдбара; сайдбар переведён на
  токены и получил адаптив (<900px становится блоком над содержимым);
  вычищен весь хардкод прежней палитры; начертания 600/700 → 500; кнопки и
  таблицы данных → общие компоненты. Побочно найден тот же баг печати, что
  в Назначениях: новая шапка не скрывалась, а правило `.sidebar-lang` ссылалось
  на удалённый блок. 17/17 сценариев в jsdom. Версии: хаб 0.11.0, Школа 1.8.0.

- **Дизайн-система, фазы 1–3 + пилот на Назначениях** — 07.08.2026.
  Слой компонентов перенесён в `shared/style.css` (разделы 4–6, +700 строк,
  разделы 1–3 не тронуты); `shared/nav.js` научен новому слоту с сохранением
  старых; модуль Назначения полностью переведён на общие компоненты.
  Побочно найден и исправлен реальный баг печати: после переименования
  классов правило `@media print` скрывало несуществующие `.topbar`/`.panel`.
  19/19 сценариев в jsdom. Версии: хаб 0.10.0, Назначения 5.3.0.

- **Дизайн-система, фаза 0 — общая настройка темы** — 07.08.2026. `shared/theme.js`
  (`cw-theme`: auto|light|dark, по умолчанию системная); жёсткий `data-theme` снят
  со всех 6 страниц; переключатель в шапке хаба; Клиндарий переведён на общий
  механизм с переносом прежнего выбора через `adopt()`; светлые значения `--cw-*`
  добавлены, чтобы хаб следовал теме. 20/20 сценариев в jsdom пройдены.
  Версии: хаб 0.9.0, Конгрессы 4.21.0, Клиндарий 9.61.0, Школа 1.7.0, Назначения 5.2.0.

- **Дизайн-проект единой системы** — 06.08.2026, база `ef495d8`. Аудит всех
  четырёх модулей и хаба, общая дизайн-система, универсальная оболочка модуля,
  концепт каждого модуля, 8 живых HTML-макетов, черновик слоя `cw-ui.css`,
  план внедрения. Всё в `docs/design-system/`, код приложения не изменён.
  Ограничение: только статическая верификация, макеты в браузере не проверялись.

- **Статический аудит репозитория** — 06.08.2026, хаб v0.8.2, Клиндарий v9.60.1.
  Найдены и устранены четыре коллизии ключей локализации в Клиндарии
  (`cp.letter_subject`, `cp.letter_short`, `cp.pin_off`, `cp.sync_hint`),
  подпись кнопки возврата в `shared/nav.js` переведена на общий слой,
  устранены расхождения `ARCHITECTURE.md`/`AGENTS.md`/`README.md` с кодом.
  Синтаксис, пути, манифесты, реестр резервного копирования и версии —
  без замечаний.
- **Резервное копирование хаба и модулей** — 06.08.2026, хаб v0.8.0. Создан
  `shared/backup.js` с декларативным реестром модулей; на главной — полная
  копия и таблица дат, в модулях — копия только своего модуля (встроена в
  `shared/nav.js`). Восстановление только целиком, с автокопией перед заменой.
- **Клиндарий переведён на общие слои** — 05.08.2026, v9.60.0. Данные
  отправителя переехали в `shared/sender.js` (разовый перенос через
  `CWSender.adopt()`, свои копии удалены), язык документа — в
  `shared/doclang.js` с засевом прежнего поведения. Формуляр визита больше не
  следует за языком интерфейса.
- **`appointments` добавлен в `CW_MODULES`** — 05.08.2026. Без записи в реестре
  плитка модуля показывалась без версии, а чек-лист выпуска для него не работал.
- **Отправитель вынесен в общий слой** — 05.08.2026. Создан `shared/sender.js`
  (ключ `cw-sender`); Конгрессы, Клиндарий и Назначения переведены на него,
  прежние копии переносятся при первой загрузке и удаляются. Чтение чужого
  хранилища из Назначений убрано — межмодульных зависимостей больше нет.
- **Язык документа отделён от языка интерфейса** — 05.08.2026. Создан
  `shared/doclang.js` (ключи `cw-doclang`, `cw-doclang:<module>`). Письмо
  «Назначений» доступно на uk/ru/de, выбор языка письма в Конгрессах переведён
  на тот же механизм.
- **Интеграция модуля «Назначения» (`appointments/`, v5.0.0)** — 05.08.2026.
  Автономный бланк Formuliar v5 приведён к стандарту хаба: сохранение состояния,
  5 языков интерфейса, MD3-оформление, кнопка возврата, свой service worker,
  плитка и запись в `CW_MODULES`. Сетка хаба перестроена на 4 колонки.
- **Документация собрана в корне монорепо** — 05.08.2026. `README.md` стал
  точкой входа с индексом; добавлены `ARCHITECTURE.md`, `TODO.md`, `IDEAS.md`,
  `ROADMAP.md`, `CHANGELOG.md`, `CLAUDE.md`.
- **Локализация, все пять фаз** — хаб и три модуля работают на одном механизме
  (`shared/i18n.js`), интерфейс переведён на ru/uk/en/pl/de.
- **Единая стратегия версионирования** через `shared/version.js`.
- **Комплект иконок** в едином стеклянном стиле для хаба и всех модулей.
- **MD3-визуальный слой** внедрён в хабе и во всех модулях, слит с фирменной
  палитрой `--cw-*`.
