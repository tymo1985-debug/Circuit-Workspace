# TODO

Задачи проекта. Разделы фиксированные: Inbox → Next → In Progress → Done.
Идеи без обязательств живут в `IDEAS.md`, крупные вехи — в `ROADMAP.md`.

## Inbox

Новые задачи, которые ещё не разобраны.

- **Работа по аудиту `docs/audit/01-full-audit.md`.** Порядок согласован с
  Алексом 28.08.2026, шаги идут по одному выпуску:
  1. ~~C-1, потолок версии базы при восстановлении~~ — **закрыто 28.08.2026**,
     см. CHANGELOG и раздел AGENTS.md «Потолок версии базы при восстановлении».
  2. ~~H-1 + H-2~~ — **закрыто 28.08.2026.** Три фазы восстановления, предел
     ожидания занятой базы, предохранительный снимок в отдельной базе
     `circuit-workspace-guard`, кнопка отката в хабе. `backup.too_new`
     вернулось на место `backup.failed`. Блок P0 отчёта закрыт целиком.
  3. ~~H-5, гейт `check-shared-bump.mjs`~~ — **закрыто 28.08.2026**, гейт
     вырос до 16 проверок.
     ~~H-4~~ — **закрыто 28.08.2026.** Семь тегов с `integrity` и
     `crossorigin`, воркер pdf.js в репозитории. Хеши приносит Алекс:
     `node scripts/build-sri.mjs` (сеть до CDN из рабочего окружения закрыта);
     `--check` сверяет проставленное с текущей отдачей CDN — прогонять при
     любой смене версии библиотеки.
  4. ~~H-3 + M-8~~ — **закрыто 28.08.2026.** `shared/escape.js`, семь редакций
     сведены в одну, `{ escape: true }` для html-шаблонов. Блок P1 закрыт.
     Задача «помнить про хеши SRI» снята с Алекса: `check-sri.mjs` в гейте.
  5. ~~M-1 + M-2 + L-2, L-4, L-6, L-8~~ — **закрыто 28.08.2026.** L-1
     рассмотрен и отклонён (обоснование в AGENTS.md).
     ~~L-3~~ — **закрыто 29.08.2026**, адрес развёртывания получен.
  6. ~~M-5 + M-6~~ — **закрыто 29.08.2026.** Раздел 5.6 ожил в Школе.
     Конгрессы намеренно не переведены (обоснование в разделе 5.6 и в
     AGENTS.md) — вариант А исправлен по коду.
  7. ~~M-3~~ — **закрыто 29.08.2026.** `shared/vendor/`, CDN ушёл целиком,
     первый запуск без сети теперь выдаёт PDF.
  8. ~~M-4~~ — **закрыто 29.08.2026.** 969 КБ на старт вместо ~4,5 МБ.
  9. ~~M-7 + M-9~~ — **закрыто 29.08.2026.** Отчёт закрыт целиком.

- **Работа по повторному аудиту `docs/audit/02-recheck.md`.** Отчёт от
  29.08.2026, шестнадцать находок предыдущего аудита перепроверены заново.
  Закрыто в тот же день: **N-1** (потолок версии для баз модулей — единственная
  оставшаяся необратимость), **N-2** (кнопка отката действительно откатывает,
  без круга через файл), **N-4** (шапка замка подписей и её сохранение при
  перезаписи), **N-6** (повторная подписка в `state.js` и `theme.js`), **N-7**
  (перевод в разметке экранируется, 67 мест, плюс 22-я проверка гейта), **N-8**
  (снят замером — два значения `background_color` верны), **N-9** (призрачные
  базы не создаются). **N-5** отклонён повторно (обоснование в AGENTS.md,
  раздел «Рассмотрено и отклонено: пустой перевод»).

  **ОСТАЁТСЯ ОДИН ПУНКТ — N-3, перенос ПДФ-стека Школы.** См. ниже отдельной
  задачей: это единственная незакрытая находка отчёта.

- **N-3: ПДФ-стек Школы пионеров догружается по требованию.** Школа грузит
  синхронно 7 файлов, 2913 КБ (`jspdf`, `pdf.min`, `xlsx.full`, `pdf-lib`,
  `fontkit`, два шрифтовых бандла) — они нужны только при выгрузке PDF, импорте
  PDF и экспорте Excel, но разбираются при КАЖДОМ открытии модуля. После
  переезда библиотек с CDN в репозиторий Школа стала самым тяжёлым модулем
  проекта, а пользуются ею чаще с телефона.

  Что делать: вынести `App.pdf` Клиндария в общий слой (`shared/pdfstack.js`) и
  подключить Школу. Наборы у неё свои, механизм тот же — строгий порядок внутри
  набора, снятие обещания при неудаче, тост только когда есть что грузить.

  Затрагивает пять трактов выдачи бумаг: выгрузка PDF, импорт PDF, экспорт
  Excel, интерактивная анкета, письма. Каждый требует живого прогона с
  перехватом конструктора `jsPDF` — поэтому отдельный выпуск, не в общей куче.

  **Проверка уже готова и ждёт.** `scripts/check-lazy-pdf.mjs` держит долг
  записанным в `DEBT` и печатает его при каждом прогоне гейта. После переноса
  достаточно убрать семь строк — храповик затянется сам, и с этого момента
  любой новый тяжёлый файл в стартовой разметке любого модуля будет краснить
  гейт.

- **Наследие контрольных точек.** `scripts/check-breakpoints.mjs` держит
  список из 17 значений вне реестра. Сводить по мере касания CSS, список
  только сокращать.

  **Пары `767/768`, `1100/1101`, `1200/1201` из этой работы исключены** —
  проверено замером 29.08.2026: разрыва нет, это правильно написанные
  взаимоисключающие лестницы, и граница `767/768` = портретный iPad. Трогать
  их значило бы менять раскладку планшетов ради порядка в числах.

  Начинать стоит с одиночных значений, которые ничего не отделяют попарно:
  `680`, `700`, `720`, `760`, `820`, `860` — все шесть лежат между `600` и
  `900` реестра, и каждое стоит проверить на вопрос «что оно на самом деле
  отделяет».
  6. P2: M-5 (решение Алекса: **вариант А** — оживить общий компонент
     `.md-table--cards`, локальную копию Конгрессов удалить), M-6, M-3, M-4,
     M-7, M-9.

- Три новых ключа `backup.blocked`, `backup.guard_restore`,
  `backup.guard_saved` заведены на пяти языках 28.08.2026. Формулировки
  рабочие, не термины служения, но вычитка носителем uk/en/pl/de не
  проводилась — при первых замечаниях править точечно в `shared/i18n/common.js`.

- `appointments/index.html` подключает `shared/backup.js`, но `CWBackup` в
  модуле не используется нигде (проверено `grep` по всей папке): файл едет в
  прекэше SW зря. Найдено 28.08.2026 при разборе C-1. Снимать вместе с шагом 5
  — и заодно решить, не нужен ли модулю `shared/db.js`, если восстановление
  когда-нибудь появится и на странице модуля (сейчас оно живёт только в хабе,
  где `db.js` подключён, и потолок версии поэтому известен).

- ~~РЕШЕНИЕ: понятие служебного года — общий слой или копия в Школе?~~ —
  **решено 24.08.2026: общий слой.** `shared/serviceyear.js` создан,
  Клиндарий переведён делегированием, см. Done.

- ~~РЕШЕНИЕ: порядок работ по архивам — А → Б → В или сразу В?~~ —
  **решено 24.08.2026: А → Б, В откладывается.** Основание Алекса: ни одна
  школа ещё не архивировалась, и архив понадобится только когда он всерьёз
  займётся модулем. А закрыто (см. Done), следующее — Б.

- ~~В общий справочник попадают не только собрания.~~ — **закрыто 25.08.2026**
  фильтром по `visitType` в `identity()` плюс `sweepNonCongregations()`. См. Done.

- ~~Подпись «Восстановить» в окне истории Клиндария мимо i18n~~ — **снято
  25.08.2026**: строки нет ни в `renderHistoryModal`, ни рядом. Либо
  исправлено попутно, либо запись была неточной с самого начала.
- ~~Два ключа Конгрессов `cong.alert.backup_unreadable` и
  `cong.alert.backup_failed` только на `ru`~~ — **снято 25.08.2026**: оба
  переведены на все пять языков. Запись устарела.
- ~~Две подписи резервных копий Конгрессов написаны прямо в коде, мимо i18n~~
  — **закрыто полностью 25.08.2026** (Конгрессы 4.44.2). Плумбинг сделан
  шестым заходом, перевод носителя залит седьмым: 2 ключа × 4 языка =
  8 значений, 0 расхождений. Обе подписи проверены живым прогоном в реальном
  списке копий на uk/en/pl/de. Долга по этим ключам больше нет.

- ~~⚠️ ДЕЙСТВУЮЩИЙ ДЕФЕКТ: правленый пользователем текст письма Конгрессов не
  применяется~~ — **закрыто 26.08.2026** (Конгрессы 4.45.0, Клиндарий 9.88.0).
  Чинилось тремя шагами по возрастанию необратимости: достижимость отката в
  обоих модулях, затем сам перенос, затем третья подпись копии. Разбор —
  `docs/documents/02-templates-migration-audit.md`. Попутно закрыты два места
  того же класса в Клиндарии: `docPages()` подставлял системную памятку
  координатору вместо правленой пользователем, `docActualLang()` отвечал
  языком системного шаблона вместо языка реального источника.
- ~~Третья подпись копии зашита по-русски~~ — **закрыто 26.08.2026** тем же
  заходом: `cong.backup.before_move_templates`.
  **Перевод залит 26.08.2026** (Конгрессы 4.45.1): 4 значения, 0 расхождений.
  Долг по подписям резервных копий Конгрессов закрыт полностью — все три
  подписи на всех пяти языках, проверены живым прогоном в реальном списке.
  **Приём на будущее:** в таблицу носителю были вложены две уже переведённые
  подписи как образец строя, и перед заливкой отдельно сверено, что он их не
  правил (10 значений, 0 расхождений). Справочные строки в редактируемом файле
  сверять обязательно — просить их не трогать мало. Заодно к решению Алекса: подпись взята с прописной
  буквы по образцу соседних — прежний литерал был со строчной, но его не видел
  ни один пользователь, потому что ветка не исполнялась. Отменяется правкой
  одной ячейки словаря.
- **Перенос НЕ сделан повторяемым — и это по-прежнему так** (решение Алекса
  26.08.2026: вариант 3, «не повторять, но сказать»). Если перенос сорвётся,
  второй попытки в этой сессии не будет — она случится при следующем запуске.
  Превращать необратимую операцию в «пробуем, пока не выйдет» — отдельное
  решение со своим разбором, и оно не принималось.
  **Закрыта была беззвучность, а не однократность** (Конгрессы 4.46.0):
  `adoptTemplates()` отвечает `"failed"` вместо общего `false`, и `main.js`
  показывает сообщение в полосе `#errorBox`. Ветка подтверждена прогоном на
  сорванной записи в базу ДО заведения ключа.
- ~~**Ключ `cong.alert.templates_move_failed` ждёт носителя**~~ — **залит
  28.08.2026** (Конгрессы 4.46.6): 4 значения, 0 расхождений в справочных
  строках. Долга перевода в репозитории больше нет вовсе — `check-i18n-coverage`
  показывает ноль и в §2, и в §3. История ниже оставлена как приём.
  Таблица собрана 28.08.2026 и отдана Алексу
  (`cong-templates-move-failed-28-08-2026.xlsx`): один ключ плюс две уже
  вычитанные строки того же модуля образцом строя. Значения образца взяты из
  словаря программно, а не набраны руками, — при сборке вручную все шесть
  ячеек оказались выдуманными.
  **Записью здесь этот долг больше не держится:** ключ, заведённый только
  по-русски, показывает `scripts/check-i18n-coverage.mjs`, §2. Заполнять
  «очевидным» переводом по-прежнему нельзя — потом не отличить от вычитанного
  носителем.

- **Приём: долг локализации проверять сканером, а не записями в этом файле.**
  25.08.2026 из пяти записей о долге три оказались устаревшими — `cp.delete_*`
  (4 ключа) и оба `cong.alert.backup_*` были переведены давно. Надёжный
  признак — русская кириллица в значениях `en`/`pl`/`de`; совпадение `uk`
  с `ru` признаком НЕ является, украинский законно совпадает в «Телефон»,
  «Тема», «Урок {n}».
- ~~Шестьдесят девять ключей ждут носителей~~ — **залиты 18.08.2026**
  (267 ячеек, 0 расхождений). ~~Ещё 62 строки из инлайновых таблиц `app.js`
  (`utils.colorName`, `VP_I18N_DICTS`)~~ — **залиты 18.08.2026 допником №2**
  (Клиндарий 9.84.0). Немецкий Клиндария закрыт целиком: интерфейс, названия
  цветов, печатный формуляр визита. С этого релиза такие таблицы стережёт
  `scripts/check-inline-lang-tables.mjs` — следующий язык не получится
  объявить готовым по одному каталогу `i18n/`.
- `cp.make_s302`: расклад не тот, что был записан (пересчитано 28.08.2026).
  Краткая форма «📋 S-302» только в uk/en/pl; немецкий — «📋 S-302 erstellen»,
  с глаголом, как и русский «📋 Сформировать S-302». То есть выбор не «русский
  против всех четырёх», а «ru + de против трёх». Решить, приводить ли к одной
  форме и к какой. Прежняя формулировка записи («носитель оставил краткую форму
  во всех четырёх языках») неверна.
- ~~`pioneer-school/sw.js` и `appointments/sw.js` читают кэш глобальным
  `caches.match()`~~ — **закрыто 28.08.2026**, см. Done. Запись была недосчитана
  вдвое: тем же глобальным чтением жили ещё `documents/sw.js` и
  `congress-project/service-worker.js` — четыре файла, а не два. Признак для
  будущих записей такого рода: искать по всему репозиторию, а не перечислять
  модули по памяти.
- ~~Корневому `manifest.json` не хватает `scope` и `id`~~ — **`scope` добавлен
  28.08.2026** во все пять манифестов, где его не было; Клиндарий объявлял его
  и раньше. Риска не оказалось: `scope` разрешается от адреса МАНИФЕСТА, и
  `"./"` посимвольно равно прежнему умолчанию (start_url без имени файла), то
  есть область не изменилась ни у одного приложения.
  **`id` по-прежнему не объявлен — и это осознанно.** Он разрешается от ORIGIN,
  а не от папки приложения, поэтому написать его можно только абсолютным путём
  вида `/Circuit-Workspace/index.html`. Адреса развёртывания в репозитории нет
  нигде, а ошибка в пути даёт не отказ, а НОВУЮ идентичность — то есть второй
  значок приложения на устройстве. Заводить `id` имеет смысл, только зная
  адрес; пока идентичностью служит `start_url`, как у всех модулей.
  Попутно снят `"id": "./"` Клиндария — см. Done.
- ~~Порядок подключения CSS расходится между модулями: `appointments` грузит
  `../shared/style.css` перед своим, Конгрессы и Школа — после.~~ — **запись
  устарела и указывала не на тот модуль** (проверено 28.08.2026). Все четыре
  модуля с отдельным CSS-файлом грузят общий слой ПЕРВЫМ, свой вторым;
  выровнено ещё 08.08.2026 («Три хвоста дизайн-системы»). Расхождение при этом
  существует, но оно в Клиндарии: у него ~335 строк инлайнового `<style>` в
  шапке (строки 13–348), а `../shared/style.css` подключается ПОСЛЕ них,
  строкой 349. То есть при равной специфичности у четырёх модулей побеждает
  свой стиль, а у Клиндария — общий. **Осталось решением Алекса:**
  переворачивать задним числом рискованно, нужен визуальный прогон, а поводов
  пока нет — ни один известный дефект сюда не упирается.
- ~~`.md-btn` в `shared/style.css` задаёт `height: 40px` жёстко~~ — **запись
  устарела**: `.md-btn` уже `min-height`, правка сделана раньше вместе с
  комментарием в файле. Третий подряд случай, когда запись здесь не
  соответствует коду — признак читать сканером, а не файлом.
  **Класс при этом был жив в других местах и закрыт 26.08.2026:** `.md-field`
  (поле адреса Назначений вылезало за карточку) и `.md-chip` (подпись вкладки
  в Документах обрезалась; корень — `md-chip-full` как признак выбора).
  Осталось наблюдением: `height` стоит ещё у `.md-sidenav__item` (48px),
  `.nav-item` Школы (40px), `.cw-btn` и `.cw-update-btn` хаба. Живой прогон на
  ru/de обрезки там не показал — трогать без повода не нужно, но при первой
  жалобе на длинном языке смотреть сюда.
- Совпадения `uk` с `ru` — **числа пересчитаны сканером 28.08.2026** и они
  другие: 24 в Клиндарии, 23 в Конгрессах, 19 в Школе, 17 в словаре документов
  Школы, 11 в общем, 4 в Назначениях, 1 в Документах — 99 против записанных
  здесь 54. Нужен просмотр носителем: часть законная омонимия, часть
  недопереведена, различить машинно нельзя.
  **Зато по надёжному признаку долга его нет вовсе:** русской кириллицы в
  значениях `en`/`pl`/`de` — ноль во всех семи словарях. Все совпадения там —
  марки и плейсхолдеры (`PDF`, `S-302`, `Google Maps`, `WhatsApp`, `EPUB`,
  `E-mail`, `№`, `{entity} · {lang}`). С этого числа обе величины считает
  `scripts/check-i18n-coverage.mjs`, а не запись здесь.

---

## Next

- ~~Перевести Клиндарий и Документы на `CWDocsView`.~~ — **закрыто 25.08.2026.**
  Копий рендерера в проекте не осталось. См. Done.

Ближайшие задачи.

- ~~**Цветовой слой: инверсия зависимости**~~ — **ТРЕК ЗАКРЫТ 27.08.2026**
  (хаб 0.38.0). Все фазы ниже помечены ✅ ещё тогда, а запись осталась висеть в
  «Next» — снято 28.08.2026. История фаз оставлена здесь намеренно: она и есть
  разбор, ради которого раздел писался.
  (Решения Алекса 26.08.2026, разбор —
  `docs/design-tokens/01-audit.md`). `--md-*` становятся источником правды,
  `--cw-*` — алиасами; собственные имена модулей приводятся к ролям MD3;
  остров пользовательского акцента Клиндария убирается — модуль живёт по тем
  же законам, что остальные, тон задаёт хаб. Значения выписываются числами,
  генератор палитры отклонён. Фазы:
  1. ✅ Тени светлой темы — сделано 26.08.2026.
  2. ✅ Реестр имён — сделано 26.08.2026,
     `docs/design-tokens/02-token-registry.md`. Часть оттенков оказалась
     мёртвой (`--cw-teal-dim`, `--cw-indigo`, `--cw-red`, `--blue`, `--red`),
     `--cw-teal`/`--cw-amber` складываются в существующие `--status-*`.
     Открыто одно решение: `--cw-lavender`/`--cw-violet`/`--cw-violet-dim`.
  3. ✅ Инверсия — сделано 26.08.2026. 3а (тёмная) ноль расхождений;
     3б (светлая) вариант А, тон задаёт хаб: поверхности стали фирменными,
     хаб 0, модули 200–614, тёмная 0.
  4а. ✅ Оттенки → роли — сделано 26.08.2026. Изменения только в хабе.
  4б-1. ✅ **Акцент Клиндария, всё кроме сайдбара — сделано 27.08.2026.**
     `--accent` → `--md-primary` (26 из 29 вхождений), заливка получила пару
     `--md-on-primary` вместо зашитого `color:#fff` (6 правил), `--accent-rgb`
     (20) и `--danger-rgb` (6) → `color-mix()`, оба объявления сняты,
     `--cal-*-bg` выведены из `--md-primary`. Попутно переведены 11 зашитых
     чисел, не следовавших за акцентом никогда: 8 × `rgba(20,83,45,…)`
     (зелёный от прежнего умолчания) и 3 × `rgba(138,92,246,…)`.
  4б-2. ✅ **Остров акцента снят целиком — 27.08.2026** (Клиндарий 9.91.0).
     Сайдбар по варианту C Алекса: плоский `--md-primary-container` +
     `--md-on-primary-container`, градиент снят и в `.sidebar`, и в подложке
     `body::before`. Вместе с фоном вычищен весь зашитый белый внутри
     сайдбара — семь правил; `.nav-btn` красился в `#eef7f1`, зеленоватый
     белый той же эпохи, что и восемь `rgba(20,83,45,…)` шага 4б-1.
     Переключатель убран целиком: `applyAccent()`, вызов, слушатель, реестр
     элементов, `renderSettings()`, разметка семи вариантов, `.accent-preview`,
     объявления `--accent`/`--accent2`, подмена `<meta name="theme-color">`,
     9 ключей × 5 языков. `settings.accentColor` в данных оставлена — проверено
     сценарием, что установка с `accentColor: 'red'` переживает загрузку,
     `save()` и перезагрузку, а цвет не применяется.
  6. ✅ **Вымывание `--cw-*` — ЗАКРЫТО 27.08.2026** (хаб 0.38.0).
     6-1: двенадцать прямых алиасов, 72 вхождения, 23 объявления, 0 расхождений.
     6-2: `--cw-bg-raised`/`--cw-surface`/`--cw-surface-2` сведены к роли тёмной
     темы (правило Алекса по `--surface`); `--cw-violet-dim` разведён по
     потребителям — рамка наведения в `--md-primary`, заливка живого блока в
     `--md-primary-container`. Починен зашитый белый на primary в четырёх
     правилах (в тёмной теме давал 1.7:1). Осталось намеренно: нецветовые
     `--cw-*`, плюс `--cw-shadow`/`--cw-shadow-sm` и `--cw-module*` — цветные,
     но роли MD3 для них нет (случай `--font-mono` Школы).
  Проверка на каждой фазе: замер вычисленных цветов всех видимых элементов на
  семи страницах в обеих темах до и после; фазы 3–6 обязаны давать ноль —
  кроме мест, где расхождение назначено решением ЗАРАНЕЕ и описано пофамильно.
  Обязателен контроль, что сценарий различает темы, иначе ноль пустой.

- **Смена фирменного цвета — ручная процедура, раз в год-три, скрипта НЕ
  заводим** (решение Алекса 26.08.2026). Генератор палитры отклонён: сборки в
  проекте нет, а инструмент пришлось бы поддерживать и он сам стал бы
  источником ошибок. Ручная работа выполнима потому, что от фирменного цвета
  зависит НЕ вся палитра, а только семейство `primary` — около двенадцати
  значений в двух темах. Поверхности, рамки и error к нему не привязаны:
  они намеренно взяты фиолетово-подкрашенными из фирменной палитры, а не
  тем, что даёт алгоритм, и пересчёту не подлежат.

  Порядок при смене (записан заранее, чтобы не вспоминать через три года):
  1. Поменять цвет в `shared/brand/icon-source.svg`, пересобрать иконки.
  2. Пересчитать семейство `primary` от нового сид-цвета (Material Color
     Utilities онлайн, вне репозитория) — светлая и тёмная тема.
  3. Обновить комментарий в шапке `shared/style.css` В ТОМ ЖЕ ЗАХОДЕ: там
     записан сид-цвет и его происхождение.
  4. Проверить `<meta name="theme-color">` на всех страницах.
  5. Прогон: снять вычисленные цвета всех элементов в обеих темах, убедиться,
     что изменилось только семейство `primary` и ничего кроме.

- **ОТЛОЖЕНО Алексом (25.08.2026): задача В — архив прошедших школ по
  служебным годам.** Не приоритет, вернуться позднее. Состояние на момент
  откладывания: ни одна школа ещё не архивировалась, то есть данных, которые
  можно потерять, пока нет — но риск задачи высокий и по данным необратимый.
  Предпосылки уже готовы: понятие служебного года вынесено в
  `shared/serviceyear.js` (24.08.2026), хранилища `documents` и `snapshots`
  в `shared/db.js` есть, `CWDocs` работает. **Условие начала:** аудит-документ
  по образцу `docs/db-migration/02-communities-audit.md` до единой строчки
  кода — что считается «прошедшей» школой, кто владеет этим признаком, что
  происходит с выданными документами архивированной школы и обратима ли
  операция.


- ~~**`CWPrint.document()`: `setTimeout(250)` → `onload`.**~~ — **сделано
  28.08.2026**, см. Done. Сделано ровно так, как было записано: сигнал — `load`,
  фиксированное ожидание осталось страховкой (3 с). Прибавилось то, чего в
  записи не было: `load` оказался необходимым, но НЕ достаточным условием, и
  после него нужны два кадра.

- **Описания новых переменных для справочника Клиндария.** Движок подставляет
  `congregation.address`, `congregation.contactPhone/contactEmail`, `visit.type`
  и все поля `sender.*`, но в таблице справочника их нет: у них нет описания в
  словаре, а описания пишет носитель языка. Ключи `ph_*` переведены на все пять
  языков (17.08.2026) — новым нужны такие же; в допник они не попали, потому что
  ключей для них ещё нет вовсе. Строка без описания в справочнике бесполезна,
  поэтому добавлять их одновременно с текстами.
- **Печатный бланк по-прежнему разложен вручную.** После схема-рендеринга
  онлайн-формы остался последний носитель структуры анкеты, не собираемый из
  схемы: `pdfExport.downloadRegistrationBlankForm`. Названия и порядок разделов
  он уже берёт из схемы (`_sectionHeading`), но сами поля расставлены
  подобранными координатами в две колонки — генерация «как получится» испортила
  бы готовую бумагу. Делать только вместе с раскладочными подсказками в схеме
  (ширина поля, перенос ряда), не раньше.
- **Линия «другой язык» на бланке пропадает при длинных переводах.** Ряд
  чекбоксов языков заканчивается подписью «Другой:», после неё рисуется линия
  для ответа. Немецкое «Andere Sprache:» дотягивается до 553 из 555 — места
  не остаётся, и линия не рисуется совсем: поле для ответа исчезает. Сейчас
  безопасно (переводов нет), всплывёт вместе с ними. Чинится переносом ряда
  или отдельной строкой под ним.
- ~~**Расхождение «Печатный» / «Печатный экземпляр» между слоями.**~~ —
  **решено Алексом 28.08.2026: «Печатный экземпляр», так точнее** (Школа
  1.13.13). Выровнена ФОРМУЛИРОВКА, ключи НЕ слиты: `doc.ps.reg.opt.format.print`
  и `ps.reg.pechatnyy_ekzemplyar` живут в разных слоях (язык документа против
  языка интерфейса), и разделение слоёв остаётся. Тексты на четыре языка взяты
  из уже вычитанного носителем ключа интерфейса, а не сочинены заново.
- ~~Включить немецкий интерфейс Клиндария~~ — **сделано 18.08.2026**
  (9.83.0 — включение, 9.84.0 — допник №2). `'de'` в `SUPPORTED`, `NEAREST`
  пуста, опции языка документа на месте, формуляр визита печатается
  по-немецки. Осталось наблюдение, а не задача: немецкий длиннее русского
  примерно на четверть, и при первой же жалобе на обрезанную подпись в шапке
  или на кнопке править точечно — живой прогон 18.08.2026 переполнений не
  показал.
- **PIN Клиндария заменить единым входом хаба** (решение Алекса 15.08.2026).
  Отдельной фазой, НЕ внутри миграции данных: смешивать два необратимых
  изменения в одном заходе нельзя. Что решить до кода: где живёт хэш
  (`shared/`, не в резервной копии — так же, как `syp-pin-hash` сейчас), что
  происходит со старым ключом при переходе, и закрывает ли вход хаба сами
  модули (их открывают по прямой ссылке, минуя хаб).

---

## In Progress

То, что сейчас реализуется.

- ~~**Миграция Конгрессов и Клиндария на `shared/db.js`.**~~ — **трек закрыт,
  все пять фаз** (аудит — `docs/db-migration/01-audit.md`; вариант **Б
  поэтапно**, решение Алекса 15.08.2026). Фаза 1 — отложенная запись; фазы 2–3 —
  блоб состояния в базе; **фаза 4 — снимки и автокопии (16.08.2026,
  `shared/snapshots.js`, `CWDB` v5)**; **фаза 5 — общий справочник собраний,
  закрыта 16.08.2026**, связывание отменено решением Алекса, шаг 4б не наступит.
  Всё — в разделах AGENTS.md «Миграция на `shared/db.js`».
  **Запись пролежала здесь устаревшей двенадцать дней** и звала следующей фазу,
  сделанную в тот же день, когда была закрыта предыдущая. Четвёртый случай
  подряд, когда TODO расходится с кодом, — читать сканером, а не файлом.
- **Судьба старого ключа `service-year-planner-v9-4-2`.** Фаза 2 его сохранила
  как дореформенный снимок — в этом обратимость фазы. Решить, когда его
  удалять (не раньше, чем переезд отработает у живого пользователя), и что
  делать с его местом в реестре `shared/backup.js`: сейчас копия модуля везёт
  и его, то есть состояние на день переезда.

---

## Done

Закрытые задачи вынесены в `docs/todo-archive/2026-08.md` (30.08.2026,
фаза 2 аудита контекста): раздел занимал 55 % файла. Раздел остаётся —
сюда пишутся задачи, закрытые в текущем месяце.
